# DECISIONS.md — יומן החלטות (ADR)

תיעוד ההחלטות המשמעותיות בפרויקט: ההתלבטות, האפשרויות, מה נבחר ולמה, והאם היום היינו בוחרים אותו דבר.
מעדכנים בכל החלטה גדולה חדשה או כשהחלטה קיימת נבחנת מחדש.

מקרא הערכה: 🟢 נשאר · 🟡 עובד אך שווה מעקב · 🔴 חוב טכני לתיקון בהזדמנות

---

## 1. פלטפורמה: אפליקציית Desktop מול Web  🟢
- **התלבטות:** איך המנהל מפעיל את הכלי — תוכנה על המחשב או אתר/ענן?
- **אפשרויות:** (א) Desktop יחיד (Python/PyQt6, EXE) · (ב) Web app + שרת · (ג) להישאר על Excel.
- **נבחר:** Desktop יחיד. **למה:** משתמש יחיד, מחשב אחד, בלי צורך בתחזוקת שרת/עלויות/חשבונות; עובד גם בלי אינטרנט; NetFree על המכונה מקשה על פתרונות רשת ממילא.
- **היום?** כן. עבור משתמש בודד זו הבחירה הנכונה. Web היה מוסיף מורכבות ועלות בלי תועלת אמיתית.

## 2. מסד נתונים: SQLite קובץ יחיד  🟢
- **התלבטות:** איפה לשמור ~500 מקבלים + היסטוריה.
- **אפשרויות:** (א) SQLite · (ב) Excel כמאגר · (ג) DB שרת (Postgres/MySQL).
- **נבחר:** SQLite. **למה:** קובץ אחד, בלי שרת, טרנזקציות אמינות, גיבוי פשוט.
- **היום?** כן, מובהק. Excel כמאגר היה שביר; שרת מיותר למשתמש יחיד.

## 3. מודל נתונים: ריבוי מוצרים כמחרוזת ב-`what_dist`  🔴
- **התלבטות:** איך לשמור "מה חולק" כשיש כמה מוצרים בחלוקה אחת.
- **אפשרויות:** (א) מחרוזת מקודדת בעמודה אחת (`what_dist`) · (ב) טבלת `distribution_items` נפרדת (חלוקה→שורות מוצר).
- **נבחר:** מחרוזת. **למה:** מהיר לבנייה, מספיק להדפסת רשימות ולתצוגה.
- **היום?** **לא בהכרח.** זה החוב הטכני הבולט ביותר: אי אפשר לשאול "כמה אורז חולק החודש" או לנתח מגמות מוצר בלי לפרסר טקסט. כל עוד אין דרישה לדוחות-מוצר — לא דחוף. אם תעלה דרישה כזו — לעבור לטבלת פריטים עם מיגרציה. **לתעד כחוב מודע.**

## 4. סיסמה: מ-plaintext ל-PBKDF2  🟢
- **התלבטות:** אבטחת כניסה למשתמש יחיד מקומי.
- **אפשרויות:** (א) plaintext "1234" · (ב) hash PBKDF2 · (ג) בלי סיסמה.
- **נבחר:** התחיל plaintext, **שודרג ל-PBKDF2** (`verify_password/set_password`).
- **היום?** כן. השדרוג היה נכון. למשתמש מקומי יחיד זה מספיק; אין צורך בהרשאות/תפקידים.

## 5. גיבוי: SQLite Online Backup API + שימור ב-3 דליים  🟢
- **התלבטות:** איך לגבות בלי לסכן את ה-DB החי.
- **אפשרויות:** (א) העתקת קובץ גולמית · (ב) Online Backup API (עותק עקבי) · (ג) בלי גיבוי אוטומטי.
- **נבחר:** Online Backup API, לפני כל פעולה הרסנית + בהפעלה; שחזור מאמת תקינות לפני דריסה.
- **עדכון (סבב יציבות, תקלה C1):** טבעת ה-30 היחידה הוחלפה ב**שלוש דליים נפרדות** ב-`utils/backup.py` — `backup_*` (routine, 20) · `daily_*` (אחד ליום, 30) · `safety_*` (לפני-הרס, 10). כך גיבוי-בטיחות לא מפונה ע"י שמירות שגרתיות (היה חלון-שחזור קצר מדי). שם-קובץ במיקרו-שניות + לולאת-ייחוד למניעת דריסה. בנוסף (C1-partner, H3): `self_heal_db` בוחר את הגיבוי **החדש-ביותר התקין והלא-ריק** במקום זה עם הכי הרבה מקבלים, כדי לא להחיות נתונים שנמחקו במכוון.
- **היום?** כן. עומד בעדיפות "שלמות נתונים" שהיא ראש הסדר בפרויקט.

## 6. הפרדת שכבות: לוגיקה עסקית מחוץ ל-UI  🟢
- **התלבטות:** איפה יושבים החישובים (ניקוד/תאריכים).
- **אפשרויות:** (א) בתוך הלשוניות · (ב) מודולים טהורים (`scoring.py`/`database.py`), לשוניות מציגות בלבד.
- **נבחר:** מודולים טהורים. **למה:** ניתן לבדיקה, לא משוכפל, קל לתחזוקה.
- **היום?** כן. מאפשר את חבילת הבדיקות העצמאיות. עיקרון נכון להמשיך לאכוף.

## 7. זרימת מתנדבים: סבב Excel במייל (לא Google Forms/Sheets)  🟢
- **התלבטות:** איך מתנדבים (שלא נוגעים בתוכנה) יסמנו מי הגיע.
- **אפשרויות:** (א) Google Form · (ב) Google Sheet משותף · (ג) קובץ Excel נשלח במייל וחוזר לייבוא.
- **נבחר:** Excel במייל הלוך-חזור (עמודת id מוסתרת להתאמה, "הגיע?" ברירת מחדל "כן").
- **למה:** Google Forms נחסם במייל (סינון), Sheets דורש API מורכב; Excel פשוט למתנדב ולא דורש חשבון.
- **היום?** כן, בהינתן האילוצים. השיפור הפתוח: ייבוא אוטומטי דרך IMAP של מיילים חוזרים.

## 8. איחוד לשוניות: 10 גיליונות → 6 לשוניות  🟢
- **התלבטות:** ה-Excel המקורי היה 10 גיליונות; כמה לשוניות באפליקציה.
- **אפשרויות:** (א) לשונית לכל גיליון · (ב) איחוד לפי זרימת עבודה (שבועי+קבוצתי→"חלוקה ורישום"; מעקב/כפילויות→דיאלוגים).
- **נבחר:** איחוד ל-6 לשוניות, כלים משניים כדיאלוגים.
- **היום?** כן. פישוט נכון למשתמש לא-טכני. שווה להיזהר מ"דיאלוג בתוך דיאלוג" שמסתיר פונקציות.

## 9. עדכון אוטומטי: EXE שמחליף את עצמו מ-GitHub Releases  🟡
- **התלבטות:** איך לדחוף עדכונים בלי מתקין.
- **אפשרויות:** (א) עדכון ידני (החלפת קובץ) · (ב) self-update מ-GitHub · (ג) מתקין (MSI/Inno).
- **נבחר:** self-update מ-`releases/latest`, מוריד ומחליף את ה-EXE הרץ.
- **היום?** בסך הכל כן, אך **שווה מעקב:** תלוי ברשת ורגיש ל-NetFree 418; יש כבר "פצע" ידוע (`os._exit` לעקיפת ניקוי `_MEI` של PyInstaller). עובד ובטוח-לנתונים, אבל זו נקודת שבירות. מתקין אמיתי היה יציב יותר אך כבד יותר.

## 10. עיצוב וגופן: qt-material + Segoe UI (חזרה מ-Rubik)  🟢
- **התלבטות:** מראה מקצועי + טקסט חד בעברית.
- **אפשרויות:** (א) Rubik · (ב) Segoe UI · qt-material כ-theme.
- **נבחר:** Segoe UI (Rubik רונדר מטושטש), theme light_blue.
- **היום?** כן. מלכודת מתועדת — לא להחזיר Rubik.

## 11. משוב מהמשתמש: Google Form + JSONL מקומי  🟢
- **התלבטות:** איך לאסוף משוב מתוך האפליקציה.
- **אפשרויות:** (א) פתיחת issue ב-GitHub עם token מוטמע · (ב) Google Form + לוג מקומי.
- **נבחר:** Google Form + JSONL. **למה:** גישת ה-token ננטשה (סיכון אבטחה בהטמעת token ב-EXE).
- **היום?** כן. הימנעות מ-secret ב-EXE הייתה הבחירה הנכונה.

---

### חוב טכני פתוח (לפי סדר חשיבות)
1. **`what_dist` כמחרוזת** (החלטה 3) — חוסם ניתוח ברמת מוצר. לשקול טבלת פריטים כשתעלה דרישה.
2. **תלות ה-self-update ברשת/NetFree** (החלטה 9) — לשקול מנגנון נפילה-לאחור ידני ברור אם יכשל.
3. `tabs/summary.py` לא מחובר, `tabs/tracking.py` קוד מת — לנקות בהזדמנות.
