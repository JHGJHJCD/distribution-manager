---
description: סקירת באגים ובדיקת תרחישים ממוקדת למסך ולוגיקת הצינתוקים (tabs/tzintukim.py + utils/yemot.py) — קורא, מדמה תרחישים, מתקן, מאמת ומשחרר
---

# בדיקת תרחישים ואיתור באגים — מסך הצינתוקים

טען את הסקיל **`tzintuk-check`** ועבוד לפיו מתחילתו ועד סופו — הוא מכיל את גדרות הבטיחות,
מפת הזרימות, רשימת התרחישים, האינווריאנטים, מתכון הבדיקה המדומה, ואת הבודק האוטומטי
(`scripts/check_tzintuk.py` — לינט + בדיקות). טען גם את `manhal-haluka` (Python 3.12, שחרור, `references/pitfalls.md`).

מיקוד לסקירה הזו (אם ריק — סקירה מלאה): **$ARGUMENTS**

תזכורת לסדר: בודק (baseline) → מפה לפי זרימות → תרחישים + אינווריאנטים → בדיקה שנכשלת לכל באג
→ תיקון לפי סדר העדיפויות → בודק ירוק + `test_all.py` → צילום דרך Gemini אם נגעת ב-UI →
מלכודת ב-pitfalls + לינט חדש ב-`check_tzintuk.py` + CLAUDE.md/NEXT_TASK → שחרור → סיכום ליהודה.
