---
description: בנה חיבור — מחבר/מאמת מקצה-לקצה את התוכנה מול ימות המשיח (פרטי גישה, כניסה, מזוהה, תבנית, /76, שרת המענה) ומדווח בעברית פשוטה; קריאה בלבד, בלי חיוג
---

טען את הסקיל **`yemot-connect`** ופעל לפי "הזרימה של בנה חיבור" שבו — מתחילתו ועד סופו,
בלי לשאול שאלות ביניים: הרץ את `scripts/connect.py`, ואם יש שלב אדום שהסקיל יודע לתקן
(`--set` מפרטי הגישה בסקיל הפרטי `yemot-line-knowledge`, `--fix` לתבנית, `--caller` למזוהה) —
תקן והרץ שוב עד שהדוח ירוק או שנשאר רק מה שדורש יד אדם (שורש הקו / נטפרי / סיסמה שהשתנתה בימות).

גדרות: לעולם לא מחייגים ולא כותבים לשורש הקו. הסיסמה לא מודפסת לצ'אט ולא נכתבת לקובץ ב-git.

מיקוד/הערה מיהודה (אם יש): **$ARGUMENTS**

בסוף: 2–4 שורות ליהודה — מה ירוק, מה תיקנתי, מה (אם בכלל) עדיין דורש אותו.
