---
description: סקירת באגים ובדיקת תרחישים ממוקדת למסך ולוגיקת המיילים (tabs/mails.py + utils/mailer.py + utils/google_auth.py) — קורא, מדמה תרחישים, מתקן, מאמת ומשחרר
---

# בדיקת תרחישים ואיתור באגים — מסך המיילים

טען את הסקיל **`mail-check`** ועבוד לפיו מתחילתו ועד סופו — הוא מכיל את גדרות הבטיחות
(בלי שליחה אמיתית, בלי נגיעה בחשבון Google האמיתי, סודות לא ל-git), מפת הזרימות, רשימת
התרחישים, האינווריאנטים, מתכון הבדיקה המדומה, ואת הבודק האוטומטי
(`scripts/check_mail.py` — לינט + בדיקות). טען גם את `manhal-haluka` (Python 3.12, שחרור, `references/pitfalls.md`).

מיקוד לסקירה הזו (אם ריק — סקירה מלאה): **$ARGUMENTS**

תזכורת לסדר: בודק (baseline) → מפה לפי זרימות → תרחישים + אינווריאנטים → בדיקה שנכשלת לכל באג
→ תיקון לפי סדר העדיפויות → בודק ירוק + `test_all.py` → צילום דרך Gemini אם נגעת ב-UI →
מלכודת ב-pitfalls + לינט חדש ב-`check_mail.py` + CLAUDE.md/NEXT_TASK → שחרור → סיכום ליהודה.
