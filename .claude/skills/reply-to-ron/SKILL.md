---
name: reply-to-ron
description: מענה במייל לרון, מנהל הקופה (ka230846@gmail.com) — בכל בקשה בסגנון "תענה לרון", "תשאל את רון", "תגיד לו שזה מוכן", "שלח לו במייל". הסקיל קובע את הפורמט הקבוע שיהודה הכריע עליו (16/9/2026)‏: HTML מיושר לימין, הכי קצר שאפשר, שורה ראשונה מודגשת שההודעה מקלוד, חתימה "קלוד (בשם יהודה)", תשובה בתוך השרשור המקורי. לטעון לפני כל שליחה לרון — גם אם יהודה לא ביקש "לפי הסקיל".
---

# מענה לרון (מנהל הקופה) במייל

רון שולח בקשות קצרות ליהודה מ-**`ka230846@gmail.com`** (למשל "שיהיה אופציה לסמן נתמך חגים").
המייל יוצא מהג'ימייל של יהודה (`xvxv99996@gmail.com`), ולכן **חייב** להיות ברור שקלוד כתב אותו.

## הכרעות יהודה (קבועות — לא לשאול שוב)
1. **מיושר לימין תמיד** — HTML עם `dir="rtl"`. טקסט-רגיל מוצג בג'ימייל שמאלה (נדחה 15/9/2026).
2. **הכי קצר שאפשר** (16/9/2026): 2–4 שורות. בלי "שלום", בלי "תודה", בלי הסברים טכניים.
3. **הדגשה שזה מקלוד:** שורה ראשונה מודגשת `🤖 הודעה מקלוד (העוזר של יהודה)`, חתימה `קלוד (בשם יהודה)`.
   לעולם לא להציג את המייל כאילו יהודה הקליד אותו.
4. **בתוך השרשור המקורי** (Re: + threadId + inReplyTo) — לא מייל חדש.
5. שליחה רק אחרי הוראה מפורשת של יהודה בצ'אט ("תענה לו", "שלח"). ניסוח/שאלה קודם — בסדר.

## איך (3 צעדים)
1. טעינת כלים: `ToolSearch "select:mcp__gmail__search_emails,mcp__gmail__read_email,mcp__gmail__send_email"`.
2. מציאת השרשור: `search_emails` עם ביטוי מהבקשה (`"נתמך חגים" newer_than:30d`) → לקרוא את ההודעה
   האחרונה של רון (`read_email`) → `Thread ID` + מזהה ההודעה האחרונה.
3. שליחה:
   ```
   to=["ka230846@gmail.com"], subject="Re: <הנושא המקורי>",
   threadId=<Thread ID>, inReplyTo=<מזהה ההודעה האחרונה של רון>, mimeType="text/html",
   htmlBody='<div dir="rtl" style="text-align:right;font-family:Arial,sans-serif;font-size:15px">
     <p><b>🤖 הודעה מקלוד (העוזר של יהודה)</b></p>
     <p>…התוכן, שורה-שתיים…</p>
     <p>קלוד (בשם יהודה)</p></div>',
   body=<אותו טקסט בלי תגיות>   (נפילה לטקסט-רגיל)
   ```

## תבניות
- **"מוכן":** `מוכן בגרסה X.YZ — התוכנה תציע לך את העדכון לבד.` + שורה אחת מה נוסף במילים של רון.
- **שאלה:** רשימה ממוספרת `<ol><li>` — שאלה אחת לכל אי-ודאות, קצרה.
- **לא מובן/חסר:** שורה אחת מה חסר כדי להתחיל.

## היסטוריה
- 15/9/2026 — שאלות על "נתמך חגים" (שרשור "אשמח"); רון ענה 16/9 "גם וגם" + סינון ליד קבוע.
- 16/9/2026 — "מוכן בגרסה 3.52" — הפורמט הקצר הזה אושר ע"י יהודה והפך לכלל.
