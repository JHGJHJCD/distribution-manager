# תוכניות עתיד — מנהל חלוקה

קובץ זה מרכז **רעיונות ותוכניות לעתיד** — דברים שרוצים לעשות מתישהו, אבל עדיין לא עכשיו.
זה **לא** קובץ הסטטוס השוטף (איפה עצרנו) — הסטטוס נמצא ב-`NEXT_TASK.md`.

איך משתמשים בקובץ:
- כל רעיון חדש לעתיד → מוסיפים כאן פסקה חדשה, גם אם זה רק משפט אחד.
- כשמתחילים לממש רעיון בפועל → מעבירים אותו ל-`NEXT_TASK.md` (סטטוס) ומסמנים כאן ✅ **בביצוע/הושלם**.
- כל רעיון מקבל: **מה הרעיון**, **למה זה טוב**, ו(אם ידוע) **איך זה יכול לעבוד**.
- לרעיונות שכבר יש להם מסמך-תכנון מפורט — יש מצביע לקובץ המלא (הקובץ המפורט נשאר במקומו).

---

## 💡 רעיון #1 — לשאול את רון דרך צינתוק כשיש שאלת-העדפה

**סטטוס:** רעיון בלבד (טרם מומש, טרם תוכנן לעומק).

**מה הרעיון (בפשטות):**
בכל פעם שהתוכנה (או אני, קלוד, בזמן עבודה) נתקלת בשאלה שהתשובה עליה תלויה בהעדפה של **רון** —
מנהל הקופה, מי שבאמת מפעיל את התוכנה — במקום לנחש או לחכות שרון ייכנס למחשב, **התוכנה מתקשרת
אליו בטלפון דרך מנגנון הצינתוקים** (ימות המשיח) ושואלת אותו ישירות, והוא עונה בהקשה על הטלפון.

**למה זה טוב:**
- רון לא תמיד ליד המחשב, אבל תמיד עם טלפון ביד.
- החלטות שהיום "תקועות" עד שמישהו שואל אותו — נסגרות מיד.
- משתמש במנגנון שכבר קיים ובנוי היטב (צינתוקים + שרת המענה בשלוחה /76), במקום לבנות משהו חדש.

**איך זה יכול לעבוד (מחשבות ראשוניות — לא סופי):**
- זיהוי "רגע של שאלת-העדפה" — למשל: איזה מצב חלוקה לבחור, האם לכלול קבוצה מסוימת, כמה רזרבה,
  אישור לפני פעולה חריגה.
- התוכנה מנסחת שאלה קצרה של כן/לא או בחירה 1/2/3, ממירה לקול (TTS — כבר קיים ב-`utils/tts.py`),
  ומצלצלת לרון.
- רון עונה בהקשה, והתשובה חוזרת דרך שרת המענה (שלוחה /76, `utils/callback_server.py`) אל התוכנה,
  שממשיכה לפי מה שהוא בחר.
- אפשר להתחיל **פשוט**: רק שאלות כן/לא, מספר קטן של סוגי-שאלה מוגדרים מראש.

**מה צריך לברר לפני מימוש (פתוח):**
- אילו שאלות באמת שוות צלצול (לא להטריד את רון על כל דבר קטן).
- איך רון מזוהה ואיך התשובה מגיעה חזרה בזמן אמת אל התוכנה.
- מה קורה אם רון לא עונה — ברירת מחדל / המתנה / התראה במחשב.
- האם השאלה נשאלת רק כשקלוד עובד, או גם באופן אוטומטי בזמן ריצת התוכנה.

**קשור ל:** מנגנון הצינתוקים הקיים (`tabs/tzintukim.py`, `utils/yemot.py`, `utils/callback_server.py`,
שרת המענה בשלוחה /76), TTS (`utils/tts.py`).

---

## 💡 רעיון #2 — "מצלמת אבטחה" לקו ימות: מעקב אחרי כל שינוי בתצורת הקו

**סטטוס:** תוכנית מפורטת קיימת, **טרם מומש**. מסמך מלא: `LINE_WATCH_PLAN.md`.

**מה הרעיון:**
הקו של הקופה בימות **משותף** עם גורמים אחרים (גבאי שבת, מתרימים) — ולמנהל השני יש סיסמת-קו
מלאה. הסכנה: מישהו ישנה/ימחק בטעות את שלוחות הקופה (76/77) או ישחזר גיבוי ישן, והצינתוקים
יישברו בלי שנדע. הרעיון: מערכת ש**מצלמת** את תצורת הקו כל הזמן ומראה **מי שינה, מתי, ומה בדיוק**
— עם התראה ואפשרות "החזר את המצב הקודם".

**למה זה טוב:**
- שקט נפשי — אם משהו נשבר בקו, רואים מיד מה השתנה ומתי, ואפשר לשחזר.
- לימות יש יומנים מובנים (מי נכנס לניהול ומתי) שאפשר למשוך — נותן "מי + מתי" אמיתי.

**איך זה יכול לעבוד (תמצית — הפירוט ב-`LINE_WATCH_PLAN.md`):**
- **שכבה בתוך התוכנה:** מצלמת את כל השלוחות/הניתובים/התבניות, משווה לצילום הקודם, ומראה diff
  שורה-שורה + התראה + כפתור שחזור.
- **שכבה בענן (24/7):** Cloudflare Worker עם Cron שמצלם את הקו כל כמה שעות גם כשהמחשבים כבויים.

**סיכון שצריך לזכור:** להחזיק את סיסמת הקו בענן = סיכון אבטחה (המשתמש אישר במפורש בתכנון).

---

## 💡 רעיון #3 — צינתוק חינמי: רישום שקט של מחייגי 04 לרשימת-צינתוק

**סטטוס:** תוכנית מפורטת קיימת, **לא מאושר לביצוע** (דורש אישור חדש + תיאום רון). מסמך מלא:
`dev/plan_auto_register_04_tzintuk.md`.

**מה הרעיון:**
כל צינתוק שהתוכנה שולחת עולה ~0.1 יחידה למספר. שליחה ל**רשימת-צינתוק בשם** היא **חינם**.
הרעיון: לצרף אוטומטית ובשקט כל מי שמתקשר ל-04 לרשימת-צינתוק חינמית משלנו, כך שלאורך זמן
הרשימה תתמלא מהמקבלים הפעילים — ואליהם נצלצל **בחינם** במקום לשלם.

**למה זה טוב:**
- חוסך כסף (יחידות) בכל שליחה למי שכבר ברשימה.
- שכבה זולה שגדלה לבד עם הזמן, בלי מאמץ.

**מה פתוח / מגבלות:**
- ה-API לא מסיר מספר בודד מרשימה — צריך אסטרטגיה לצלצל חינם רק למי שזכאי השבוע.
- מתמלא לאט (רק ממי שבעצמו מתקשר, ורק מספר מזוהה).
- **שער בטיחות:** נוגע בשורש הקו המשותף — רק באישור רון + תוכנה סגורה בשני המחשבים + גיבוי.

---

## 💡 רעיון #4 — חיבור לנדרים פלוס (SMS והתאמת משפחות)

**סטטוס:** מתוכנן, **טרם מומש**. ידע מלא בסקיל `nedarim-plus-knowledge` (חוזה API ב-`references/api-full.md`).

**מה הרעיון:**
הקופה כבר משתמשת ב**נדרים פלוס** (matara.pro) לניהול כרטיסים נטענים למשפחות — בדיוק אותן ~500
משפחות שבתוכנה. אינטגרציה תאפשר להתחבר לנתונים האלה.

**למה זה טוב:**
- ה-SMS-API של נדרים פלוס יכול לשמש **התראת טקסט** למקבלים — במקביל לצינתוק הקולי הקיים
  (מי שלא עונה לטלפון יקבל SMS).
- אותה ישות "משפחה" בשתי המערכות → אפשר לסנכרן/להתאים נתונים ולחסוך הזנה כפולה.

**מה פתוח / מלכודות:**
- **use-case ראשון מומלץ:** SMS (הכי פשוט).
- טרם אומת מול חשבון אמיתי. מפתח `npk_` חובה; **10 מפתחות שגויים = חסימת IP לשעה** — לא לנסות בלולאה.
- נטפרי חוסם את matara.pro (ראה זיכרון `reference-netfree`).

---

## 💡 רעיון #5 — לפשט את המסך הראשי ("מה עושים היום")

**סטטוס:** בריף מפורט קיים, מומש חלקית (צעד 1 — קיפול "חלוקה מיוחדת"). המשך **טרם מומש**.
מסמך מלא: `UX_SIMPLIFY_BRIEF.md`.

**מה הרעיון:**
להפוך את המסך הראשי מ"טופס-הגדרות עמוס" ל"החלוקה של השבוע, מוכנה ללחיצה" — פחות כפתורים
ופחות החלטות בפעולה היומית, בלי לשנות לוגיקה עסקית.

**מה עוד נשאר (מ"מפת ההמשך" בבריף):**
- **בינוני:** מיזוג בחירת החד-פעמיים לתוך אותו מסך; צמצום הלשוניות; כותרת-סטטוס למעלה
  (תאריך, כמה מקבלים, כמה נפשות).
- **גדול:** מסך-בית מוכוון-משימה עם 3 צעדים (רשימה ← שליחה/חלוקה ← רישום); אשף לחלוקה מיוחדת;
  המערכת מזהה לבד רגיל מול מיוחד.

**עיקרון מנחה:** אם המערכת יכולה להחליט במקום המשתמש — שלא תכריח אותו להחליט.

---

## 💡 רעיון #6 — ניקוי אוטומטי של תזמוני צינתוק ישנים

**סטטוס:** רעיון תחזוקה קטן, **טרם מומש** (המשתמש ביקש "בלי לגעת" בקו כרגע). רקע ב-`NEXT_TASK.md`.

**מה הרעיון:**
בקו נערמים תזמוני `RunTzintuk` ישנים (נמצאו 84 משימות "מתות" שכבר רצו). התוכנה יוצרת תזמון בכל
שליחה מתוזמנת ולא מוחקת אותו אחרי שרץ. הרעיון: אחרי שתזמון רץ — למחוק אותו (או ניקוי תקופתי
של תזמונים שעבר זמנם), כדי שלא ייערמו.

**למה זה טוב:** מסדר את הקו, מונע בלבול בפאנל, ומצמצם שאריות.

**הערה:** אין סכנה במצב הקיים (אף תזמון לא חוזר), לכן זה בעדיפות נמוכה.

---
