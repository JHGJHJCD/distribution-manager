# איפה עצרנו

> קובץ סטטוס זמני — מתעדכן כל הזמן. פרטים קבועים (ארכיטקטורה/כללים/שחרור) ב-`CLAUDE.md`.

## ✅ v3.13 — סקירת באגים של מסך הצינתוקים (3/9/2026 לילה) — שוחרר
- **תוקנו (פרטים ב-CLAUDE.md, מקטע v3.13):** סימוני V נמחקו בכל סנכרון · "לא הגיב" נעלם אחרי מעבר לשונית · שיגור חכם שנכשל באמצע השאיר קבוצות בשרת בלי רישום · תזמון שהבשיל בזמן מעקב קלאסי דרס את הרשומה · תזמון-שהבשיל/שליחה-חוזרת ספרו תשובות-סקר משבוע שעבר · מעקב לא התחדש כשיש >5 תזמונים · רצועת התזמון לא הופיעה מיד · בדיקה בזמן תזמון ממתין הוסיפה את מספר המנהל · כפתור השיגור החכם לא ננעל · טקסטים הבטיחו השמעה-בהתקשרות-חוזרת (כבויה) ו"הקש 7" (ישן) · Esc בחלון מעקב-בדיקה · ביטול ב-TtsDialog באמצע יצירה.
- ממצאים פתוחים (2) ו-(15) מרשימת 2/9 נסגרו. test_tzintuk: כל הבדיקות עוברות (+2 חדשות); smoke UI על DB זמני עבר.

## 🔬 מחקר 3/9/2026 ערב — "צינתוק יעיל ובר-שליטה": ממצאים + הצעת ארכיטקטורה (ממתין להכרעה)
- **ניסוי חי (20:54):** `RunTzintuk` ל-0548434668 עם `callerId=0773019787`, 5 שנ' ⇒ הצליח, 0.1 יחידה. **הקו תומך בצינתוק מובנה מה-API, ו-DID משני מתקבל כזיהוי יוצא.** (קריאה חוזרת של המשתמש למספר טרם נראתה ביומן.)
- **מספור הפילטר פוענח:** `check_template_filter=N` ⇔ התבנית ה-(N+1) ב-GetTemplates (0-based). 17→1430693 (מיקום 18) תואם 12/12. מחיקת תבנית מזיזה הכל ⇒ לא לבנות על זה. (אימות חי אופציונלי: שלוחת-ניסוי /79 עם `check_template_filter_19/20_active_go_to` + תבנית חדשה עם מספר אחד — לא נבנה.)
- **הצעה (במקום פילטר בשורש):** מספר ייעודי לצינתוקי החלוקה. שליחה עם `callerId=0773019787`, ובקובץ `Did_Go_To.ini` בשורש שורה `0773019787=/78` (ליד `033060315=/555`; `ext.ini` של השורש **לא** משתנה). מי שחוזר למספר שראה נופל ל-78 (הודעה + אקסס-פילטר "פעם אחת" + תפריט), כל השאר ממשיכים כרגיל. מעקב חזרות = שורות `IncomingDID#0773019787` ביומן (מדויק, בלי תלות ברשימות). המספר כיום → /50 "קמפיין בין המצרים" (רדום: 0 ביקורים בספטמבר). **שאלה למשתמש:** להקצות את 0773019787 (או להזמין DID חדש מימות)?
- **לתקן לפני חזרה לשליחה:** (1) שתי התבניות נושאות אותו תיאור — לשנות את 1430693 ל"…(מחשב שני)" או למחוק, ואז `ensure_template` יאמץ לפי תיאור; (2) `78` חסר `ExtensionNumbersAndAssociations.ini` (להעתיק מהשורש) — עדיין לא תוקן.

## 🔎 תחקיר 3/9/2026 ערב — "הקו שבור בתוך השלוחות": הפילטר חזר לשורש ב-14:50 והוסר ידנית ב-~18:00
- **סימפטום (מהמשתמש):** מי שמחייג ל-04 שומע פתיח רגיל, אבל בהקשה: 3 → "ההגדרות לא הושלמו", 1/2/4 → "אנא הקישו את מספר השלוחה" → "שיחתכם מועברת" → שלוחה 2 (0799488386).
- **הסיבה (אומתה ביומן `Log/LogFolderEnterExit-2026-09.ymgr`):** הפילטר (`check_template_filter=17→/78`) שהוסר ב-14:38 **נכתב מחדש ב-14:50:35** ע"י התוכנה (v3.07, לפני מתג הביטול) בתוך השליחה הקלאסית ל-363 מספרים (14:50:43, מכוונת). שתי התבניות 1430692/1430693 מכילות היום את **אותם 363 זכאים** ⇒ כל זכאי הופנה ל-78 → 78/menu. **בשיבוט 78 חסר `ExtensionNumbersAndAssociations.ini`** (בשורש: `00=0…09=9`) — ימות מרפד ספרה בודדת ל-2 ספרות ("3"→"03"): בשורש הקובץ ממפה חזרה, בשיבוט "03" נפל ל-`/03` (ניתוב-לפי-זמן ריק = "ההגדרות לא הושלמו"), "01/02/04" לא קיימות → M1000 → `timeout=1` → `/2`. 239 מספרים עברו דרך 78 היום.
- **אקדח מעשן:** התוכנה כתבה לשורש גם ב-14:44:20 (בדיקה — הוסיפה פילטר לתוך מצב `hangup` של "הקו כבוי") וב-14:50:35 (דרסה את השחזור של 14:47). שלושה כותבים לקובץ אחד בלי נעילה. הזכאים צולצלו פעמיים היום: 14:31 עם הודעה מ-1430692 (המחשב השני, 361 יח') ו-14:50 קלאסי מ-1430693 (המחשב הזה, 361 יח'). לא מוסבר: רשומת המנהר 9→0, פילטר 17→18, 8 קמפיינים של מספר בודד 15:25–15:27, "שתי שיחות" בבדיקה.
- **תוקן (~18:00, באישור המשתמש):** השורש נכתב מחדש ל-תוכן `line_backups/root_ext.ini.20260903-145035` (= המצב התקין של 14:47, 11 שורות של המנהל, בלי פילטר). אומת בקריאה חוזרת (241 בייט, אין `check_template_filter`). עותקים: `line_backups/root_ext.ini.live-before-manual-fix-*`. שלוחות 78/78-menu נשארו בקו (לא פעילות בלי הפילטר).
- **⚠ שני המחשבים עדיין על 3.07** — כל שליחה/בדיקה תכתוב את הפילטר מחדש. **לעדכן ל-3.08 לפני כל פעולה בצינתוקים.** המשתמש התחייב שלא ישלחו מהמחשב השני.
- **נותר לאמת:** חיוג מהמספר של המנהל → ביומן `Folder#main` ולא 78; הקשה 3 → `Folder#3`. פירוט מלא: `C:/Users/יהודה/.claude/plans/streamed-exploring-ripple.md`.
- **לקחים לארכיטקטורה (לפני כל חזרה למנגנון 78):** התוכנה לא כותבת לשורש בכלל; אם כן — בדיקת hash מול הכתיבה הקודמת וסירוב על שינוי זר; בלי `pass` שקט על שגיאות קו; שיבוט תפריט חייב להעתיק גם `ExtensionNumbersAndAssociations.ini`; בדיקות על DID/שלוחת ניסוי ולא על כניסת 04.

## 💡 רעיונות לעתיד (לא לביצוע עכשיו)
- **ברכה אישית בשם למתקשר (הכרעת המשתמש 3/9/2026: רעיון בלבד, "אי-פעם"):** הקו מזהה את המתקשר לפי מספר הטלפון ואומר "שלום משפחת כהן" ואז ממשיך לתפריט הרגיל (בלי סטטוס חלוקה, בלי סקר). מנגנון ימות: שלוחה `type=id_list_message` + `enter_id_type=phone_from_list_all_information` + קובץ `IdListMessage.ini` (שורה לכל טלפון: `05xxxxxxx=t-שלום משפחת כהן.g-/`, ו-`default=g-/`) שהתוכנה בונה מכל המקבלים הפעילים (כל המספרים, דרך `yemot.pick_phones`) ומעלה ב-`UploadTextFile` (דפוס קרא-ערוך-כתוב, סקיל `yemot-line-howto`). פירוט בסקיל `yemot-caller-data` סעיפים 5 ו-7. מגבלות: מספר חסוי/לא-שמור לא מזוהה; TTS של שמות בעברית לא מושלם; מקף/נקודה בשם שוברים את הקובץ (לנקות); **בונים על שלוחה נפרדת לניסוי, לא בשורש הקו** (לקח תקרית 78); טרם נוסה על הקו האמיתי.

## 🚨 תקרית 3/9/2026 14:30–14:45 — הפילטר של שלוחה 78 תפס את הרשימה הלא-נכונה; המנגנון נעול (v3.08)
- **מה קרה:** ב-14:30 יצא צינתוק אמיתי ל-**363** מספרים מהתבנית **1430693** ("מנהל חלוקה — צינתוקים", כנראה מהמחשב השני; הודעה 5.4 שנ'). מי שחזר לשיחה הופנה ל-78 ושמע את **הודעת הניסוי** של המחשב הזה (מ-1430692). לפי היומן ~140 כניסות ל-78 ו-~147 ל-78/menu עד 14:38.
- **הסיבה:** `check_template_filter=17` תפס את 1430693 (מיקום **18** ב-GetTemplates) — 12/12 מדגם-מתקשרים נמצאו רק ברשימה הזו. כלומר המספור של ימות **אינו** 1-based לפי GetTemplates (כנראה 0-based, או מספר-קמפיין מהממשק). ⚠ אותו ספק חל גם על ההנחה הישנה "17=1430692" ב-`campaign_message_to_play` — ייתכן שזו הסיבה שהמנגנון הישן "לא השמיע" לבדיקה (הבדיקה הייתה ב-1430692).
- **מה נעשה:** 14:38 הפילטר הוסר מהשורש; 14:40 לבקשת המשתמש הקו כובה (שורש=hangup, 11 שיחות נותקו); 14:47 הקו הודלק (השורש שוחזר מ-`root_ext.ini.before-line-off`, **בלי** הפילטר). קובץ המספרים של הצינתוק יוצא ל-`Downloads\מספרי_הצינתוק_האחרון_2026-09-03.xlsx`. בתוכנה: `yemot.CALLBACK_ENABLED=False` (kill switch) — שליחה/בדיקה לא נוגעות בשורש. שלוחה 78 (+menu) נשארה בקו, לא פעילה בלי הפילטר.
- **לפני הפעלה מחדש חובה:** (1) לאמת את מספור הרשימות בקו: `check_template_filter=N` ← איזו תבנית (בדיקה: מספר-בדיקה ברשימה של 1430692 בלבד, ניסיון N=16/17/18, יומן `Folder#78`). (2) לפתור את כפל התבניות 1430692/1430693 בין שני המחשבים (`ensure_template` לאמץ לפי תיאור; המחשב השני שולח מ-1430693 עם 363 נמענים!). (3) הודעת 78 חייבת להיבנות מהתבנית ש**באמת** שלחה. (4) לשקול הגדרת הפילטר לפי DID ייעודי במקום מספר-רשימה.

## ✅ v3.07 — ההודעה בהתקשרות חוזרת נשמעת פעם אחת בלבד (3/9/2026, 14:15)
- **דרישת המשתמש:** "מפריע מאוד" שההודעה חוזרת בכל חיוג. **הפתרון:** אקסס-פילטר בשלוחה 78 (`access_filter_1=g.*.*.*.*.*.30d.1.30d`, מעבר אחד ל-30 יום למספר) — מי שכבר שמע מופנה ל-`/78/menu` (שיבוט התפריט בלי ההודעה). הזיכרון `78/AccessFilterLogTime.ini` נמחק ע"י התוכנה בכל שליחה/בדיקה (`yemot.reset_callback_memory`) ⇒ הודעה חדשה נשמעת פעם אחת. הקו עודכן (`callback_line.py apply`), test_tzintuk עובר.
- **אימות חי v3.06 (בוצע):** חיוג מ-0548434668 ב-13:59 נכנס ל-`Folder#78` ל-20 שנ' (ההודעה 12.6 שנ' + תחילת הפתיח) — הפילטר בשורש וההשמעה עובדים.
- **אומת חי (3/9 14:28):** חיוג ראשון → `Folder#78` 18 שנ' (הודעה); חיוג שני → `Folder#78/menu` 9 שנ' (בלי הודעה). ⚠ `AccessFilterLogTime.ini` נראה 0 בייט ב-API גם כשהזיכרון פעיל — לא להסיק ממנו כלום.

## ✅ v3.06 — התקשרות חוזרת נבנתה מחדש: שלוחה 78 + פילטר בשורש (3/9/2026 אחה"צ) — אומת חי
- **למה:** גם אחרי v3.05 (איפוס זיכרון + `1h`) החוזרים לא שמעו (יומן: 5–8 שנ' בכניסה ב-12:57 וב-13:22). המשתמש ביקש לזרוק את המנגנון ולבנות מחדש עם דרך שחזור. `campaign_message_to_play` ננטש כליל.
- **הקו (בוצע ואומת מבנית):** שורש — הרשומה שלנו הוסרה (`0-ACTIVE,` של המנהל נשמרה), `file_by_template` הוסר, נוספו 5 שורות `check_template_filter=17` → `/78` (+`none/blocked/error_enter=yes`). שלוחה `/78` (menu) עם `M0000.wav` = הודעה (12.6 שנ') + פתיח השורש (14 שנ') = 27 שנ', ו-20 קישורי `go_to_folder`. `validate_ext_ini` OK. גיבוי/שחזור: `dev/line_backup_2026-09-03-callback/RESTORE.md`, `dev/callback_line.py`, תג `pre-callback-78`.
- **התוכנה:** `yemot.ensure_callback_extension`/`ensure_root_filter`/`publish_callback_message` (פירוט ב-CLAUDE.md). test_tzintuk עובר.
- **נותר — אימות חי:** המשתמש מחייג מ-0548434668 (ברשימת התבנית) ל-048691834 → צריך לשמוע הודעה+פתיח; ביומן `LogFolderEnterExit` אמורה להופיע כניסה ל-`Folder#78` (~27 שנ' בלי הקשה). אם לא נכנס ל-78 בכלל → הפילטר לא זיהה (לבדוק מספר-הרשימה/פורמט); אם נכנס ושתק → בעיית הקובץ (`callback_line.py apply` מחדש).

## ✅ v3.05 — (בוטלה ב-3.06) מי שחוזר לצינתוק שומע את ההודעה פעם אחת אחרי כל שליחה (3/9/2026)
- **הבעיה (מאומתת ביומן הקו):** הרשומה `17-ACTIVE-1-6d` בשורש = פעם אחת ל-6 ימים לכל מתקשר; המשתמש שמע ב-2/9 (31 שנ' בכניסה) ולכן ב-3/9 12:57 שהה 5 שנ' ולא שמע כלום.
- **בקו (באישור המשתמש):** שורש `ext.ini` — `17-ACTIVE-1-1h` + `campaign_message_to_play_file_by_template=yes`. גיבוי ושחזור: `dev/line_backup_2026-09-03/` (`root_ext.py show|apply|restore`), תג git `pre-callback-fix`.
- **בתוכנה:** `yemot.template_position` + `yemot.reset_callback_memory` (FileAction delete על `CampaignMessageAmountPlay-Template-17.ini`) בכל שליחה/בדיקה. אומת חי (קובץ דמה נמחק). test_tzintuk: 154 OK.
- **בדיקה שהמשתמש יכול לעשות:** "שלח בדיקה" למספר שלו → לחזור ל-048691834 → שומע את ההודעה לפני התפריט; חיוג שני מיד → לא שומע; שליחה חדשה → שומע שוב.

## ✅ v3.04 — צינתוקים יוצאים ממספר 04 (048691834) במקום המספר הראשי 079 החסום (3/9/2026)
- **הבעיה:** הצינתוקים יצאו מהמספר הראשי של הקו (0795378810, מספר 079) שחסום בפלאפונים כשרים — הזכאי לא יכל לחזור לשיחה.
- **התיקון:** `yemot.py` — קבוע `DEFAULT_CALLER_ID="048691834"` + helper `_caller_id()` שכל מסלולי החיוג (`run_tzintuk`/`run_campaign`/`set_template_entries` המתוזמן) עוברים דרכו; כשההגדרה `yemot_caller_id` ריקה נופלים ל-04. נבדק חי מול הקו: 048691834 מאושר ליציאה (`check_caller_id`→`isApproved:true`).
- **הגדרות:** שדה חדש "מספר מזוהה ביוצא" בפאנל הצינתוקים (`ym_caller`) + הערה; נשמר ב-`_save_yemot_settings`. אומת ויזואלית.
- test_tzintuk עובר.

## ✅ v3.02 — סקר אישור הגעה בשלוחה 77 (1 מגיע / 2 לא מגיע / 3 לא יודע) — שוחרר (2/9/2026 לילה); נותר אימות חי עם המשתמש
- **רקע:** דרך חיבור MCP חדש לקו (`pitron-ivr`, ivr.pitronai.com) אומת שהשרת דוחה `yemotContext=REPEAT` ⇒ "אישר הגעה בהקשה 7" מעולם לא עבד. הכרעות המשתמש: סקר בקו, הקשה **77** מהתפריט הראשי (המספר המזהה נשאר 04), תוויות ניתנות לעריכה, **מי שלא הקיש = "לא הגיב"** (אי-שיתוף-פעולה), זהירות מרבית + דרך שחזור.
- **בקו — בוצע:** שלוחה `/77` נוצרה (MCP planId `1048938ec0ba8d93197024b9c8f0ed47`) + `ext.ini` מלא ו-`050.tts` דרך `UploadTextFile` (ה-MCP השמיט את `050-option`). אומת ב-`read_extension`. **לא נגענו** בשורש/`Did_Go_To.ini`/תבניות. שחזור: `dev/line_backup_2026-09-02/RESTORE.md`; תג git `pre-survey` (=v3.01).
- **בתוכנה:** yemot.py (פרסר/merge/labels/upload), tzintukim.py (מעקב, "רענן תשובות", "לא הגיב"), group_update (3 תגים), print/excel, settings (תוויות+שאלה+"עדכן את הסקר בקו"), test_tzintuk (+27 בדיקות). פירוט ב-CLAUDE.md.
- **נותר:** בדיקות מלאות + צילומים (Gemini) + build/release 3.02 → **בדיקה חיה עם המשתמש:** חיוג ל-048691834 → 77 → הקשה → `read_text_file 77/ApprovalAll.ymgr`; "שלח בדיקה" ל-0556752642 → חזרה → 77 → "רענן תשובות" מציג את התשובה + תג ברשימה.
- **באג דחוי ("לא עכשיו"):** תבנית 1430692 (המרכזית בפועל) ב-8 שנ'/ניסיון אחד + אימוץ-לפי-תיאור ב-`ensure_template`.

## ✅ v3.00 — עיצוב מחדש של מסך הצינתוקים — שוחרר (2/9/2026 לילה) — ⚠ הצ'אט "סינטוקים מסך עיצוב" נחסם ע"י נטפרי
- **מה קרה:** הצ'אט בשם "סינטוקים מסך עיצוב" נחסם (418). חשד ראשי: `Read` של הצילומים
  `dev/_shots/tzintuk_tab.png` / `tzintuk_tab_loaded.png` (נוצרו 21:32–21:33). **אסור לקרוא אותם
  לתוך צ'אט** — אימות רק דרך `gemini_task.py -f <png>`. הצ'אט הנוכחי ממשיך את העבודה.
- **מצב הקוד:** שינויים לא-מקומטים ב-`tabs/tzintukim.py` (+342/−228) ו-`dev/_shot_tzintuk.py`.
  המסך עוצב מחדש בשפת העיצוב של "חלוקה ורישום" (ייבוא `_BG/_CARD_QSS/_CHIP_QSS/_BTN_*` מ-
  `group_update`): גוף גולל + סרגל תחתון קבוע עם רצועות (`sched_frame` תזמון, `prog_frame` מעקב
  חי), צ'יפים (`_CHIP_AMBER`), כפתור-קישור לפרסום בשלוחה 1 (`_BTN_LINK`).
- **הושלם בצ'אט ההמשך:** test_tzintuk עבר, צילומים אומתו דרך Gemini (בלי לקרוא PNG), שוחרר v3.00.
  ⚠ `gemini_task.py` עבר ל-`Desktop\תיקיות שונות למיון\חוסך טוקנים` (עודכן ב-CLAUDE.md הגלובלי). אם צ'אט נחסם שוב — לפתוח צ'אט חדש נקי, **לא** "המשך", ולהתחיל מהסעיף הזה.

## ✅ v2.99 — דוח תקלות 2/9/2026, 17 פריטים (2/9/2026 ערב)
תוקנו ושוחררו: מצבי ניקוד חתוכים למוצרים+רזרבה (`selection.limit_to_products`) · שם אוטומטי בכל מצב ·
נפשות בשלב הכנה · עמודת שם · תדירות רק לקבועים בהוספה ידנית · מתג/כפתור מבריקים · דיאלוג קהילות רחב ·
הדפסה (לוגו קטן, כותרות לבנות, עמודת "אישר הגעה") · רשימת גיבויים עם שחזור בלחיצה · איפוס שומר הגדרות.
פירוט ב-CLAUDE.md "תיקוני v2.99". נותר לאמת בשטח: עמודת "אישר הגעה" בהדפסה עם צינתוק אמיתי.

## ✅ v2.98 — סקירת באגים מלאה ותיקון 11 ממצאים (2/9/2026)
סקירה של כל הקוד (4 סוכני-משנה + בדיקות). תוקנו ושוחררו: קריסת צ'אט במחיקת ההודעה האחרונה ·
הערות בטבלת החלוקה שנמחקו ברענון · חד-פעמיים לפי guid (חוצה מחשבים) · איפוס = "התחל מחדש
מהמחשב השני" (`sync.restart_from_peer`/`snap_req`) · ייבוא מתנדב = רישום מלא (`_after_round_recorded`) ·
רשימה עצמאית עם כמה מספרים בשורה · נפילת צינתוק קלאסי רק בסירוב שרת · "בחר הכל" בלי רזרבה ·
קריאות שרת ברקע (`_run_blocking`) · PDF עם שעה + בדיקת קובץ ריק. פירוט ב-CLAUDE.md "תיקוני v2.98".
**הכרעות המשתמש (לא לתקן):** undo_incoming עם מחיקה כפויה — מכוון; משקלי ניקוד לא מסונכרנים — רצוי;
ממצא "חיפוש במסך המקבלים מתרוקן אחרי בדיקת כפילויות" (`recipients._open_dup_check`, `_rows_data=[]`) —
המשתמש לא ראה אותו בפועל, נשאר כמו שהוא.
**ממצאים פתוחים לפי סדר חשיבות (מהדוח):** (1) ביטול בחלון הסינון משאיר מצב filter בלי קריטריונים
(`group_update._on_mode_changed`); (2) רצועת "צינתוק מתוזמן" לא מתרעננת מיד אחרי תזמון + שיחת בדיקה
בזמן תזמון ממתין מוסיפה את מספר המנהל לרשימה (`_send_test` בלי בדיקת `_pending_sched`); (3) מחיקת
רישום בודד בחיפוש מהיר לא מעדכנת `recipient_count` באצווה; (4) אותו אדם בשני guid-ים אחרי ייבוא כפול
(`sync._adopt_match` מחליף guid קיים); (5) `set_need_weights` בלי `_sync_log` (מכוון); (6) פעולות
הרסניות אחרות לא מסונכרנות: ייבוא-החלפה ושחזור מגיבוי (לא מאפסים `sync_state`); (7) LWW בלי בדיקת
הפרש שעונים; (8) טוקן גיטהאב בתוך ה-EXE (`utils/_secret.py`, הרשאת Issues בלבד — לוודא); (9) עדכון
אוטומטי בלי hash; (10) תא אקסל שמתחיל ב-"=" הופך לנוסחה בייצוא; (11) מכסת GitHub API (36 פניות/שעה);
(12) `settings._open_manager_log` ענף else שבור (`res` לא מוגדר); (13) `password_len` מסונכרן; (14)
טקסטים ישנים (recipients:706 "בדיקת נתונים", settings:215 "חד פעמי", מדד "זכאים השבוע" ברשימה עצמאית);
(15) Esc בחלון מעקב בדיקה לא עוצר סקירה; ביטול ב-TtsDialog באמצע יצירה; (16) קוד מת: `ProductsEditor`,
`import_recipients_from_list`, ענפי מצב `all`, `_PRINT_CSS`, `_resource_path` ב-excel_utils.

## 🔎 תחקיר "הודעות ישנות בקו" — הושלם, נותרו אימותים (1/9/2026 ערב)
תחקיר מלא של קו 048691834 (הפדיחה: מתקשרים שמעו פתאום הודעות ישנות מאייר). **כל הידע
המפורט בסקיל `yemot-line-knowledge`** (מקטעי "זרימת ההודעה השבועית", "תקרית 01/09",
"מערכות חיצוניות", references/community). תמצית:
- **סיבת הפדיחה:** בצ'אט אחר נוספה `19-ACTIVE` ל-`campaign_message_to_play` בשורש (הפיצ'ר
  "חוזר-לצינתוק שומע את ההודעה" לקמפיין תכשיטים) — העדכון איפס את זיכרון "כבר-הושמע"
  והעיר רשומות ישנות (10/6/7/9 = קמפיינים מאייר). **תוקן:** נוקה ל-`19-ACTIVE,17-ACTIVE-1-6d,`.
- **שלוחה 1 קפואה מ-ב' סיוון (18/05):** הצוות עבר אז מהקלטה-בשלוחה-9 (שממלאת את /1,
  `folder_move=/1`) לשיגור קמפיינים מהאפליקציה (נשמר ב-/campaign, לא נוגע ב-/1). **תוקן קבוע:**
  בשלוחה 0 נוסף `admin_login_campaign_message_template_default_copy=/1` — כל הקלטת קמפיין
  תשוכפל אוטומטית לשלוחה 1. **⚠ נותר לאמת בהקלטת הקמפיין הבאה של המנהל!**
- **טענת המנהל "הקלטתי בשלוחה 9 בתחילת אלול והופיע בשלוחה 1" — נבדקה ונשללה ביומנים:**
  נסרק כל חלון א'–ז' אלול (14–20/08; ב' אלול=0 שיחות כי שבת) — **אף כניסה של אף מספר**
  ל-9/2 בכל אוגוסט. ביקורו היחיד בשלוחה 9 = ז' אלול (20/08) 08:42 → שלוחה 9/**1**
  (האזנה בלבד, 72 שנ'); יומן ההקלטות הפנימי של 9/2 מסתיים ב-18/05 08:16 (קובץ 524);
  אין קובץ חדש ב-/1 מאז. הסבר סביר: הקיש 1 (האזנה) במקום 2 (הקלטה), ו/או בדק את
  שלוחה 1, שמע את קולו-שלו (הקלטת סיוון שלו) והסיק שההודעה החדשה נקלטה.
- **עוד תוקן:** הגדרות ימות/ג'מיני שנמחקו מה-DB ב-15:37 (איפוס הגדרות בטעות) שוחזרו מגיבוי
  safety דרך db.set_setting (מסתנכרן למחשב השני).
- **פתוחים:** (א) אימות השכפול-האוטומטי בהקלטה הבאה; (ב) הסרת `19-ACTIVE` כשחלוקת
  התכשיטים תסתיים (קמפיין-שנגמר-ולא-הוסר = הפדיחה הבאה); (ג) מייל שחזור בלשונית אבטחה
  בממשק ימות (רק המשתמש, דורש 2FA); (ד) אופציה: העברת ארכיון /1 הישן ל-Old +
  `playfile_end_play_old=tfr` (⚠ לא להפעיל `playfile_move_file_to_old` — נעלם אחרי מאזין ראשון).

## ✅ v2.96 — סטטוס צינתוק קלאסי + התראות הורדה — הושלם ושוחרר (1/9/2026)
המשימה הגיעה מצ'אט "סטטוס צינתוק ועדכוני הורדה" ש**נחסם ע"י נטפרי 3 פעמים** — השורש אותר:
`Read` של הצילומים `dev/shots_classic_track/*.png` לתוך הצ'אט (נטפרי סורק גם תמונות בבקשות
ל-api.anthropic.com → 418 badwords; "המשך שיחה" יורש את התמונה ונחסם מיד). הכללים המלאים
תועדו: CLAUDE.md "מלכודות ידועות", pitfalls של סקיל manhal-haluka, סקיל visual-check,
וזיכרון reference-netfree. **אימות צילומים חשודים → ראיית Gemini (`gemini_task.py -f`), לא Read.**
פתרון-קבע מומלץ למשתמש: לבקש בנטפרי החרגת `api.anthropic.com` מסריקת תוכן.
- **מה נבנה (פרטים ב-CLAUDE.md מקטע v2.96):** מעקב חי אחרי צינתוק קלאסי (מי חזר לשיחה ומי
  אישר הגעה בהקשת 7; רצועה עם מונים + "הארך מעקב ב-30 דק'") — לימות אין יומן-שיחות-עבר,
  רק `GetIncomingCalls` חי; והתראות למחשב המנהל על הורדות גרסה (GitHub download counts)
  ועל עדכון המחשב השני (צ'קבוקס פר-מחשב בהגדרות, הופעל במחשב של המשתמש).
- אומת: הסוויטה המלאה עברה (release.py test, exit 0), fetch_download_stats מול GitHub אמיתי,
  3 צילומים אומתו דרך ראיית Gemini (רצועת מעקב חי, רצועת סיום, צ'קבוקס ההגדרות).
- **נותר לאמת בשטח (בשליחת קלאסי הראשונה):** שהמעקב החי אכן קולט מתקשר-חוזר אמיתי
  (GetIncomingCalls מול שיחה חיה) ושהקשת 7 בהתקשרות-חוזרת נרשמת כ"אישר הגעה".

## ✅ v2.89 — תיקון פרטיות מיידי: פרסום בשלוחה 1 ידני בלבד — שוחרר (31/08/2026)
- המשתמש התריע מיד אחרי v2.88: הפרסום האוטומטי בשלוחה 1 חושף הודעת-נזקקים לכל שומעי הקו. **הוסר כל פרסום אוטומטי** (שליחה/תזמון); במקומו כפתור "📢 פרסם בשלוחה 1…" עם אזהרה מפורשת ("כל שומעי הקו ישמעו") ואישור ידני. אף הודעה לא פורסמה בפועל בין הגרסאות (לא נשלח צינתוק). ההשמעה למתקשר-חוזר (#z4xy9) נשארת — היא פרטית (רק למי שברשימת הצינתוק).

## ✅ v2.88 — דוח 31/08/2026 (7 פריטים) — שוחרר (31/08/2026)
כל 7 פריטי הדוח טופלו בגרסה אחת (פרטים טכניים במקטע v2.88 ב-CLAUDE.md):
- **#n6wte** ביטול מוחלט של חסימת השעות (הכרעת המשתמש: בלי אזהרה בכלל) · **#gaira** צלצול לכל המספרים של כל מקבל · **#dx28e** מספר הבדיקה מוזן בכפתור הבדיקה (הוסר מההגדרות) · **#kx6wd** פרסום ההודעה בשלוחה 1 (הכרעת המשתמש: כהודעה נוספת) · **#z4xy9** מי שלא ענה שומע את ההודעה בהתקשרות חוזרת (פעם אחת; שורש הקו הוגדר חד-פעמית `17-ACTIVE-1-6d` — בוצע ואומת מול השרת) · **#9hgvi** קליק ימני על חלוקה קודמת: פרטים/הדפסה/PDF/אקסל/צינתוק · **#y7jr0** תזמון חכם — **שלב 1** (הכרעת המשתמש: בשלבים).
- אומת: `test_tzintuk.py` הורחב (+20 בדיקות: allocate_phones, store_list, publish, answer_stats), כל הסוויטה, 3 צילומים (`dev/_shot_v288.py`).
- **נותר לאמת בשטח (בשליחה הראשונה):** (א) שההודעה אכן מושמעת למתקשר-חוזר — המספר 17 ב-`campaign_message_to_play` הוא מיקום התבנית 1430692 ברשימת התבניות (פרשנות סבירה אך לא מתועדת; אם לא עובד — לתקן את המספר בשורש הקו). (ב) שהקובץ שפורסם בשלוחה 1 מושמע כמצופה. (ג) ⚠ התגלה: `UpdateTemplate` דוחה `yemotContext=REPEAT` — ייתכן שאישור-הקשת-7 (v2.85) לא פעיל בכלל; לבדוק בשטח (שלח בדיקה ← הקש 7 ← המונה עולה?) ואם לא — להגדיר "השמעה חוזרת" ידנית בממשק ימות לתבנית 1430692 או לברר מול התמיכה.
- **v3.11 (3/9/2026):** ההיסטוריה נמשכת **מאז ומעולם** (יומן מ-02/2020, כל הקמפיינים) ונשמרת מצטברת לפי מספר→שעה. המשיכה הראשונה במחשב השני ~15–20 דקות ברקע.
- **v3.10 (3/9/2026):** ההיסטוריה של ימות עצמם (כל הקמפיינים מאז ינואר + יומן השיחות הנכנסות של 12 החודשים האחרונים) נמשכת למטמון מקומי ומזינה את `answer_stats` — ראו CLAUDE.md. לבדוק בשטח: הריצה האוטומטית היומית במחשב השני (מטמון נפרד לכל מחשב), ושהמשיכה לא נחסמת ע"י נטפרי (קובצי יומן גדולים — סריקת תוכן).
- **v3.09 (3/9/2026):** שלב 1ב של #y7jr0 — שעת החיוג האמיתית של כל מספר + שעות ההתקשרות החוזרת לקו נצברות ב-`answer_stats` (ראו CLAUDE.md). עד עכשיו כמעט כל ההיסטוריה היא מהיום (98 מספרים, כמעט כולם ב-14:00 בגלל התקרית) — ההמלצות יהפכו למשמעותיות אחרי כמה שבועות של שליחות בשעות שונות.
- **שלב 2 של #y7jr0 (גרסה עתידית):** שליחה מפוצלת אוטומטית — קיבוץ הנמענים לפי `best_hour` ותזמון קמפיין לכל קבוצה (מגבלה: תזמון-ממתין-אחד לתבנית; יידרשו כמה תבניות או שליחה מקומית מדורגת).

## ✅ v2.87 — תזמון צינתוקים (#xi85i) + מנוע Gemini TTS — שוחרר (31/08/2026)
- **תזמון בצד השרת של ימות** (`ScheduleCampaign`) — עובד גם כשהמחשב כבוי. כפתור "🕒 תזמן שליחה", רצועת "צינתוק מתוזמן" + ביטול, קליטת תוצאות בהפעלה הבאה (`_check_scheduled`), סטטוסים scheduled/canceled/sched_failed בהיסטוריה ובסנכרון. פרטים מלאים ב-CLAUDE.md (מקטע "תזמון צינתוקים").
- אומת: `test_tzintuk.py` +20 בדיקות (payload, ביטול, LWW, תאימות לאחור), כל הסוויטה, צילומים (`dev/_shot_sched.py` — לשונית עם רצועת תזמון + דיאלוג).
- **נותר לאמת בשטח (בתזמון הראשון):** שדה ה-schedId בתשובת `ScheduleCampaign` ו-campaignId ברשומת SUCCESSFUL של `GetScheduledCampaigns` (לא מתועדים במלואם; `_dig` סלחני). בדיקה מומלצת: לתזמן צינתוק בדיקה לעצמך לעוד כמה דקות, לסגור את התוכנה, לפתוח אחרי השעה — ולוודא שהתוצאות נקלטו.
- שוחרר יחד עם מנוע Gemini TTS (ראו מקטע הבא) אחרי תיאום בין שני הסשנים.
- מדוח 31/08 נותרו פתוחים: קו חוזר לשמיעת ההודעה (#z4xy9) · מספר בדיקה באותו מסך (#dx28e) · הודעה לשלוחה מרכזית (#kx6wd) · ביטול חסימת שעות (#n6wte) · הבהרת "הוסף אדם" (#2cfy7).

## ✅ מנוע Gemini TTS נוסף — שוחרר יחד עם v2.87
- לבקשת המשתמש ("וודאי ג'מיני תוסיף אותו"): 2 קולות "גמיני — משופר" (Charon/Aoede) נוספו לבורר ב-`TtsDialog`, מנוע ב-`utils/tts.py` (`_gemini_synthesize`), מפתח ב-setting מסונכרן `gemini_api_key` + שדה בהגדרות; **המפתח הקיים של המשתמש כבר נזרע ב-DB האמיתי**. נפילה אוטומטית לאברי/הילה כשהמכסה נגמרת (עם הערה). חוזה חדש: `synthesize -> (path, note)`.
- אומת: `test_tts.py` מורחב (עובר), יצירת גמיני אמיתית E2E דרך קוד התוכנה עם המפתח השמור, צילומי דיאלוג+הגדרות.
- **לא שוחרר בכוונה:** צ'אט התזמון (#xi85i) רץ במקביל על אותם קבצים — השחרור הבא (שלו) יכלול גם את זה. אם משוחררים מכאן — לכלול בהערות השחרור גם את קולות גמיני.

## ✅ v2.86 — יצירת הקלטת צינתוק מטקסט (#9vy1b) + מאגר הקלטות (#kgmcw) — שוחרר (31/08/2026)
- `utils/tts.py` (edge-tts, קולות עברית נוירליים חינם: אברי/הילה) + `TtsDialog`/`LibraryDialog` ב-`tzintukim.py`. פרטים ב-CLAUDE.md.
- אומת: `test_tts.py` כולל יצירת MP3 אמיתית מול השירות (עובר גם דרך NetFree), כל הסוויטה (כולל test_tzintuk שנוסף לרשימת ה-release), 3 צילומים (לשונית, דיאלוג יצירה, מאגר).
- **נותר לאמת בשטח:** העלאת MP3 של TTS לימות (`convertAudio=1` אמור להמיר) + שמיעה בטלפון — בבדיקה הראשונה של המשתמש ("צור הקלטה מטקסט" ← "שלח בדיקה למספר שלי").
- מדוח 31/08 נותרו פתוחים: תזמון כשהמחשב כבוי (#xi85i, קריטי) · קו חוזר לשמיעת ההודעה (#z4xy9) · מספר בדיקה באותו מסך (#dx28e) · הודעה לשלוחה מרכזית (#kx6wd) · ביטול חסימת שעות (#n6wte) · הבהרת "הוסף אדם" (#2cfy7).

## ✅ v2.83–v2.84 — E2E צינתוקים: אובחן ותוקן באג ה-_MEI (31/08/2026)
- "בדוק חיבור" אצל המשתמש נכשל ("אין חיבור"). v2.83 (אבחון מפורט + retry + truststore + זיהוי "סיסמה שגויים") חשפה את הסיבה: `base_library.zip` נעלם מ-_MEI של המופע החי — **`_cleanup_prev_mei` רוקן תיקייה חיה בחלון עדכון-הגרסה** (פירוט במלכודות). v2.84: מחיקה רק אחרי בדיקת-חיות ב-`os.rename` + חימום ייבוא-רשת עצלן ב-main. בנוסף מההוראות הרשמיות: שרת תאום `private.call2all` כ-fallback, תמיכה במפתח API (עוקף MFA, מוזן בשדה הסיסמה), והסבר עברי ל-MFA_REQUIRED.
- **✅ אושר ע"י המשתמש (31/08/2026): "עבד מצוין" על v2.84** — החיבור מאומת בשטח; הלקחים נשמרו (learned-solutions בסקיל yemot-hamashiach + pitfalls). נותר לצפות בשימוש אמיתי ראשון: העלאת הקלטה (`tpl:` מול `ivr2:`) + קמפיין ראשון על רשימה קטנה.

## 🔶 v2.82 — מערכת צינתוקים (ימות המשיח) — הקוד שוחרר; ממתין ל-E2E עם המשתמש
- **מה נבנה (31/08/2026):** לשונית "צינתוקים" (אזור עליון חמישי) + פאנל חיבור בהגדרות + `utils/yemot.py` + טבלת `tzintuk_campaigns` עם סנכרון (`tz_add`/`tz_update`). פרטים מלאים ב-CLAUDE.md ("צינתוקים — ימות המשיח"). התכנון לפי `הדמיית_צינתוקים.html` שאושר.
- אומת: `test_tzintuk.py` (חדש, הכל מדומה) + כל הסוויטה (test_all 104/104, sync, deep, scenarios, selection, data_safety) + 2 צילומים (לשונית מאוכלסת עם חריגים/כפולים, פאנל הגדרות).
- **הכרעות המשתמש (30/08):** יש לו מערכת ימות פעילה · בלי דגל "לא לצלצל" בשלב זה · פרטי הגישה מוזנים בתוכנה (לא בצ'אט).
- **הצעד הבא — E2E אמיתי עם המשתמש (לא ניתן לאמת בלעדיו):**
  1. המשתמש מעדכן לגרסה 2.82, נכנס להגדרות ← "צינתוקים (ימות המשיח)", מזין מספר מערכת + סיסמה + המספר שלו, ולוחץ **"בדוק חיבור"**.
  2. מעלה קובץ הקלטה (או "שלח בדיקה" — התבנית נוצרת אוטומטית) ושומע אותה בצינתוק בדיקה למספר שלו.
  3. קמפיין אמיתי ראשון על רשימה קטנה. ⚠ לא מאומת עדיין מול השרת האמיתי: נתיב העלאת ההקלטה (`tpl:` עם נפילה ל-`ivr2:`), מבנה GetSession, וערכי entryStatus בפועל — אם משהו נשבר, לתקן לפי התגובה האמיתית ולתעד ב-learned-solutions של סקיל `yemot-hamashiach`.

## ✅ v2.81 — חלון עדכון מעוצב + התראת עדכון בזמן ריצה (30/08/2026, שוחרר)
- `UpdateOfferDialog`: פירוט שינויים מרונדר יפה ומיושר לימין (`utils.ui._release_notes_html` — כותרת+נקודות), חץ בין גלולות הגרסאות.
- בדיקת עדכונים כל שעה בזמן ריצה + התראת Windows (tray balloon) שלחיצה עליה פותחת את חלון העדכון (`main.py: _run_update_check/_notify_update/_offer_pending_update`).
- אומת: 12/12 קובצי בדיקה + 2 צילומים (`dev/shots_upd281`). **שוחרר מ-worktree נקי** כי סשנים מקבילים עבדו על העץ (צינתוקים/ייצוא-אקסל) — הקומיט כלל רק main.py/utils/ui.py/version.py; ראה מלכודת חדשה ב-skill.

## ✅ דוח תקלות 30/08/2026 (4 פריטים) — v2.80, טופל במלואו
1. **#x2yn5 — גודל טקסט בזמן אמת ובאחוזים.** `styles.apply_app_theme(app, percent)` — מחיל את כל ערכת העיצוב (qt-material + EXTRA_QSS) עם כל ה-font-size מוכפלים; שינוי בהגדרות (QSpinBox‏ 80–150%, debounce 250ms) נקלט **מיידית** בכל התוכנה. setting חדש `ui_font_scale` (פר-מחשב, מוחרג מסנכרון; מיגרציה מ-small/normal/large דרך `db.get_ui_font_percent`).
2. **#n02fc — סטטוס סנכרון ויזואלי.** נקודת-מצב צבעונית (ירוק/ענבר/אדום/אפור) + כותרת מודגשת + צ'יפים (מחשב · סנכרון אחרון יחסי · ממתינים · מחשבים נוספים) במקום בלוק טקסט. `_refresh_sync_status` שוכתב; רץ גם בכל פתיחת הלשונית.
3. **#ce6a0 — הודעות למפתח בתוך התוכנה.** טבלת `feedback` חדשה + ops סנכרון `fb_add`/`fb_status` (+seed) — כל הודעה משני המחשבים נראית בהגדרות: כפתור "📥 הודעות שנשלחו (N פתוחות)" → `FeedbackInboxDialog` (העתק הודעה / העתק את כל הפתוחות / סמן כטופל↔החזר לפתוח, LWW). ייבוא חד-פעמי של feedback.jsonl ישן (`feedback.import_legacy_jsonl`, guid דטרמיניסטי, דגל פר-מחשב).
4. **#o8l0v — ביקורת מגבלות/אובדן נתונים.** אין שום מחיקה אוטומטית ב-DB. תוקנו: `get_all_history_export_rows` קוצץ ייצוא ל-500 אצוות (עכשיו הכול); `get_distributions`/`get_distribution_batches` ברירת-מחדל ללא LIMIT; **באג צ'אט** — `get_messages` החזיר את ה-400 הישנות (ASC+LIMIT) כך שמעל 400 הודעות חדשות לא היו מוצגות — עכשיו 400 החדשות.
- אומת: כל הסוויטה (test_all 104/104, test_sync כולל בדיקת fb חדשה, deep/scenarios/selection/search/volunteer/priority/data_safety/fixes/updater) + צילומים (הגדרות מלא, 100% מול 130%, מסגרת סנכרון, דיאלוג הודעות). מלכודת שנלמדה: `selectRow` לא תופס בטבלה בדיאלוג — להשתמש ב-`setCurrentCell`.

## דוח תקלות 27/08/2026 (13 פריטים) — בעבודה, מחולק לבאטצ'ים

**הכרעות המשתמש (27/08):** הודעות = צ'אט קבוצתי בין בעלי-התוכנה (מנהל/מזכירה/הנהלה) דרך סנכרון Drive (לא מיידי, אין וואטסאפ — קהל חרדי) · אישור מנהל = **מיידי + יומן שינויים לביטול בדיעבד** · כפתור "בטל" + הגדרת "מחשב מנהל" **רק עם קוד**, ורק במחשב המנהל · סנכרון רציף כל 10 שנ' + מחיקת "סנכרן כעת" · הפעלת Drive ברקע אם כבוי · פיצול שם פרטי/משפחה **בכל המערכת** (אם לא שביר; אקסל = 2 עמודות) · תיקיית ייצוא נפרדת לכל סוג, ברירת-מחדל הורדות · שם/משפחה פיצול.

### ✅ Batch 1 — v2.72 (6 פריטים, אומת + שוחרר)
1. **#4zque** — הוסרה מגבלת 500 בחיפוש מהיר (`filter_recipients`/`search_recipients` limit=None).
2. **#p1o35** — טבלת מקבלים: חיפוש **בכל השדות** (לא רק שם) דרך `db.filter_recipients`; placeholder + רוחב עודכנו.
3. **#x4rce** — אייקון "שחזר" (שני ריבועים) שוכתב לגליף Windows-11 נקי (ריבוע קדמי + מסגרת אחורית).
4. **#k6bqi + #13** — מתג הזזה מונפש `_StageToggle` (הכנה⇄רישום) במקום 2 כפתורים; מעבר בין מצבים **שומר סימונים** (`_record_visited`; מתאפס בחלוקה חדשה/שמירה/שינוי מצב).
5. **#sdj7g** — דיאלוג תצוגה-מקדימה מותאם `_PreviewDialog` (QPrintPreviewWidget) עם כפתור "הדפס" ירוק בולט + זום/התאם/שמור-PDF/סגור, במקום סרגל Qt הצפוף.
6. **#g4v4q** — הסבר גיבויים ברור (מה/מתי/אוטומטי) + תוויות כפתורים ("בחר תיקיית גיבוי"/"שחזר נתונים מגיבוי").

### ✅ Batch 2 — v2.73 (סנכרון, 3 פריטים, אומת על המחשב האמיתי + שוחרר)
- **#ysqq0** — `detect_drive_folders` מזהה גם את השם העברי **"האחסון שלי"** (לא רק My Drive). אומת: מחזיר `G:\האחסון שלי`. `looks_like_backup_folder` + `_MY_DRIVE_NAMES` עודכנו.
- **#kzuo2** — `sync.ensure_drive_running()` מפעיל את `GoogleDriveFS.exe` ברקע אם Drive לא רץ (נקרא בעלייה כשהתיקייה חסרה). אומת: מצא את ה-exe.
- **#hd4as** — סנכרון רציף כל **10 שנ'** (היה 20); הוסר כפתור "סנכרן עכשיו" + `_sync_now`; נוסף חיווי "פועל אוטומטית ברקע".

### ✅ Batch 3 — v2.74 (#5e1jc, אומת + שוחרר)
- **#5e1jc** — `excel_utils.export_dir(kind)` + `EXPORT_KINDS` (dist/recipients/volunteers); settings `export_dir_<kind>` (מוחרג מסנכרון — נתיב פר-מחשב). כל 6 נקודות הייצוא (5 אקסל + PDF) עברו ל-`export_dir`. פאנל "תיקיות ייצוא" בהגדרות עם בחר/ברירת-מחדל לכל סוג. ברירת מחדל = הורדות.

### ✅ Batch 4 — v2.75 (#aka27 + #10 פיצול שם + תיקון אזהרת _MEI, אומת + שוחרר)
- **#aka27 + #10 — פיצול שם פרטי/משפחה בכל המערכת.** עמודות `first_name`/`last_name` (מיגרציה + back-fill). **קונבנציה: `full_name` = "משפחה פרטי"** (כמו הייבוא, שורה 409) — נשמר כמזהה סמכותי (`_apply_name_fields`/`split_full_name`/`join_name` **כולם משפחה-קודם**, כדי לא לשבור זהות/היסטוריה/הדפסות/סנכרון). טופס=2 שדות · טבלה=2 עמודות (אינדקסי badge הוזזו +1) · אקסל: ייבוא-תבנית שומר משפחה/פרטי, `_FULL_FIELDS`+ייצוא כוללים 2 עמודות. אומת: מיגרציה על DB ישן, add/update, round-trip, כל הבדיקות + 2 צילומים.
- **תקלה חיה (_MEI):** אזהרת "Failed to remove temporary directory _MEI..." שקפצה ליד התוכנה — שומר-המופע-היחיד יצא ב-`sys.exit(0)` (ניקוי בוטלואדר נכשל). תוקן ל-`_hard_exit(0)` (דילוג על הניקוי).

### ✅ Batch 5 — v2.76 (#ya4f7 לשונית הודעות, אומת + שוחרר)
- **#ya4f7 — צ'אט צוות מסונכרן.** לשונית עליונה "הודעות" (אזור רביעי) · טבלאות `messages`+`message_reads` · ops סנכרון `msg_add`/`msg_read` (+seed ב-snapshot). תג לא-נקראו על הלשונית (`_refresh_messages_badge`). **שיפורים לבקשת המשתמש:** זמן יחסי בעברית ("לפני 5 דקות") · **סימוני נקרא ✓/✓✓ בסגנון וואטסאפ** (read markers מסונכרנים) · **כל הזמנים בשעון ישראל** (`utils/timefmt.py`, zoneinfo Asia/Jerusalem + fallback; גם "סנכרון אחרון" בהגדרות) · **רקע מותאם** שהמשתמש מעלה (`chat_bg_path`, פר-מחשב, מוחרג מסנכרון). אומת: סנכרון הודעות + ✓✓ בין 2 מחשבים, שעון ישראל, 2 צילומים.
- `tzdata` נוסף ל-spec (שעון ישראל בבנייה קפואה). בדיקות `test_all` עודכנו ל-4 אזורים/6 עלים.

### ✅ Batch 6 — v2.77 (#5rhe9 יומן שינויים + ביטול מנהל, אומת + שוחרר)
- **#5rhe9 — בקרת מנהל.** הגדרת "מחשב מנהל" מוגנת בקוד (`db.set/verify_manager_code` PBKDF2, הקוד מסונכרן; הדגל `is_manager` פר-מחשב ב-sync_state). במחשב המנהל בלבד: כפתור "יומן שינויים…" → `ManagerLogDialog` — מציג כל שינוי שנקלט מהמחשב השני (זמן ישראל · מקבל · סיכום מפורט של השדות שהשתנו) עם כפתור **"בטל"** שמחזיר את הערך הקודם. הביטול הוא כתיבה מקומית → מסתנכרן חזרה (LWW חדש יותר) לכל המחשבים. הקלטה רק במחשב מנהל (`_RECORD_INCOMING` ב-`pull_changes`; טבלת `sync_incoming` עם before/after JSON). ops נתמכים לביטול: `rec_upsert` (עריכה→שחזור, הוספה→מחיקה) · `rec_delete` (→שחזור). אומת: E2E מנהל+מזכירה (הקלטה, ביטול, הפצה חזרה, קוד), אי-הקלטה במחשב לא-מנהל, כל הרגרסיה + צילום.

**🎉 כל 13 פריטי הדוח + תקלת ה-_MEI הושלמו ושוחררו (v2.72–v2.77).**
- **#ya4f7** — לשונית "הודעות": צ'אט קבוצתי מסונכרן + תג לא-נקראו.
- **#5rhe9** — יומן שינויים + כפתור "בטל" (רק במחשב מנהל, מוגן קוד).

## v2.68 — סבב דוח 26/08 (8 פריטים) — **שוחרר (26/08/2026)**
כל 8 הפריטים מהדוח החדש, אומתו בבדיקות + 4 צילומים אמיתיים (הוסף-מקבל, שיוך קהילות, כפילויות, בורר עדיפות).
1. **#hkdif** — חלון "＋ הוסף מקבל" נפתח כשהרשימה **ממוינת לפי ניקוד** (הנצרכים ביותר למעלה; שובר-שוויון: המתנה ארוכה). הסינונים הקיימים נשמרו.
2. **#7ycrg** — **הוסר מהבורר** מצב "כל המקבלים" (`all`) שנוסף היום בצהריים — היה אי-הבנה. ברירת-המחדל חזרה ל"רגיל" (schedule). "לראות את כולם לפי ניקוד" עבר לחלון הוסף-מקבל (#hkdif). ה-backend של `all` נשאר לתאימות; DB ישן ממופה ל-schedule.
3. **#5lpgl** — דיאלוג שיוך קהילות: תוקנה חפיפת הקומבואים (גובה שורה 46 + עיצוב קומבו). השיוך נשמר דרך `db.update_recipient` (מסונכרן).
4. **#ld7lr** — בורר עדיפות במקבלים: צ'יפים צבעוניים מעוגלים כמו באדג'ים בטבלה.
5. **#eiqat** — כפתור "יבוא מ-Excel" הותאם בגודל ובעיצוב לכפתור "הוסף מקבל".
6. **#yg76l** — בדיקת כפילויות: הוסר כפתור "רענן" (ריענון אוטומטי בפתיחה/עריכה/מחיקה).
7. **#p11zo** — בדיקת כפילויות: עמודת "שם מלא" הורחבה (220px) — לא נחתכת יותר ל"וי…".
8. **#ipjgd** — בדיקת כפילויות: צ'יפ צבעוני לכל סוג כפילות (שם כפול=אינדיגו, טלפון משותף=ענבר).
- **תקלה 3 (#eda47)** בדוח הייתה ריקה (נשלחה בטעות) — דולגה.

## v2.67 — תיקון סנכרון: אזהרה מפני תיקיית-גיבוי + חיווי "מחשב שני מחובר" — **שוחרר (26/08/2026)**
**הסיבה שהסנכרון לא עבד אצל המשתמש:** נבחרה תיקייה בתוך `Downloads` (`Downloads/סינכרון חלוקה`).
Google Drive מגבה תיקיות כאלה **בנפרד לכל מחשב** ואינו משתף אותן — כל מחשב כתב רק את היומן שלו
(`applied`/`offsets` ריקים, יומן יחיד בתיקייה). הפתרון האמיתי (משתמש): לבחור תיקייה בתוך «Drive שלי».
- `sync.looks_like_backup_folder` / `other_device_count` חדשים. `SyncSetupDialog` חוסם באזהרה כשבוחרים
  Downloads/Desktop/Documents ואין מחשב שני; פאנל הסטטוס + הודעת ההפעלה מציגים אם זוהה מחשב שני.

## v2.61 — סבב דוח 25/08/2026 (7 בקשות) + סנכרון 2 מחשבים. **הקוד מוכן ואומת; ממתין ל-build/release**
כל 7 הפריטים מהדוח + פיצ'ר גדול נוסף שביקש המשתמש (עבודה משני מחשבים). אומת: test_selection
(כולל C1–C10 איזון קהילות), test_sync (2 מחשבים מדומים), test_all 104/104, test_deep, test_scenarios,
test_volunteer_flow, test_priority_import, test_data_safety, test_search, test_updater — **הכל עובר**;
+ 6 צילומים אמיתיים (דיאלוג סינון עם איזון, הוסף-מקבל משודרג, שיוך קהילות, אחוזי קהילות, הגדרת סנכרון, הגדרות).
1. **#lejmr — איזון בין קהילות** (הפיצ'ר הגדול): מצב סינון מותאם מחלק מוצרים בין קהילות (=שם נציג)
   יחסית לגודל הקהילה; בתוך קהילה לפי ניקוד, השלמה מאותה קהילה אם חסר. אחוזים ידניים בהגדרות.
   השלמת נציג חסר לפי רוב בית-כנסת (מסומן בכרטיס). `selection.balance_by_community`/`community_quotas`/
   `infer_communities`; `CommunityAssignDialog`+`CommunityQuotasDialog`. הכרעות המשתמש: מכסה יחסית לגודל
   הכולל, השלמה מאותה קהילה, נופלים לא ברזרבה, ברירת-מחדל דלוק.
2. **#cdxqp/#4v4gt/#6gcqq — "＋ הוסף מקבל" משודרג**: מציג **כל** המקבלים (כולל לא-פעילים, מסומנים
   באדום — נשארים לא-פעילים), עמודות עדיפות/תדירות/אזור/ניקוד(שלם)/סטטוס, סינון מהיר לפי עדיפות+תדירות.
3. **#gli21 — ייצוא לאקסל לפני חלוקה**: כפתור "ייצוא לאקסל" בשלב הכנה → `export_full_distribution_to_excel(include_got=False)` (עמודה "ברשימה" במקום "קיבל").
4. **#hlcmj — ייבוא הייצוא של התוכנה**: `import_from_excel` מזהה אוטומטית פורמט ייצוא-עצמי ('שם מלא')
   → `import_app_export`. מיזוג עם אישור: `db.diff_incoming_recipients` + `ImportReviewDialog` (סיכום אחד,
   צ'קבוקס לכל שינוי) → `apply_import_confirmed`. שדה ריק בקובץ לא מוחק נתון.
5. **#dancj — ייצוא היסטוריה**: 2 כפתורים בלשונית "חלוקות" — הנבחרת / כל ההיסטוריה →
   `export_history_to_excel` (כל פרטי המקבל + עמודת קיבל/לא הגיע; לכל-ההיסטוריה גם שם+תאריך חלוקה).
6. **סנכרון 2 מחשבים** (`utils/sync.py`) — ראה CLAUDE.md. Google Drive משותף, journal-per-device, LWW,
   offline-safe, רקע כל 20 שנ'. UI בהגדרות (`SyncSetupDialog`). בו-זמנית בטוח; אותו כרטיס = האחרון גובר.
**להמשך:** build (`python -m PyInstaller ... מנהל_חלוקה.spec`) → `cp dist/מנהל_חלוקה.exe dist/Manhal-Haluka.exe`
→ commit/push → `gh release create v2.61 dist/Manhal-Haluka.exe --latest`.

## v2.60 — **בוצע, אומת ושוחרר (24/08/2026).** כל 10 ההכרעות מומשו בגרסה אחת
אומת: test_all ‏104/104 + test_selection + test_fixes + test_scenarios + 5 צילומים אמיתיים
(שלב הכנה/רישום, הגדרות עם גודל-טקסט+סף התראות, חיפוש עם חלוקות-קודמות באזור אנשים,
דיאלוג עדכון חדש). הצאט המקורי נקטע ע"י נטפרי בשלב הצגת הצילומים — הושלם בצאט המשך.
רשימת ההכרעות שמומשו:
1. **מבנה:** 3 האזורים טובים. **להעביר "חלוקות קודמות" מאזור "חלוקה" לאזור "אנשים"**.
2. **להסיר את לשונית "חד פעמי"** לגמרי (כפתור "בחר חד-פעמיים" מכסה אותה).
3. **שני שלבים מפורשים במסך החלוקה:** שלב הכנה **בלי צ'קבוקסים בכלל**; כפתור מעבר ברור
   "רישום מי קיבל" מחליף את המסך למצב סימון (מתחיל ריק — מסמנים מי הגיע).
4. **רישום "לא הגיע":** מי שלא סומן בשלב הרישום נרשם מפורשות בהיסטוריה כ"לא הגיע"
   (פותר את #yjcny). ⚠ שינוי כלל עסקי — דורש עדכון test_selection/test_fixes.
5. **התראות אי-הגעה:** סף רצוף מתכוונן בהגדרות (ברירת מחדל 3); מוצג כתג צבעוני ברשימת
   החלוקה **וגם** בכרטיס המקבל בחיפוש מהיר.
6. **גודל גופן:** אפשרות בהגדרות (קטן/רגיל/גדול) לכל התוכנה.
7. **רענון עיצוב כללי:** "תחושת בלאגן, גופן קטן" — הכל בבת אחת. **נבחר (מתוך 3 דוגמאות):
   כיוון "ירוק רגוע"** — ירוק-טורקיז (primary ‎#0f9d78‎/#0f766e, רקע ‎#f4faf7‎, כתום ‎#f59e0b‎
   לפעולות משניות).
8. **בלי מסך בית** — המשתמש ראה דוגמה והכריע: לא צריך; שני השלבים נכנסים למסך הראשי.
9. **להסיר את הסיור המודרך** (כפתור ❓ + tour.py + ההצעה בהפעלה ראשונה).
10. **דיאלוג העדכון:** לעצב יפה יותר, פתיח משעשע "הנה העדכון שחלמת עליו.. (או שלא)" —
    **בלי להזכיר שמות** בתוך התוכנה (הכרעה מפורשת).

## v2.59 — פישוט שלב 2: צמצום 6 לשוניות ל-3 אזורים (24/08/2026)
המשך `UX_SIMPLIFY_BRIEF.md` סעיף 8 ("בינוני"): **חלוקה** (חלוקה ורישום · חד פעמי ·
חלוקות קודמות) · **אנשים** (כל המקבלים · חיפוש מהיר) · **הגדרות**. מימוש: `QTabWidget`
פנימי לכל אזור (property `subtabs` → עיצוב פיל-בהיר ב-styles.py); כל לשונית-תוכן שומרת
attribute + objectName `tab_<key>` (הסיור המודרך אותר רקורסיבית — `_locate_tab`).
חדש ב-`MainWindow`: `navigate_to_tab` / `_current_leaf` / `_leaf_tabs` / `_show_leaf`.
כותרת-סטטוס נפרדת לא נדרשה — שורת המונים (סומנו/סה"כ/נפשות) + תאריך כבר קיימים במסך.
אומת: test_all ‏104/104, test_selection, test_fixes, test_scenarios + 5 צילומים אמיתיים
(RTL תקין, שני האזורים, ניווט מקונן). **נותר מהתוכנית (טרם בוצע):** שני מצבים מפורשים
"הכנת רשימה"/"רישום מי קיבל" (ממתין להבהרת #yjcny), מסך-בית מוכוון-משימה (שלב "גדול").

## v2.58 — פישוט המסך הראשי "חלוקה ורישום" (צעד 1 + מיזוג בחירת חד-פעמיים) — הקוד מוכן ואומת
מקור: `UX_SIMPLIFY_BRIEF.md` + הכרעות המשתמש (24/08/2026): חד-פעמיים הם חלק מ**כל** חלוקה
(מוצרים/רזרבה נשארים גלויים!), מייל למתנדב **לא** מרכזי (קופל), הדפסה = הפעולה הראשית.
- **שני אזורים מתקפלים** (`_CollapsibleCard`, סגורים כברירת מחדל): "מצבי חלוקה מתקדמים"
  (mode_combo + סמן מובילים + הגדר סינון — הוצאו מה-toolbar) ו"שליחה למתנדב במייל" (כל כרטיס
  המתנדב; הקליטה האוטומטית ממשיכה ברקע). האזור המתקדם נפתח אוטומטית בבנייה אם mode≠schedule
  או סינון פעיל (פתיחה-בלבד; "חלוקה חדשה" מקפל אם אין מצב מיוחד).
- **בחירת חד-פעמיים בלי לעזוב את המסך:** כפתור "בחר חד-פעמיים" בתוך כרטיס החיווי →
  `OneTimePickerDialog` (אותה ליבה: `get_one_time_list`+`compute_suggested_n`+`assign_roles`,
  מומלצים מסומנים מראש, רזרבה בסגול, לחיצה על שם = פירוט ניקוד) → `add_one_time_picks` הקיים.
  **השער:** `_one_time_gate_ok` נשאר טהור (הבדיקות!); עוטף חדש `_ensure_one_time_picks`
  פותח את החלון במקום לקפוץ ללשונית "חד פעמי", וממשיך את הפעולה אם אושרו picks.
  לשונית "חד פעמי" נשארת כגיבוי בינתיים.
- **שם אוטומטי:** `_effective_dist_name` — שם ריק בשבועי רגיל (schedule+בלי סינון) ← "חלוקה
  שבועית DD/MM/YYYY" נכתב לשדה וממשיך (הדפסה/PDF/שליחה/שמירה); במצב מיוחד החסימה נשארת.
  שמות אוטומטיים לא נדחפים להיסטוריה (`_push_name_history`) ומנוקים מהשדה אחרי שמירה.
- אומת: test_all ‏102/102, test_selection, test_fixes, test_scenarios + סקריפט אימות ויזואלי
  (5 צילומים אמיתיים, RTL תקין, 17 בדיקות התנהגות). **שוחרר v2.58** (24/08/2026).
- **מדיניות חדשה מהמשתמש:** שחרור לגיטהאב אוטומטי אחרי כל שינוי מאומת (ראה CLAUDE.md).

## סבב תקלות מדוח 23/08/2026 — v2.54 (הקוד מוכן, אומת; **טרם build/release**)
5 תקלות, כולן טופלו ואומתו (טסטים + צילומי מסך אמיתיים):
- **#fw5s2 + #j6czs** — בדיאלוג "הוסף/ערוך מקבל": שדה "תדירות" מוצג **רק** כשעדיפות="קבוע", עם 3 אפשרויות בלבד (בלי שדה ריק). התדירות הנשמרת נגזרת מהעדיפות (`_effective_frequency`). ✔ צולם.
- **#ow11d** — החלון השחור בבורר "מצב חלוקה לקבועים": `mode_combo.setView(_light_popup_view())` (אותו דפוס כמו שאר הקומבואים). ✔ צולם popup בהיר.
- **#qxnvx** — כפתור "שמור PDF" חדש ב"חלוקה ורישום" → `print_view.export_distribution_pdf` (אותו HTML, לתיקיית הורדות, פתיחה אוטומטית). ✔ נוצר PDF אמיתי.
- **#vq4fx** — מצב חלוקה חדש **"לפי סינון מותאם"**: כל המקבלים הפעילים העונים על ספי מספר-ילדים/הכנסה/פנוי-לנפש (בלי עדיפות/אזור). `selection.filter_by_criteria` + `db.get_filtered_list` + `FilterCriteriaDialog`. טסטים ב-`test_selection.py`. ✔ צולמו דיאלוג + לשונית.

**להמשך:** bump כבר בוצע ל-2.54. לבנות EXE → `cp dist/מנהל_חלוקה.exe dist/Manhal-Haluka.exe` → commit/push → `gh release create v2.54 ...`. (לשחרר רק כשהמשתמש מבקש.)

---

## סבב תקלות מדוח 14/07/2026 (ישן — ייתכן ששוחרר בגרסאות 2.50–2.53)
מקור: `דיווח_תקלות.html` (6 תקלות). מצב:
- **#kdx4u** (סדר טורים RTL) — **תוקן ואומת.** הבאג היה ב**דיאלוג "פירוט ניקוד"** (`utils/ui.py show_score_breakdown`), לא בטבלה הראשית: `QTextDocument` מתעלם מ-`dir='rtl'` בטבלאות → כתבתי את העמודות בסדר הפוך ידנית (גורם מימין). צילום אימות.
- **#nfp9i** (בזבוז מקום במייל למתנדבים) — **תוקן ואומת.** `tabs/settings.py`: הוחלף ה-`QGridLayout` המשותף-שורות בשתי עמודות `QVBoxLayout` עצמאיות → פאנלים נצמדים למעלה בלי רווח; "אזור מסוכן" ברוחב מלא למטה.
- **#gl62a** (למה רואים גיבויים) — **תוקן.** נוספה שורת-הסבר קצרה בפאנל הגיבויים.
- **#abv1w** (טבלת קבועים ריקה, קריטי) — **לא שוחזר בקוד הנוכחי.** ה-DB (%APPDATA%) מכיל 518 מקבלים אך רק **19 קבועים** (שאר 498 חד-פעמי, מזה 343 קוד-1 "נתונים בלבד") — וזה תקין לפי המשתמש. הקוד מציג את ה-19 תקין (צולם, מצב "לפי ניקוד"). "ריק" קורה רק במצב "רגיל" כשאף קבוע לא תורם השבוע → **שופרה ההודעה** ב-`group_update._populate` (מסבירה + מכוונת ל"לפי ניקוד"). אם המשתמש עדיין רואה ריק — צריך צילום + באיזה מצב.
- **#yjcny** (בינוני — "אין אפשרות לסמן מי קיבל ומי לא") — **ממתין להבהרת המשתמש.** היום: צ'קבוקס=קיבל, "שמור חלוקה" רושם רק מסומנים; לא-מסומן = לא נרשם (אין רישום "לא קיבל"). לא ברור אם רוצים רישום-שלילי מפורש או שרק לא ברור שהצ'קבוקס הוא הסימון.
- **#yz3v1** (נמוך, לשונית חלוקות) — **בלי תיאור בדוח** — צריך פרטים מהמשתמש.

בדיקות עוברות (102/102 + selection + fixes). **טרם שוחרר** — לאסוף את #yjcny/#yz3v1 ולשחרר יחד (bump `version.py`, build, release).



## גרסה אחרונה
v2.54 — **תיקון: הדפסה/PDF ב"חלוקה ורישום" הראו רק את הרזרבה.**
- `group_update._get_export_rows` החזיר `checked + reserve`. כברירת מחדל אף אחד לא
  מסומן לפני החלוקה (bug #ebnr2), ולכן כשהיו רזרבות ה-fallback ל"הדפס את כולם" לא
  נכנס — הרשימה שנשמרה/הודפסה כללה **רק את מקטע הרזרבה** ואת כל הרשימה הראשית איבדה.
- תוקן: הרשימה להדפסה/ייצוא היא רשימת ☐ ריקה שמוסרת ליד → תמיד כוללת את **כל**
  רשימת השבוע (קבועים + picks של חד-פעמי) + מקטע הרזרבה בנפרד. הסימון (checked)
  קובע רק מה **נרשם** ב-`_save`, לא מה מודפס.
- `test_scenarios.py` S2: נוספו טענות שהפלט כולל את הקבועים ואת ה-picks לפני סימון
  (נכשלו לפני התיקון, main rows=0).

### v2.49 — **סבב תיקונים גדול (21 פריטים) + כלי דיווח תקלות HTML.**
- **זרימת עבודה חדשה ב"חלוקה ורישום" (#9):** עורך המוצרים ("מה מחלקים") הוסר. במקומו
  שדות **"מוצרים זמינים"** + **"רזרבה"** (מקור-אמת יחיד; נקראים ב"חד פעמי" מההגדרות
  `available_products`/`reserve_count`). חיווי חי "נשאר לחד-פעמיים". **שער הדפסה/ייצוא:**
  אם נשארו מנות לחד-פעמיים ולא נבחרו — חסום, וניווט אוטומטי ללשונית "חד פעמי" (#11).
- **#4 (שני מקורות נתונים):** `delete_batch`/`delete_distribution` מגלגלים אחורה את
  `last_distribution`/`next_distribution` (`_recompute_recipient_dates`) — אחרי מחיקת
  היסטוריה כבר לא מופיע "קיבל בתאריך".
- **#14/#10:** חלוקה בתאריך רביעי הקרוב (עתידי) כבר לא מבריחה קבועים — `get_weekly_list`
  `served_recently` כולל חלון עד רביעי הקרוב.
- **#12:** מצב "לפי ניקוד" מבוסס `get_regulars_scored` (קבועים בלבד) — חד-פעמיים נכנסים
  רק בהוספה מפורשת.
- **#7:** ההערה הכללית נשמרת פעם אחת ברמת האצווה (לא משוכפלת לכל מקבל).
- **#5:** לשונית "חד פעמי" מציגה רק מועמדים (עדיפות 3/2), לא חסרי-עדיפות.
- שאר: #2/#13 רקע הצעות לבן (completer popup), #6 הסרת ספירת מועמדים, #8/#21 הסרת "רענן"
  מיותר, #15 הודעה במצב "בלי קבועים", #16 פריסת הגדרות מאוזנת, #17 הסרת צעד "יצוא לאקסל"
  מהסיור, #18 גופן גדול בהדפסת כרטיס מקבל, #19 בדיקת מייל שולחת מייל אמיתי, #20 אפשרות
  לשלוח משוב למייל המפתח.
- **כלי דיווח תקלות:** `דיווח_תקלות.html` (קובץ יחיד, RTL, LocalStorage, ייצוא Markdown).
- בדיקות: `test_fixes.py` חדש (#4/#5/#7/#9/#12/#14). כל הסוויטה עוברת.

### v2.48 — **סבב "עורך-דין": ותק לפי תאריך רישום + שובר-שוויון לפי ותק.**
- ותק (`db.recency_days`): מי שלא קיבל מעולם מחושב מ-`start_date`→`created_at`, לא אפוק-2000 (שהשטח את הסקאלה). תאריך עתידי→0.
- שובר-שוויון בדירוג: ניקוד→ותק (הכי הרבה המתנה)→שם. שוויון כבר לא מוכרע לפי אלף-בית.
- **הכרעות עורך-הדין:** הכנסה 0 = "אין נתון"→תחתית (ללא שינוי). נורמליזציה ליניארית — נשארת (חריגים אמיתיים נספרים; שיטוח = מחיר מודע, לא לוגריתמי).
- **פתוח (רעיון, טרם מומש):** אזהרת טעות-קלדה בלשונית בדיקות — לסמן ערך חריג פי-כמה מהחציון, בלי לגעת בנוסחה. עדיפות מוטעית (1 במקום 3) עוקפת צורך — המקבל מופיע ב"אחרים" לסימון ידני.

### v2.47 — **ריכוז לוגיקת בחירת-המקבלים ל-`selection.py` (מקור-אמת יחיד) + 4 כללים עסקיים שהכריע המשתמש + תיקוני חישוב.** משחרר יחד עם סבב 2.46 שלא שוחרר.
- **`selection.py`** (טהור, נבדק ב-`test_selection.py`, 30+ תרחישים): gate (עדיפות 3/2) · דירוג (`rank_one_time_priority` = ראשונה גוברת / `rank_by_need` = ניקוד בלבד) · תפקידים main/reserve/out · רזרבה=המתנה.
- כלל 1: חד-פעמי — ראשונה לפני שנייה, ניקוד בתוך דרגה. מצב "לפי ניקוד" — ניקוד בלבד.
- כלל 3: רזרבה לא נרשמת (מודפסת כהמתנה, נרשמת רק אם הופעלה למקום מי שלא הגיע).
- כלל 4: חוסר-נתונים → תחתית התור (גורם חסר = 0 נק', לא 0.5).
- תיקונים: ניקוד ממוזג על סולם אחד ("לפי ניקוד"), `compute_suggested_n` מנכה רק קבועים שבתור השבוע, "סמן מובילים" תואם למוצג, `served_recently` מתעלם מתאריך עתידי.
- **פתוח לשיקול המשתמש (מודל, לא באג):** ניקוד יחסי-לרשימה (לא מוחלט), ותק של מי שלא קיבל מבוסס על ברירת-מחדל 2000 (מנרמל את הטווח), income=0 נחשב "אין נתון". לא שוניתי — ערכים אלה הם החלטת-ערך, לא באג.

### כלול בשחרור זה — סבב 2.46 (שלא שוחרר קודם), 14 פריטים:

**תוקן ואומת:**
- לוח התאריכים לא שחור יותר (סגנון בהיר מפורש ל-`WednesdayCalendar`).
- ניקוד: גורם יחיד רץ 0→100, הוצאה אפסית/חסרה = 0 (לא 50%). "high" מנורמל מ-0.
- גודל משפחה לא נכנס יותר כשובר-שוויון נסתר (מיון tie-break לפי שם בלבד).
- פירוט ניקוד: דיאלוג משותף `utils.ui.show_score_breakdown` (RTL תקין) — זמין גם בלשונית "חלוקה ורישום" בלחיצה על שם.
- ריבוע "סה\"כ" עבר לשורה נפרדת (לא נחתך במצב "לפי ניקוד").
- **מצב "קבועים לפי ניקוד" = רשימה ממוזגת** (`db.get_scored_all`): קבועים + חד-פעמיים (עדיפות 3/2) מדורגים יחד לפי ניקוד. סמן top-N לפי "מנות זמינות".
- ברירת מחדל "קיבל": במצב לוח-זמנים/רגיל כולם מסומנים, מבטלים למי שלא הגיע. במצב "לפי ניקוד" מתחילים ריק ומסמנים מובילים.
- מספר מוצרים משותף בין הלשוניות (`available_products`); בחד-פעמי הקבועים נצרכים קודם (`compute_suggested_n`).
- קבוע שקיבל השבוע נשאר ברשימה השבועית (אפשר חלוקה נוספת) + במצב לפי-ניקוד תמיד מוצג.
- מחיקת רישום חלוקה בודד מלשונית "חיפוש מהיר" (`db.delete_distribution`) — מנקה רישומים ישנים ללא batch.

**נותר לאישור המשתמש:** בדיקה בפועל ואז שחרור (bump כבר בוצע ל-2.46).

## פתוח להמשך (טרם אושר)
- ייבוא-אוטומטי (IMAP) של מיילים חוזרים ממתנדבים.
- לשונית "הגדרות": להוסיף שורת-הסבר קצרה ליד הגיבוי (תקלה 13 — הוסבר, טרם מומש).
