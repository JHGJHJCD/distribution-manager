# בריף עבודה: פישוט חוויית המשתמש — "מנהל חלוקה"

> **למי הבריף הזה:** למודל/סוכן שממשיך את העבודה בלי הקשר קודם. הכול כאן עצמאי.
> קרא קודם את `CLAUDE.md` (עקרונות + מלכודות ידועות) ואת `NEXT_TASK.md` (סטטוס שוטף).
> ניתוח ה-UX המלא שממנו נגזר הבריף: הופק כמסמך נפרד (ראה סעיף "רקע").

---

## 1. המטרה (במשפט)

להפוך את המסך הראשי של התוכנה מ**טופס-הגדרות עמוס** ל**"החלוקה של השבוע, מוכנה ללחיצה"** — בלי להוסיף אף פיצ'ר, בלי לשנות לוגיקה עסקית, ובלי לשבור תאימות/נתונים. פחות קליקים ופחות החלטות בפעולה היומית.

**עיקרון מנחה:** אם המערכת יכולה להחליט במקום המשתמש — שלא תכריח אותו להחליט.

---

## 2. רקע טכני מינימלי (כדי לעבוד מיד)

- אפליקציית **Windows בעברית, RTL מלא**, PyQt6 6.11 + SQLite. ~500 מקבלים. אין שרת — EXE יחיד.
- **Python לבנייה/בדיקה — רק** `C:\Users\יהודה\AppData\Local\Programs\Python\Python312\python.exe`
  (ה-`python` שב-PATH הוא 3.14 וחסר PyQt6/openpyxl/qt-material — יקרוס).
- הרצה: `python main.py` (סיסמה: `1234`).
- בנייה: `python -m PyInstaller --noconfirm --clean מנהל_חלוקה.spec` → `dist/מנהל_חלוקה.exe`.
- **צילומי מסך עברית:** להריץ **בלי** offscreen (offscreen מרנדר ריבועים). להשתמש ב-`WA_DontShowOnScreen` + `grab()`.
- ארכיטקטורה: לוגיקה עסקית ב-`selection.py`/`scoring.py`/`database.py` (מודולים טהורים); הלשוניות ב-`tabs/` רק מציגות. **אין לגעת בלוגיקה העסקית במשימה הזו** — זו משימת UI/פריסה בלבד.

**הקובץ המרכזי לעבודה:** `tabs/group_update.py` — לשונית "חלוקה ורישום" (לשונית הפתיחה, שם ~95% מהשימוש). המתודה `_build_ui`.

---

## 3. הממצא המרכזי (מה מנחה את השינוי)

הלוגיקה של התוכנה חזקה ומדויקת. הבעיה: **המסך הראשי מציג בכל שבוע פקדים שקיימים בשביל החלוקה המיוחדת הנדירה** (פסח וכד'), ומעמיס בכך על החלוקה השבועית הרגילה.

חשוב: **ההפרדה כבר קיימת בלוגיקה** — כש-`available_products = 0` (ברירת המחדל) התוכנה כבר מתנהגת כחלוקה שבועית פשוטה (`compute_suggested_n` מחזיר 0 לחד-פעמיים). היא פשוט **לא מיוצגת בממשק**. זה מה שמתקנים.

הפעולה שהכי מלוטשת כבר היום — **שליחה למתנדב + קליטה אוטומטית של התוצאות** — לא לגעת בה, רק (אולי) להבליט אותה.

---

## 4. המשימה המיידית — צעד 1 (קטן, בלי שינוי ארכיטקטורה)

**קיפול כל פקדי "החלוקה המיוחדת" לאזור מתקפל אחד, סגור כברירת מחדל.**

זה השינוי עם יחס התועלת-לסיכון הגבוה ביותר: מוריד 5 פקדים מהמסך השבועי, לא נוגע בלוגיקה, לא שובר תאימות — רק מזיז ווידג'טים קיימים לתוך מיכל מתקפל.

### 4.1 הפקדים שצריך לאסוף לתוך האזור המתקפל
כולם כבר קיימים ב-`GroupUpdateTab._build_ui` — צריך רק להעביר אותם לתוך מיכל אחד חדש:

| ווידג'ט (שם המשתנה) | היכן הוא היום | מה עושה |
|---|---|---|
| `self.mode_combo` (+ ה-`_field("מצב חלוקה לקבועים", ...)`) | ב-`toolbar` שמעל הרשימה | בורר 4 מצבי חלוקה לקבועים |
| `self.btn_mark_leaders` | ב-`toolbar` | "סמן מובילים" (רק במצב scored) |
| `self.btn_edit_filter` | ב-`toolbar` | "הגדר סינון" (רק במצב filter) |
| `self.products_spin` (+ `self.lbl_regulars_count`) | ב-`grid` של Card 1 "פרטי החלוקה" | "מוצרים זמינים" |
| `self.reserve_spin` | ב-`grid` של Card 1 | "רזרבה" |
| `self.leftover_card` (+ `self.lbl_leftover`) | ב-`grid` של Card 1 | חיווי חי "נשאר לחד-פעמיים" |

> שים לב: הפקדים מפוזרים היום בין **Card 1 (פרטים)** לבין **ה-toolbar** שמעל הרשימה. לאסוף את כולם לאזור אחד חדש שכותרתו למשל **"⚙ חלוקה מיוחדת — מוצרים, חד-פעמיים, סינון"**.

### 4.2 מה נשאר במסך השבועי הנקי
Card "פרטי החלוקה" עם: **שם החלוקה · תאריך · מחלק · הערה כללית** · הרשימה · כרטיס "שליחה למתנדב" · סרגל הפעולות התחתון (שמור / הדפסה / PDF). זהו.

### 4.3 כלל פתיחה/סגירה של האזור המתקפל
- ברירת מחדל: **סגור**.
- **נפתח אוטומטית** אם יש חלוקה מיוחדת פעילה כדי לא להסתיר מצב קיים, כלומר אם **אחד מ**:
  - `int(db.get_setting("available_products") or 0) > 0`, **או**
  - `db.get_regulars_mode() != "schedule"`, **או**
  - `selection.criteria_is_active(db.get_filter_criteria())` (סינון פעיל).
- אין ווידג'ט מתקפל מובנה בפרויקט — לממש פשוט: `QToolButton` checkable עם חץ ▸/▾ ככותרת, ומתחתיו `QFrame` שמוצג/מוסתר ב-`setVisible()`. (לא להשתמש ב-`QGraphicsDropShadowEffect` על כרטיס בתוך layout גמיש — מלכודת ידועה, שובר את גובה הכרטיס.)

### 4.4 מילוי-אוטומטי לשם חלוקה שבועית
היום שם ריק **חוסם** הדפסה/PDF/שליחה (ראה `_print`, `_export_pdf`, `_send_to_volunteer`, וה"שער" `_one_time_gate_ok`). בחלוקה שבועית רגילה זה חיכוך מיותר.
- **הרצוי:** אם השם ריק ואנחנו במצב שבועי רגיל (האזור המתקפל סגור / `mode == "schedule"` ואין מוצרים), להשלים שם ברירת-מחדל כמו **"חלוקה שבועית DD/MM/YYYY"** במקום לחסום — ולהמשיך.
- לשמור על החסימה הקיימת רק לחלוקה מיוחדת (שם משמעותי כמו "חלוקת פסח" עדיין חשוב שם).
- לבחור את הנקודה הנקייה ביותר ליישום (למשל helper `_effective_dist_name()` שממנו קוראים כל הפעולות), בלי לשכפל לוגיקה.

---

## 5. אילוצים וכללים (חובה)

- **RTL מלא** בכל שינוי UI. גופן **Segoe UI בלבד** (לא Rubik, לא `AA_EnableHighDpiScaling`).
- **אל תיגע** ב-`selection.py`/`scoring.py`/`database.py` — זו משימת פריסה/UI. כל ה-settings-keys וההתנהגות העסקית נשארים זהים.
- **תאימות לאחור מלאה**: אותם שמות settings (`available_products`, `reserve_count`, `dist_regulars_mode`, `dist_filter_criteria`), אותם signals, אותן מתודות. רק מיקום ויזואלי משתנה.
- לא לשבור את זרימת המתנדבים (שליחה/קליטה אוטומטית) ואת "שער" בחירת החד-פעמיים — רק לשנות את **מיקום** הפקדים ואת התנהגות שם-ברירת-המחדל.
- שמור על ה-`valueChanged`/`currentIndexChanged` הקיימים (`_on_products_changed`, `_on_reserve_changed`, `_on_mode_changed`, `_update_mode_controls`, `_update_leftover_hint`) — הם חייבים להמשיך לעבוד אחרי ההזזה.

---

## 6. הגדרת "גמור" (Definition of Done)

1. המסך השבועי (ברירת מחדל, ללא מוצרים) מציג רק: פרטים בסיסיים + רשימה + שליחה למתנדב + פעולות. פקדי המוצרים/רזרבה/מצב/סינון **מוסתרים** מאחורי "חלוקה מיוחדת".
2. פתיחת "חלוקה מיוחדת" מחזירה בדיוק את היכולות הקיימות (מוצרים, רזרבה, 4 המצבים, סימון מובילים, סינון) — כולן עובדות כמו קודם.
3. אם קיימת חלוקה מיוחדת פעילה — האזור נפתח אוטומטית (לא מסתיר מצב קיים).
4. חלוקה שבועית עם שם ריק — נשמרת/מודפסת/נשלחת עם שם ברירת-מחדל אוטומטי, בלי חסימה.
5. **אימות ויזואלי אמיתי** (צילום מסך, לא רק "לא קרס") — RTL תקין, שני המצבים (סגור/פתוח).
6. הבדיקות הרלוונטיות עוברות: הרץ `python test_all.py` ו-`python test_selection.py` (כל אחד בנפרד — קובצי `test_*.py` קוראים `sys.exit` ברמת המודול, אל תריץ ב-pytest).
7. **בנה EXE** אחרי השינוי (`python -m PyInstaller --noconfirm --clean מנהל_חלוקה.spec`) — כלל קבוע בפרויקט.
8. עדכן `NEXT_TASK.md` (סטטוס) ואת `CLAUDE.md` אם המבנה הוויזואלי של הלשונית השתנה מהותית.

---

## 7. שאלה אחת לאשר עם המשתמש לפני הבלטת הכפתור הראשי

מה המסלול היומי העיקרי:
- **(א) שליחה למתנדב במייל** (כפי שמתואר בקוד ובהנחיות הפרויקט) → הכפתור הראשי צריך להיות "שלח למתנדב".
- **(ב) הדפסה וחלוקה עצמית** → הכפתור הראשי צריך להיות "הדפסה".

עד שהמשתמש עונה — **אל תשנה את היררכיית הכפתורים הקיימת**; צעד 1 (הקיפול) לא תלוי בזה ואפשר לבצע אותו מיד.

---

## 8. מפת המשך (אחרי שצעד 1 יציב) — לא לבצע עכשיו

- **בינוני:** מיזוג בחירת החד-פעמיים **פנימה** (פאנל באותו מסך במקום קפיצה ללשונית "חד פעמי"); שני מצבים מפורשים "הכנת רשימה" / "רישום מי קיבל" במקום ה-☑ הדו-משמעי; צמצום 6 לשוניות ל-3 (חלוקה · אנשים · הגדרות); כותרת-סטטוס למעלה (תאריך, כמה מקבלים, כמה נפשות).
- **גדול:** מסך בית מוכוון-משימה ("מה עושים היום") עם 3 צעדים (רשימה ← שליחה/חלוקה ← רישום); המערכת מזהה לבד רגיל מול מיוחד לפי מוצרים; אשף לחלוקה מיוחדת.
- ניקוי נלווה: `tabs/summary.py` אינו מחובר (קוד מת — לוודא לפני מחיקה); ה"סיור המודרך שמסביר כל כפתור" הוא סימן לעודף כפתורים — הצורך בו יפחת ככל שהמסך מתנקה.

---

## 9. סדר עדיפויות בהתנגשות (מהפרויקט)

**שלמות נתונים → כללים עסקיים → יציבות → חוויית משתמש → איכות קוד.** כששניים מתנגשים, המוקדם מנצח. במשימה הזו: אם ספק — אל תיגע בלוגיקה/נתונים, רק בפריסה.
