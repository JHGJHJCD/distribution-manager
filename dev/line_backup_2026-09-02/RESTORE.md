# שחזור — סקר אישור הגעה (שלוחה 77) + גרסה 3.01

## מה השתנה (ורק זה)
- **בקו:** נוצרה שלוחה חדשה `/77` (סוג "קבלת נתונים") + קובץ `77/050.tts` (טקסט השאלה).
  לא שונה שום דבר אחר: לא השורש (`ext.ini`/`Did_Go_To.ini`/`ivr.ini`), לא התבניות.
  צילום-מצב של השורש: `root_snapshot.md` באותה תיקייה.
- **בתוכנה:** גרסה 3.01 (קומיט נפרד). נקודת-שחזור בגיט: תג `pre-survey` (= קומיט 3f1287f, גרסה 3.00).
  אין שינוי במבנה מסד הנתונים. הגדרות חדשות (מפתחות נוספים בלבד): `tzintuk_answer_labels`,
  `tzintuk_survey_prompt`. התשובות נשמרות בתוך `report_json` של הצינתוקים (שדה `answer`).

## שחזור הקו (2 דקות)
1. בממשק ימות: ניהול שלוחות ← מחיקת התיקייה **77** (או דרך MCP: `file_action delete ivr2:/77` — עובר לסל,
   ניתן לשחזור). זהו — הקו חוזר בדיוק למצבו הקודם.
2. אם בוצעה תוכנית דרך `plan_extension_changes` — אפשר גם `rollback_extension_plan` עם ה-planId
   שנרשם בסוף הקובץ הזה.

## שחזור התוכנה
- קוד: `git revert <קומיט 3.01>` או `git checkout pre-survey`.
- EXE: להוריד ידנית את `Manhal-Haluka.exe` של Release **v3.00** מ-GitHub (JHGJHJCD/distribution-manager)
  ולהחליף את הקובץ. התוכנה הישנה מתעלמת מההגדרות/השדות החדשים.
- נתונים: גיבוי אוטומטי בהפעלה ב-`%APPDATA%\ManhalHaluka\backups` (שחזור בלחיצה מההגדרות).

## מזהי ביצוע (מתמלא בזמן העבודה)
- planId של יצירת /77: (יתעדכן)
