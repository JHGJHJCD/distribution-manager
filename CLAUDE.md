# CLAUDE.md

הנחיות ל-Claude Code בעבודה על מאגר זה. סטטוס שוטף ("איפה עצרנו") — ב-`NEXT_TASK.md`, לא כאן.

> תחזוקה עצמית: בשינוי מבני משמעותי (לשונית/פיצ'ר גדול/שינוי בבנייה-שחרור) — עדכן קובץ זה באותה הזדמנות. סטטוס זמני → `NEXT_TASK.md`.

## עקרונות עבודה
- **המשתמש לא מתכנת** — לעבוד ולשחרר, לא להסביר צעדים טכניים. הערת-שחרור קצרה בעברית פשוטה בסוף.
- אל תשבור תאימות לאחור; אל תשנה שמות עמודות/שדות ב-DB בלי מיגרציה.
- שמור RTL מלא ופשטות למשתמש בכל שינוי UI.
- לפני מחיקת קוד — ודא שאינו בשימוש.
- כשאפשר לאמת — אמת בעצמך (בדיקה/צילום מסך אמיתי), אל תבקש מהמשתמש. **אימות = העיקרון החשוב ביותר:** אם יש דרך לסגור לולאת-משוב לבד (בדיקות/הרצה/צילום), השתמש בה עד שהתוצאה נכונה.
- **משימה מורכבת → תכנן קודם (plan mode):** השקע בתכנון לפני מימוש. אם משהו משתבש באמצע — חזור לתכנון מחדש, אל תתקן תוך כדי ריצה. במקרה גבולי או UX — שאל שאלה ממוקדת אחת לפני שמתחילים.
- **אל תסתפק בפתרון הראשון:** אחרי מימוש בינוני — שקול "לזרוק ולממש את הפתרון האלגנטי" מחדש עם מה שלמדת. אחרי שינוי — עבור על הקוד המשתנה לאיתור שכפול/סיבוך/חוסר-יעילות (`/simplify`).
- **הנדסה מצטברת:** בכל פעם שטעיתי או שהמשתמש תיקן אותי — הוסף את הכלל ל-CLAUDE.md (או למלכודות/זיכרון) כדי לא לחזור על הטעות.
- **תחזוקת סקילים (הנחיה קבועה):** בכל פעם שאני לומד משהו חדש שרלוונטי לאחד הסקילים ב-`.claude/skills/` — עדכן אותו **באותה הזדמנות**, בלי לחכות שיבקשו. מיפוי: שלב/כלי/מלכודת בבנייה-שחרור → `manhal-haluka` (ומלכודות ל-`references/pitfalls.md`); דפוס צילום/UI → `visual-check`; כלל בחירת-מקבלים/ניקוד → `selection-rule`; שינוי סכימה/סנכרון → `db-migration`; **באג/כלל/תרחיש בלוגיקת הצינתוקים → `tzintuk-check`** (6/9/2026: אינווריאנט חדש = שורה ב-`references/invariants.md` + לינט ב-`scripts/check_tzintuk.py`; הבודק = `python .claude/skills/tzintuk-check/scripts/check_tzintuk.py` — לינט 33 כללים מבאגים ישנים + `test_tzintuk`+`test_sync`; להריץ לפני ואחרי כל עבודה על `tzintukim.py`/`yemot.py`). **באג/כלל/תרחיש בלוגיקת המיילים → `mail-check`** (14/9/2026, אח-תאום של `tzintuk-check`: `references/invariants.md` (32 כללים M1–M32) + לינט ב-`scripts/check_mail.py`; הבודק = `python .claude/skills/mail-check/scripts/check_mail.py` — לינט + `test_mail`+`test_sync`; להריץ לפני ואחרי כל עבודה על `mails.py`/`mailer.py`/`google_auth.py`/נתיב השליחה ב-`email_utils.py`). **בקשת תכונה חדשה / שינוי התנהגות → `feature-impact`** (6/9/2026: "מה זה שובר / אילו כפתורים רלוונטיים במצב החדש / מה חסר במסכים אחרים" — לענות לפני הקוד; מקומות שנלמדו שמחלישים תכונה חצי-גמורה → להוסיף לרשימות שם). אם הידע לא שייך לאף אחד מהם — שקול סקיל חדש. עדכון סקיל = חלק מ-Definition of Done של המשימה שבה למדתי.
- **הקשר נקי:** במשימות רחבות/סריקה — האצל לסוכני-משנה (subagents) כדי לשמור על מיקוד, במקום לגרור הכל לשיחה הראשית.
- **ניהול ידע — דפוס LLM Wiki (14/9/2026, פירוט ב-CLAUDE.md הגלובלי + זיכרון `reference-llm-wiki-pattern`):** קובץ זה + הסקילים = ה-wiki (אני כותב, יהודה קורא); `docs/raw/` = מקורות קפואים (תמלולי שיחות/דוחות/תיעוד כפי שהתקבלו — לא עורכים, בלי סודות); `git log` + `NEXT_TASK.md` = היומן. **Ingest:** מקור חדש → `docs/raw/` → תובנות לדפים (CLAUDE/סקיל/NEXT_TASK) עם הפניה למקור. **Query→דף:** ניתוח/הכרעה מהשיחה נכתבים חזרה למסמך. **Lint:** `/בדוק-הקשר` (כולל יתומים/מושגים בלי דף/טענות מיושנות/raw לא מעובד).

## Architecture Rules
- העדף שימוש בקוד קיים על פני כתיבת חדש; אל תשכפל לוגיקה — חלץ helper משותף.
- לוגיקה עסקית מחוץ ל-UI כשאפשר (`scoring.py`/`selection.py`/`database.py` — מודולים טהורים), הלשוניות רק מציגות.
- שמור על הפרדה קיימת: `scoring` = ניקוד, `selection` = מי-מקבל (gate/סדר/רזרבה), `database` = נתונים, `tabs/` = תצוגה, `utils/` = שירותים. **כלל בחירת-מקבלים חדש? ל-`selection.py` + טסט ב-`test_selection.py`, לא לתוך לשונית.**

## סדר עדיפויות בהתנגשות
**שלמות נתונים → כללים עסקיים → יציבות → חוויית משתמש → איכות קוד.** כששניים מתנגשים, המוקדם מנצח.

## לפני סיום משימה (Before completing a task)
- הרץ את הבדיקות הרלוונטיות (`python test_X.py` הנוגעים לשינוי).
- אם נגעת ב-UI — אמת ויזואלית (צילום מסך אמיתי / הרצה), לא רק שהקוד רץ.
- בשינוי מבני — עדכן `CLAUDE.md`; בשינוי סטטוס — עדכן `NEXT_TASK.md`.

**Definition of Done:** השינוי עובד ואומת בפועל (לא רק "לא קרס"), הבדיקות הרלוונטיות עוברות, לא נשברה תאימות/שלמות נתונים, והתיעוד עודכן אם צריך.

## ⚠️ מלכודות ידועות (Known Pitfalls)
- **Python:** בנה/בדוק רק עם `C:\Users\יהודה\AppData\Local\Programs\Python\Python312\python.exe`. ה-`python` שב-PATH הוא 3.14 וחסר PyQt6/openpyxl/qt-material.
- **גופן:** Segoe UI בלבד. **אל** תחזיר Rubik (רונדר מטושטש) ואל תוסיף `AA_EnableHighDpiScaling` (הוסר ב-PyQt6).
- **אל תשתמש ב-`QGraphicsDropShadowEffect`** על כרטיס בתוך layout גמיש — שובר את משא-ומתן הגובה ומכווץ תוכן.
- **openpyxl:** `get_column_letter(col)` בלבד — `.column_letter` קורס על MergedCell.
- **הדפסה:** `QTextDocument` מתעלם מ-`dir` → עמודות RTL נכתבות בסדר הפוך ידנית.
- **צילומי מסך עברית:** להריץ **בלי** offscreen (offscreen מרנדר ריבועים) — עם `WA_DontShowOnScreen` + `grab()`.
- **בדיקות:** קובצי `test_*.py` קוראים `sys.exit` ברמת המודול → pytest קורס באיסוף. הרץ כל אחד `python test_X.py`.
- **נטפרי חוסם צ'אטים של Claude על תמונות (1/9/2026, 3 צ'אטים אבדו):** נטפרי סורק את גוף הבקשות ל-`api.anthropic.com` **כולל תמונות**; `Read` של PNG מסוים (קרה עם `dev/shots_classic_track/*.png` וב-2/9 עם `dev/_shots/tzintuk_tab*.png`) יכול להקפיץ 418 "badwords" — ומאותו רגע הצ'אט מת לצמיתות (התמונה נשלחת מחדש בכל בקשה), וגם "המשך שיחה" יורש אותה ונחסם מיד. **כלל:** אם צ'אט נחסם מיד אחרי קריאת צילום — לא לקרוא את הקובץ שוב בשום צ'אט; לאמת צילומים חשודים דרך `gemini_task.py -f <png>` (ראיית Gemini, רק טקסט חוזר), ואחרי חסימה לפתוח צ'אט חדש נקי מ-`NEXT_TASK.md`, לא "המשך".
- **דליפת הגדרות:** סקריפט שמריץ `init_db()` על DB אמיתי עלול לדלוף — נקה `DELETE FROM settings WHERE key LIKE 'need_w_%'`.
- **הודעת "_MEI / Failed to remove temporary directory":** מגיעה מ**תהליך-האב של bootloader** של PyInstaller onefile (מנקה את `_MEI` אחרי שהתהליך-הבן יוצא), **לא ניתן לחסום מהקוד** — `os._exit`/`_hard_exit` רץ בבן ולא משפיע על האב. הסיבה במכונה זו: NetFree מחזיק את התיקייה לסריקה → המחיקה נכשלת. לא שובר נתונים. הפתרון האמיתי היחיד = בניית onedir (שוברת את מנגנון העדכון EXE-יחיד). **הכרעת המשתמש 27/08/2026: להשאיר כמו שזה — לא לגעת.** אל תשקיע שוב בניסיון לחסום מהקוד.

## עץ הפרויקט
```
main.py            # tab_specs, splash, single-instance, DPI, יציאה
database.py         # SQLite + re-export של scoring/selection
scoring.py          # מודול טהור: annotate_need_scores, NEED_FACTORS
selection.py        # מודול טהור: מקור-אמת יחיד ל"מי מקבל" (gate/ranking/roles/reserve)
widgets.py          # ProductsEditor ועוד
styles.py · version.py
tabs/
  group_update.py   # "חלוקה ורישום" (לשונית פתיחה, שני שלבים: הכנה/רישום). v3.03: עוצב מחדש בשפת ההגדרות/צינתוקים (כותרת+צ'יפים חיים, כרטיסים ממוספרים ① ②, סרגל תחתון ③)
  recipients.py     # CRUD מקבלים
  search.py         # "חיפוש מהיר" מאוחד
  distributions.py  # "חלוקות קודמות" (אצוות; באזור "אנשים" מאז v2.60)
  settings.py       # סיסמה/עדכון/משקלים/SMTP/גודל-טקסט/סף התראות
  review.py         # בדיקת כפילויות (דיאלוג בלבד)
  messages.py       # v2.76: לשונית "הודעות" — צ'אט צוות מסונכרן (✓✓, זמן יחסי, שעון ישראל, רקע מותאם). v2.78: מחיקת הודעה ע"י כותבה (op סנכרון msg_delete)
  tzintukim.py      # v2.82: לשונית "צינתוקים" — הודעה קולית לזכאי החלוקה דרך ימות המשיח. v3.53: עוצב מחדש (/שפר-מסך)
  (one_time.py הוסר ב-v2.60 — OneTimePickerDialog ב-group_update מחליף אותו)
utils/
  email_utils.py · excel_utils.py   # זרימת מתנדבים
  richtext.py        # v3.50: עורך-מייל מעוצב → HTML נקי (document_to_markup/load_into/align_kind)
  yemot.py           # v2.82: לקוח ה-API של ימות המשיח (call2all) — טהור, בלי Qt
  sync.py            # v2.61: סנכרון בין 2 מחשבים דרך תיקיית Google Drive (journal-per-device, LWW)
  print_view.py · updater.py · backup.py · feedback.py · ui.py  (tour.py הוסר ב-v2.60)
  timefmt.py         # v2.76: פורמט זמן משותף — שעון ישראל (Asia/Jerusalem) + זמן יחסי בעברית
  yemot.py · tts.py · call_history.py · callback_server.py   # צינתוקים: לקוח ימות, יצירת הקלטה מטקסט, היסטוריה מהשרת, שרת המענה (Cloudflare)
  hebdate.py         # v2.93: שם חלוקה עברי אוטומטי (pyluach)
  netblock.py        # v3.22: זיהוי חסימת NetFree (418) והודעה אחידה
  effects.py · win_frameless.py   # מראה מודרני: צללים/אנימציות, חלון ללא-מסגרת ששומר Aero Snap
  _secret.py         # סודות מקומיים (ב-.gitignore; נכנס רק ל-EXE) — Issue אוטומטי בגיטאב
dev/                # probe/screenshot/benchmark/stress/create_icon
test_*.py           # בשורש
```

## מה הפרויקט
אפליקציית Windows בעברית (RTL מלא) לניהול חלוקת מצרכים ל"קופה של צדקה הר יונה" (נוף הגליל), ~500 מקבלים. המנהל מפעיל **מתנדבים** שמחלקים בנקודה קבועה; המתנדבים לא נוגעים בתוכנה — מקבלים רשימה במייל, ממלאים מי הגיע, והקובץ מיובא חזרה.

## Tech Stack, הרצה ובנייה
- Python 3.12, PyQt6 6.11, SQLite, openpyxl, qt-material (theme light_teal — "ירוק רגוע" v2.60: primary ‎#0f9d78‎/#0f766e, רקע ‎#f4faf7‎, כתום ‎#f59e0b‎ לפעולה משנית). מייל דרך `smtplib`/`email.mime` (stdlib בלבד).
- אין שרת — EXE יחיד (PyInstaller, `מנהל_חלוקה.spec`), DB מקומי.
```
python main.py                                              # הפעלה (סיסמה: 1234)
python -m PyInstaller --noconfirm --clean מנהל_חלוקה.spec   # → dist/מנהל_חלוקה.exe
```
אם ה-EXE נעול (WinError 5) לפני בנייה: `rm -f "dist/מנהל_חלוקה.exe" "dist/Manhal-Haluka.exe"`.

## סנכרון בין 2 מחשבים (`utils/sync.py`, v2.61)
- **דרישת המשתמש:** לעבוד משני מחשבים במקומות שונים על אותם נתונים, כולל בו-זמנית. אין שרת → הסנכרון רוכב על **תיקיית Google Drive משותפת** (Drive for Desktop).
- **מנגנון:** כל מחשב כותב journal משלו (`journal-<device>.jsonl`) — כותב יחיד לכל קובץ, בלי מיזוג של Drive. כל שינוי נתונים ב-`database.py` קורא `_sync_log(op, payload)` → `sync.log_change` (מזהה שורות ב-`guid` יציב, לא ב-id מקומי). כל מחשב קורא את ה-journals האחרים ומחיל רשומות שלא ראה (מעקב per-device ב-`sync_state.json` **ליד ה-DB**, לא ב-settings שמסונכרן בעצמו). **התנגשות על אותו כרטיס = last-write-wins לפי חותם UTC** (`updated_at`).
- **טבלאות:** עמודות `guid` ב-recipients/distributions/dist_batches + `updated_at`/`representative_auto` ב-recipients (מיגרציה אוטומטית, back-fill guid). ops: `rec_upsert`/`rec_delete`/`batch_add`/`batch_delete`/`dist_delete`/`dist_add`/`setting`.
- **התאמת אישיות חוצת-מחשבים** (`_adopt_match`): guid לא מוכר → זיהוי לפי `external_id` ואז שם+טלפון, כדי לא לשכפל מי שכבר קיים בשני המחשבים.
- **offline:** שינויים מצטברים ב-`sync_outbox.jsonl` ונשלחים בריצה הבאה. `EXCLUDED_SETTINGS` (password/win_geometry/backup) לא מסונכרנות.
- **דחיסת יומן (v3.51, סקירת "פצצות-זמן" 15/9/2026):** ה-journal של כל מחשב גדל לנצח ו-Drive מעלה את כל הקובץ בכל שינוי ⇒ אחרי שנים עשרות MB לכל עריכה. `flush` שחוצה `JOURNAL_MAX_BYTES` (3MB) קורא ל-`compact_journal`: היומן הישן → `archive-<dev>-<stamp>.jsonl` (נשמרים 2, לא נקראים ע"י אחרים), ויומן חדש **עצמאי**: שורת `journal_head` עם `epoch` (המחשב השני משווה ל-`state["epochs"][dev]` ומאפס offset כשהשתנה — בלי זה offset ישן היה נוחת באמצע הקובץ החדש), **tombstones** של מחיקות (`rec/batch/dist/msg_delete` נזכרים ב-`sync_state["tombstones"]`, עד 5000) כדי שמחשב שהיה מנותק לא יחזיק מקבל שנמחק, snapshot מלא, והגדרות עם ה-ts **האמיתי** שלהן (`log_change(ts=)`) כדי ש-LWW לא ידרוס הגדרה חדשה יותר במחשב השני. seq ממשיך לעלות ⇒ כל מחשב מחיל את הראש פעם אחת. גם: `sync_incoming` (יומן-מנהל) נגזם ל-400 יום/5000 שורות; `yemot_history.json` שומר 48 חודשים; `updater` שולח `If-None-Match` (304 לא נספר במכסת 60/שעה של גיטהאב). בדיקה ב-`test_sync.py` (סעיף הדחיסה).
- **UI:** `SyncSetupDialog`+`_refresh_sync_status` בהגדרות. **הפעלה בלחיצה אחת (v2.69):** כפתור ראשי "הפעל סנכרון אוטומטי" → `sync.auto_setup()` — מזהה את «Drive שלי» (`detect_drive_folders`), יוצר בתוכו תיקייה בשם קבוע `sync.SHARED_SUBFOLDER` ("מנהל-חלוקה-משותף") כך ש**שני המחשבים נופלים על אותה תיקייה בלי תיאום ידני**, נותן למחשב שם מ-hostname (`suggested_device_name`), מפעיל+זורע+מושך. אם Drive לא מותקן (`drive_installed()==False`) → `_no_drive_message` עם קישור הורדה ישיר (`DRIVE_INSTALLER_URL` = GoogleDriveSetup.exe). קישור הורדה גלוי תמיד בדיאלוג. הבחירה הידנית (תיקייה/שם) עברה ל"אפשרויות מתקדמות" מקופל. רקע: `MainWindow._setup_auto_sync` — `_SyncWorker(QThread)` כל 20 שנ' + ריצה 2.5 שנ' אחרי עלייה; `refresh_all` רק כשנקלטו שינויים.
- **בדיקות:** `test_sync.py` (מדמה 2 מחשבים על 2 DB עם תיקייה משותפת: seed, הפצת add/edit/delete, batch, LWW, offline, idempotence, adopt-by-match).

## נתונים ואבטחה
- DB + גיבויים ב-`%APPDATA%\ManhalHaluka\` (data.db, backups/) — נפרד מה-EXE, שורד עדכונים, מהגר DB ישן אוטומטית.
- סיסמה מגובבת PBKDF2 (`database.verify_password/set_password`), ברירת מחדל `1234`.
- גיבוי אוטומטי בהפעלה ולפני כל פעולה הרסנית (Online Backup API — הגיבוי הוא עותק). שחזור מאמת שהקובץ DB תקין לפני דריסה.

## מיילים לרון (מנהל הקופה) — סקיל `reply-to-ron` (16/9/2026)
כל מייל לרון (`ka230846@gmail.com`) לפי הסקיל: HTML מיושר לימין, הכי קצר שאפשר, שורה ראשונה מודגשת "🤖 הודעה מקלוד (העוזר של יהודה)", חתימה "קלוד (בשם יהודה)", בתוך השרשור המקורי, ורק אחרי "שלח"/"תענה לו" מפורש של יהודה.

## נתמך חגים (v3.52, בקשת רון במייל "אשמח" 14–16/9/2026 — "גם וגם")
- **מה זה:** סימון בכרטיס המקבל "נתמך חגים" (כללי) + אפשרות לצמצם לחגים מסוימים (ראש השנה/סוכות/חנוכה/פורים/פסח/שבועות; לא סומן אף חג = כל החגים). **`holidays.py`** (טהור): `HOLIDAYS`, `supports(rec, holiday="")`, `display`, `to_field`/`parse_list`, `from_text` (אקסל).
- **DB:** עמודות `recipients.holiday_support` (0/1) + `holidays` (TEXT, מופרד בפסיקים) — מיגרציה תוספתית, ב-`_RECIPIENT_FIELDS` ⇒ מסונכרן דרך `rec_upsert` הרגיל (בדיקה ב-`test_sync.py`). באקסל: עמודה "נתמך חגים" ב-`_FULL_FIELDS` (ייצוא טקסט/ייבוא `from_text`).
- **סינון (הכרעת רון: "בסינון של קבוע וכד'"):** ברשימת כל המקבלים — פריט "נתמך חגים" בקומבו העדיפות + קומבו חג (פעיל רק אז); אותו דבר ב-"＋ הוסף מקבל" בחלוקה. **חלוקת חג:** ב-`FilterCriteriaDialog` (מצב "סינון מותאם") קומבו "חלוקת חג — רק נתמכי חגים" ⇒ `criteria["holiday"]` (`""`/`"*"`/שם חג; `selection.HOLIDAY_KEY/HOLIDAY_ANY`, `holiday_filter`/`holiday_matches`/`holiday_label`) — **שער קשיח** ב-`db.get_filtered_list` לפני איזון הקהילות (לא-נתמך לא נכנס גם כהשלמה); הצ'יפ במסך החלוקה מציג "סינון מותאם · נתמכי פסח". מוצג גם בחיפוש מהיר (תג "חגים" + שורה) ובהדפסת כרטיס. בדיקות H1–H10 ב-`test_selection.py`; צילום+assert-ים `dev/_shot_holidays.py`.

## לשוניות (v3.39: 6 אזורים)
מוגדרות ב-`main.py` `_build_tabs`: **חלוקה** (מסך "חלוקה ורישום" ישירות) · **אנשים** (כל המקבלים · חיפוש מהיר · חלוקות קודמות) · **הודעות** (v2.76, `messages.py` — עלה ישיר; תג לא-נקראו דרך `_refresh_messages_badge`) · **צינתוקים** (v2.82, `tzintukim.py` — עלה ישיר) · **מיילים** (v3.39, `mails.py` — עלה ישיר) · **הגדרות**.

## מיילים למקבלים (v3.39, `tabs/mails.py` + `utils/mailer.py` + `utils/google_auth.py`)
- **מה זה:** מייל לקבוצת מקבלים / לבודדים / לכתובות חיצוניות מחשבון ה-Gmail של הקופה. הלשונית בשפת העיצוב של הצינתוקים (① למי שולחים · ② ההודעה · היסטוריה · סרגל ③). התכנון אושר דרך `הדמיית_מיילים.html`.
- **`utils/mailer.py`** (טהור): `render` (placeholders `{שם}`/`{שם פרטי}`/`{תאריך חלוקה}`/`{פרשה}`), `build_targets` (כתובת תקינה אחת לאדם, dedupe, סיבת-דילוג), `html_body`, `send_batch` (אחד-אחד דרך `email_utils.send_email`, רץ ב-thread).
- **`utils/google_auth.py`** (טהור, stdlib): OAuth 2.0 PKCE + loopback על 127.0.0.1, scope `gmail.send`+email בלבד. `connect()` פותח דפדפן, שומר `google_refresh_token`/`google_email` ב-settings **מסונכרנות** (הכרעת המשתמש 10/9/2026: חיבור אחד לשני המחשבים). `gmail_send_raw` שולח דרך Gmail API. **זיהוי-לקוח** (client_id/secret) מ-`utils/_secret.py` (`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`, נכנס רק ל-EXE) או `google_client.json` ליד ה-DB; **בלעדיו כפתור "התחבר עם Google" נעול** (`is_available`) והשליחה נופלת ל-SMTP עם סיסמת-אפליקציה (`email_utils.is_configured` = Google **או** SMTP; `sender_email`). **v3.41:** סדר חיפוש הזיהוי = `_secret.py` → settings **מסונכרנות** `google_client_id`/`google_client_secret` → `google_client.json`. (הכרעת המשתמש 14/9/2026: המובנה ב-EXE גובר על קובץ שנטען — לא לשנות.) המשתמש טוען את ה-JSON שגוגל מורידה דרך כפתור "טען קובץ זיהוי מגוגל…" בהגדרות (`google_auth.import_client_file` — דוחה קובץ web/לא-גוגל בעברית) — פעם אחת, מסתנכרן. ✔ אומת במסוף גוגל 15/9/2026: פרויקט "Manhal Haluka", לקוח "Manhal Haluka Desktop" (Desktop, 10/9/2026), Publishing status = In production (⇒ אין פקיעת refresh-token כל 7 ימים כמו במצב Testing). ה-loopback מטפל בבקשות עד שמגיע הקוד (favicon.ico לא מאבד את הקוד). **כותרת המייל** (`mailer.html_body`) = טבלה + `<img width='44' height='44'>` (Qt מתעלם מ-CSS height/flex → לוגו ענק בתצוגה המקדימה). מייל בדיקה (במיילים ובהגדרות) רץ ב-`_BgWorker`, לא חוסם. שורת "חיבורים" בהגדרות פוצלה (3 כרטיסים = 1423px > מסך).
- **DB:** `mail_campaigns` (guid, sent_at, subject, body, audience, sender, total/sent/failed, status, status_ts, report_json) + `mail_templates` (guid, name, subject, body, updated_at, deleted). ops סנכרון `mail_add`/`mail_update` (LWW לפי status_ts) / `mtpl_upsert` (LWW לפי updated_at, מחיקה רכה) + seed ב-snapshot; `reset_all_data` מנקה. `get_mails_for_recipient` — מוצג בכרטיס המקבל בחיפוש מהיר.
- **v3.42 (סקירת `/בדוק-מיילים` 14/9/2026):** (א) **תקלה כללית עוצרת את האצווה** — חריגה עם `fatal=True` (`GoogleAuthError(msg, fatal=…)`, `email_utils.MailFatalError`: אין אינטרנט / סיסמה / הרשאה / מכסה / נטפרי) אצל נמען אחד ⇒ `mailer.send_batch` מדלג על השאר (`skipped` עם סיבת התקלה, `mailer.stop_reason`), במקום 500 ניסיונות × timeout או 500 כניסות שגויות ל-Gmail; `_on_finished` מציג אזהרה עם הסיבה. כתובת-נמען שגויה נשארת כישלון מקומי. (ב) **SMTP:** `smtplib.SMTPException` יורשת מ-`OSError` ⇒ סיסמת-אפליקציה שגויה דווחה "אין חיבור לאינטרנט" — עכשיו נתפסת קודם ("שרת המייל דחה את הכניסה…"); `SMTPRecipientsRefused`/`SMTPDataError` (מכסה 5.4.5) בעברית. (ג) **התקדמות נשמרת מקומית** — `_on_progress` → `update_mail_campaign(..., sync=False)` אחרי כל נמען (UPDATE בלי רשומת סנכרון) ⇒ שליחה שנקטעה נסגרת כ"נקטע" **עם** מי שכבר קיבל ו"שלח שוב לנכשלים" עובד. (ד) `html.escape` לנושא בדיאלוג האישור/פירוט/תצוגה מקדימה. אינווריאנטים M29–M32.
- **v3.43 (סקירת `/בדוק-מיילים` 14/9/2026, סבב שני):** (א) **שליחה שנקטעה זוכרת גם את מי שלא נוסה** — `_send` כותב לדוח מראש שורת `pending` לכל יעד (`mailer.pending_rows`), `_on_progress` מחליף אותה, ו-`_close_stale_campaigns`/`_on_finished(Exception)` הופכים את הנותרים ל-`skipped` עם סיבה (`mailer.close_pending`) ⇒ "שלח שוב לנכשלים" אוסף אותם (קודם הדוח הכיל רק את מי שכבר טופל). (ב) כפתור "שלח שוב לנכשלים" לפי **תוכן הדוח** (`mailer.resendable`) ולא רק `failed>0` — עצירה ידנית/נקטע עם 0 כישלונות לא הציגו כפתור. (ג) "עצור" נשאר נעול בשליחה הבאה אחרי עצירה — `_send` פותח אותו. (ד) שליחה-חוזרת באמצע שליחה פעילה דרסה את הטיוטה — נחסמת עם הודעה. (ה) קובץ מצורף שנמחק מהדיסק → אזהרה במקום שליחה בשקט בלי הקובץ. (ו) **שם קובץ עברי** בצירוף: `add_header("Content-Disposition", …, filename=)` (RFC 2231) במקום השמה ישירה שקודדה את כל הכותרת ⇒ הנמען ראה "noname". אינווריאנטים M33–M36, `test_mail.py` §7.
- **v3.46 (14/9/2026, "4 באגים קריטיים" במסך המיילים):** (א) **סגירת התוכנה באמצע שליחה** — `MainWindow.closeEvent` → `mails_tab.sending_active()`/`confirm_close()` (ברירת-מחדל "לא"); אישור → `_abort_for_close`: עצירה אחרי המייל הנוכחי, `wait`+`processEvents`, הרשומה נסגרת מיד כ-`interrupted` (`close_pending`, מסונכרן). (ב) **מייל גדול ב-Gmail API** — בקשת JSON מוגבלת ל-10MB (base64²): מעל `JSON_RAW_LIMIT`=5MB → `GMAIL_UPLOAD_URL` (`uploadType=media`, `message/rfc822`); 413 = fatal; `send_email` עוצר > `MAX_MAIL_BYTES`=25MB לפני רשת; תקרת קובץ במסך `MAX_ATTACHMENT_BYTES`=18MB (היה 20MB = 27MB מקודד, נדחה אצל כולם). (ג) **שליחה-חוזרת לא דורסת טיוטה** — `_send(subject=, body=)` מפורשים, `_resend_failed` לא נוגע בשדות. (ד) **"התנתק" מגוגל ברקע** — `disconnect(revoke=False)` + `revoke_token` ב-`_BgWorker`. אינווריאנטים M45–M48, `test_mail.py` §10.
- **v3.55 (`/שפר-מסך מיילים`, 17/9/2026 — עיצוב+זרימה בלבד, ביצוע אוטונומי בלי הדמיה; כל ה-attributes/slots נשמרו):** (א) **כותרת בשתי שורות** — `chips_row` (`FlowLayout`): `chip_account` · `chip_with_mail` ("N מתוך M עם מייל") · `chip_ready` ("✓ N יקבלו מייל") · `chip_msg` ("ההודעה מוכנה"/"חסר נושא"/"חסר תוכן") · `chip_sending`; `btn_settings` = "חבר חשבון מייל בהגדרות" (רק כשלא מחובר). (ב) **תגי-שלב ✓** — `card_list`/`card_msg` + `_set_step_done`/`_CARD_DONE_QSS` (מיובאים מ-`tzintukim.py`), מתעדכנים ב-`_update_metrics`. (ג) **① :** כפתורי-המקור עם מספרים (`_MODE_TEXT`, ב-`_refresh_account`); `btn_show_bad` הוא עכשיו **כפתור-מסנן checkable** + `list_search` (מוצג מעל 8 שורות) → `_apply_filter` (מסתיר שורות, גובה לפי הגלויות); תא "מצב" של חריג "⚠ סיבה · תקן…" ושורה בענבר; `_on_cell_clicked` (עמודה 3 "✕ הסר" = **טקסט-תא, לא 500 כפתורים**; עמודה 2 של חריג → כרטיס) ו-`_open_row_card` בלחיצה כפולה (`RecipientDialog` → `update_recipient` → `refresh_all`); `lbl_list_empty` מצב-ריק מוסבר לכל מקור; `_show_bad`/`_NamesDialog` נשארו בקוד (לא מחווטים). (ד) **② :** "נמען אחר ◂" (`btn_preview_next`/`_preview_idx`) מדפדף בתצוגה המקדימה. (ה) **היסטוריה 7 עמודות** — מתי (יחסי, מלא ב-tooltip) · נושא (📎) · למי · **מצב** (`_HIST_STATUS` צבעוני) · "N מתוך M" · נכשלו ("—") · פעולות; ⚠ עמודת הפעולות = `MailsTab._HIST_ACT` (6, לא 5). (ו) **סרגל ③ בשתי שורות** — `_refresh_summary` (☑/☐ חשבון · נמענים · הודעה · 📎) ומתחתיו הכפתורים; "שלח עכשיו ל-N ✉". (ז) חלון האישור = טבלת-עובדות HTML **בתוך `QMessageBox.question`** (הבדיקות מחליפות אותו — לא להפוך לדיאלוג מותאם בלי לעדכן את `test_mail.py`). צילום+assert-ים: `dev/_shot_mails.py` (`mails_v355_*`: loaded/window/filter/empty/130).
- **v3.50 (בקשת המשתמש 15/9/2026 — עיצוב כמו ב-Gmail):** סרגל עיצוב מעל "תוכן ההודעה" (`_build_format_bar`: בטל/בצע-שוב, גודל קטן/רגיל/גדול/ענק, **B/I/U/S**, צבע טקסט, מרקר, יישור ימין/מרכז/שמאל, רשימת נקודות/ממוספרת, קישור, נקה עיצוב; Ctrl+B/I/U). **`utils/richtext.py`** (QtGui בלבד): `document_to_markup(doc)` — בלי עיצוב מחזיר **טקסט-רגיל זהה לקודם** (תאימות: תבניות/היסטוריה/המחשב השני), עם עיצוב מחזיר `mailer.RICH_PREFIX` (`<!--rich-->`) + HTML נקי (`<p>/<b>/<i>/<u>/<s>/<span style=color|background|font-size>/<a>/<ul>/<ol>`; לא ה-`toHtml` המנופח של Qt); `load_into(edit, body)` לטעינה חזרה (round-trip זהה). `mailer.is_rich`/`to_plain` (גרסת text/plain + אימות "ריקה" + היסטוריה); `render` מבריח ערכי-placeholder בגוף rich; `html_body` מכניס rich כמו-שהוא עם `_linkify_markup` (כתובות מחוץ ל-`<a>`). במסך: כל קריאת גוף דרך `_body_markup()` (לינט M53 אוסר `self.body.toPlainText()` לתוכן). הדבקה נשארת טקסט-רגיל (`setAcceptRichText(False)`) — העיצוב רק מהסרגל. ⚠ **יישור ב-Qt:** `AlignLeft` בלי `AlignAbsolute` = "מוביל" (=ימין בפסקה עברית) — הסרגל קובע `AlignRight|AlignAbsolute`/`AlignLeft|AlignAbsolute`, ו-`richtext.align_kind` מתרגם לשלושת המצבים. DB/סנכרון ללא שינוי (הגוף מחרוזת). `test_mail.py` §12; צילום `dev/_shot_mails.py` (מדגים הדגשה+צבע+רשימה).
- **v3.47 (סקירת `/בדוק-מיילים` 15/9/2026, סבב חמישי; כולל את v3.46 שלא שוחרר):** (א) **שליחה-חוזרת עם הקובץ המקורי** — עמודות `mail_campaigns.attachment`/`with_header` (מיגרציה, `mail_add`+seed+`_apply_mail_add`); `_send(attachment=, with_header=)`; `_resend_failed` לוקח מהרשומה (קודם: הקובץ שבטיוטה *כרגע*); קובץ חסר → שאלה; נכשלים בלי כתובת → הודעה. (ב) **SMTP: חיבור אחד לכל האצווה** — `email_utils.SmtpSession`/`mail_session()`, `send_email(session=)`, `send_batch` משתף; ניתוק חיבור ותיק → ניסיון נוסף (500 כניסות = "Too many login attempts"). (ג) **עדכון-תוכנה באמצע שליחה** — `quit()` לא עובר ב-`closeEvent`; `settings._mails_guard_ok` → `mails_tab.guard_update()` לפני ההורדה ולפני ההחלפה. (ד) "שמור כתבנית" בשם קיים → עדכון אותה תבנית אחרי שאלה. אינווריאנטים M49–M52, `test_mail.py` §11.
- **v3.45 (סקירת `/בדוק-מיילים` 14/9/2026, סבב רביעי — "המייל עצמו", אחרי שהמשתמש קיבל מייל-בדיקה בהצלחה):** (א) **גרסת טקסט-רגיל** — המבנה עכשיו `mixed › alternative › [text/plain, related › [html, לוגו]] › קובץ` (`send_email(text_body=)`; `html_to_text` כנפילה לזרימות שמעבירות רק HTML — מתנדבים/משוב). HTML-בלבד = ניקוד ספאם (MIME_HTML_ONLY) ו-snippet/קורא-מסך שבורים. (ב) **שם השולח** — `From: קופה של צדקה הר יונה <כתובת>` (`email_utils.SENDER_NAME`, `formataddr`), במקום כתובת חשופה; `Date`+`Message-ID` נכתבים בתוכנה (דומיין מכתובת השולח — `make_msgid` הדיפולטי מדליף את שם המחשב). (ג) **לוגו מוקטן** — `mails._logo_path` מחזיר עותק 88px (`logo_mail.png` ליד `DB_PATH`, מתחדש לפי mtime) במקום המקור (725px/34KB; לוגו-מותאם עלול להיות צילום של MB) ×500 נמענים. (ד) **קישורים לחיצים** — `mailer._linkify` הופך `http(s)://` ל-`<a>` אחרי `html.escape`. אינווריאנטים M40–M44, `test_mail.py` §9.
- **v3.44 (סקירת `/בדוק-מיילים` 14/9/2026, סבב שלישי):** (א) **`rec_id` בדוח הוא מזהה מקומי** — במחשב השני אותו מספר = אדם אחר: "שלח שוב לנכשלים" על שליחה שהגיעה מהמחשב השני שלח לאדם הלא-נכון, וכרטיס מקבל הציג מייל של מישהו אחר. עכשיו `get_mails_for_recipient(rec_id, guid)` ו-`_resend_failed` מתאימים לפי **guid** (`get_recipient_by_guid`); `rec_id` רק כשהשליחה של המחשב הזה. (ב) SMTP: ניתוק/timeout באמצע `sendmail` → עברית (היה חריגה גולמית). (ג) `_send_test` בודק שהקובץ המצורף קיים (כמו `_send`). אינווריאנטים M37–M39, `test_mail.py` §8.
- **בדיקות:** `test_mail.py` (placeholders, יעדים, DB, סנכרון 2 מחשבים, disconnect; v3.42: סעיף 6 — עצירה על תקלה כללית, הודעות SMTP, התקדמות מקומית, מסך Qt offscreen). צילום: `dev/_shot_mails.py` (assert-ים + `dev/_shots/mails_*.png` — לאמת דרך Gemini, לא לקרוא PNG לצ'אט).

## צינתוקים — ימות המשיח (v2.82, `utils/yemot.py` + `tabs/tzintukim.py`)
- **מה זה:** הודעה קולית אוטומטית ("יש לך חלוקה") לזכאי החלוקה הנוכחית, דרך ה-API הרשמי `call2all.co.il/ym/api` (RunCampaign/GetCampaignStatus). התכנון אושר דרך `הדמיית_צינתוקים.html` (קובץ תכנון היסטורי — כבר לא בשורש). **ידע-עומק על ימות — 11 סקילים** (01/09/2026, עודכן 03/09): סקיל הענן `yemot-hamashiach` (כללי + API חיצוני; לא ניתן לעריכה מקומית) · 3 סקילי-קו פרטיים מוחרגים מ-git (`yemot-line-state`/`knowledge`/`howto` — סודות חיים, לא להעתיק מהם ערכים לקבצים שנכנסים ל-git!) · 7 סקילי-תחום ציבוריים ב-`.claude/skills/`: `yemot-enter-id` (זיהוי), `yemot-api-module` (שלוחה→שרת חיצוני), `yemot-routing` (ניתוב/תורים), `yemot-audio-files` (הקלטות/השמעה/TTS/ymgr), `yemot-campaigns-ivr` (רשימות תפוצה מהטלפון), `yemot-caller-data` (נתונים אישיים למתקשר/סקרים), **`yemot-ecosystem`** (03/09/2026: מפת התוספים — מודולים רשמיים בתשלום, פתרונאי/MCP, ספריות קוד לפי שפה, מה לא קיים; לבדוק בו לפני שכותבים משהו שאולי כבר קיים). כשלומדים משהו חדש על ימות — לעדכן את הסקיל התחומי המתאים.
- **`utils/yemot.py`** — לקוח טהור (urllib, בלי Qt): `normalize_phone` (ולידציה ישראלית, ‎+972) · `pick_phones` (phone1/2/3 בלי כפולים) · `send_block_reason` (מחזיר תמיד `""` — **אין חסימת שעות מקומית**; החסימה היחידה = שבת/חג בשרת, שגיאה 104) · `ensure_template` (CreateTemplate חד-פעמי, נשמר ב-`yemot_template_id`) · `run_campaign(phones_dict)` · `get_campaign_status` (דליי: delivered=accepted/done/up/bridged/amd, failed=failed/no_answer/busy/canceled/error/blocked) · `upload_message_wav` (נתיב `tpl:{id}`, נפילה ל-`ivr2:` על שגיאות 107/109/110) · `run_test`. תחבורה ניתנת להזרקה (`_TRANSPORT`) לבדיקות. שגיאות ממופות לעברית (`_ERROR_HE`: 1/100/101/102/103/104/120).
- **settings** (מסונכרנות, כמו SMTP): `yemot_system`/`yemot_password`/`yemot_template_id`/`yemot_caller_id`/`yemot_test_phone`. פאנל בהגדרות: "צינתוקים (ימות המשיח)" — שמור + "בדוק חיבור" (GetSession + יתרה).
- **DB:** טבלת `tzintuk_campaigns` (guid, sent_at, dist_date, campaign_id, total/delivered/failed, status sending/done, status_ts, report_json). ops סנכרון `tz_add`/`tz_update` (LWW לפי status_ts) + seed ב-snapshot. `tzintuk_campaign_for_date(dist_date)` = שומר שליחה-כפולה חוצה-מחשבים (מזהיר לפני שליחה שנייה לאותה חלוקה).
- **`tabs/tzintukim.py`:** הרשימה = `group_tab._rows_data` בלי רזרבות (אותו מקור כמו המסך הראשי). חריגים (בלי מספר/שבור/מספר-כפול-במשפחה) מסומנים ולא נשלחים; קומבו לבחירת מספר למי שיש כמה. זרימה: בדיקה למספר המנהל → דיאלוג אישור (כפתור ננעל) → RunCampaign → `_PollWorker(QThread)` כל ~4 שנ' → כתיבת תוצאה ל-DB **רק בסיום** (לא להציף את יומן הסנכרון) → "שלח שוב לנכשלים". `_maybe_resume_tracking` ממשיך מעקב אחרי קמפיין 'sending' שנקטע (גם של המחשב השני). ⚠ קומבו בתא טבלה: `clearContents()` לפני repopulate (אחרת קומבו ישן "צף"), גובה שורה 48 + `setFixedHeight(40)`, עמודת טלפון Fixed 180 (ResizeToContents מתעלם מ-cell widgets).
- **בדיקות:** `test_tzintuk.py` (HTTP מדומה: נרמול, payload, שגיאות בעברית, סטטוס, LWW, סנכרון 2 מחשבים). צילום: `dev/_shot_tzintuk.py`.
### תכונות שנוספו מעל בסיס v2.82 (תמצית — פירוט מלא ב-`NEXT_TASK.md` + `git log` + סקיל `tzintuk-check`)
- **מנועי TTS** (`utils/tts.py`, טהור): Gemini (`gemini-2.5-flash-preview-tts`, מפתח `gemini_api_key`; מכסה קטנה → נפילה אוטומטית ל-Edge) ו-Edge (בלי מפתח/מכסה). **מאגר הקלטות** מקומי פר-מחשב (`%APPDATA%\ManhalHaluka\recordings\` + index.json). הקלטה במיקרופון (`RecordDialog`, QtMultimedia). ההעלאה לימות ממירה בשרת (`convertAudio=1`).
- **תזמון בצד השרת** (`ScheduleCampaign`) — עובד גם כשהמחשב כבוי; תבנית ייעודית לכל תזמון כדי שלא ידרסו זה את זה. **שיגור חכם** — כל מקבל בשעה שלו לפי היסטוריית מענה (`utils/call_history.py` מושך שנים אחורה מהשרת).
- **מצב שליחה** (נבחר בכל שליחה): "עם הודעה" או **קלאסי** (`RunTzintuk`, ~0.1 יח', אי אפשר לענות).
- **שרת מענה לחזרה-לצינתוק** (`utils/callback_server.py` + Cloudflare Worker): השרת /76 פרוס ועונה בחיוג ידני, אך ⚠ **הניתוב האוטומטי בשורש (Did_Go_To ‎048691834=/76‎) אינו קיים בקו נכון ל-10/9/2026** (אומת חי — `Did_Go_To.ini`=`033060315=/555` בלבד; ראה תיקון-אימות ב-`project-tzintuk-cloudflare-callback`). לכן מי שחוזר לקו *אמור* להגיע לשלוחה **/76** (`type=api` → השרת שלנו) ושומע לפי הבחירה בשליחה — ההקלטה ואז שאלת 1/2/3, ההקלטה בלבד, או כלום. כתובת/סוד/endpoints בזיכרון `project-tzintuk-cloudflare-callback`. **/77** = סקר אישור-הגעה ותיק (גיבוי עד שבוע חי מלא של /76). אישורי ההגעה משני המקורות ממוזגים (`merge_survey_answers`) ומוצגים כתגים ב"חלוקה ורישום", בהיסטוריה, בהדפסה ובאקסל.
- **בדיקות:** `test_tzintuk.py`, `test_tts.py`, `test_callback_server.py`; בודק לינט `python .claude/skills/tzintuk-check/scripts/check_tzintuk.py` (להריץ לפני ואחרי כל עבודה על `tzintukim.py`/`yemot.py`).
- **v3.48 (סקירת `/בדוק-צינתוקים` שתים-עשרה, 15/9/2026):** (א) **הרשומה נכתבת לפני הדחיפה לשרת המענה** — `_send` רשם את הקמפיין רק אחרי `_push_list` (לולאת-אירועים עד ~10 שנ') ⇒ סגירה/קריסה בחלון הזה = צינתוק אמיתי בלי רשומה; עכשיו `add_tzintuk_campaign`+seed קודם, דחיפה אחת משותפת לקלאסי/רגיל. (ב) **הדוח הסופי לא מאבד מספרים** — `yemot.merge_with_seed`: מספר שנזרע ושהתשובה הסופית של השרת לא מזכירה (ריקה/חלקית/לא-צולצל אחרי עצירה) נשאר `pending` (⇒ `stopped` ומוצע ב"שלח שוב"). (ג) **המעקב מכבד את הרשומה המסונכרנת** — `_on_tick` קורא את הרשומה בכל טיק: `stopping` מהמחשב השני ⇒ מאמץ `stopped_at` (קודם ב' סקר 60 דק' נעול); `done` מהמחשב השני ⇒ סגירה מיידית מהרשומה. (ד) **שגיאת-שרת קבועה במעקב** (YemotError code>0 ×5, מזהה לא מוכר) ⇒ `_PollWorker.permanent` + `_dead_polls` — בלי חידוש כל דקה שנעל כפתורים 40 שנ' מכל 100 לשבוע; רשומה בת 2+ שעות נסגרת עם הזריעה. `_after_campaign_end` חולץ מ-`_on_tick`. אינווריאנטים I47–I49, `test_tzintuk.py` §29.
- **v3.49 (סקירת `/בדוק-צינתוקים` שלוש-עשרה — מיקוד: החיבור לימות, 15/9/2026):** (א) **"בדוק חיבור" בהגדרות ברקע** — `_test_yemot_connection` הריץ 4 קריאות-רשת תחת `busy_cursor` (GetSession ×2, GetCustomerData, GetApprovedCallerIDs; retry + שרת תאום, 40 שנ' כל אחת) ⇒ ברשת מתה/נטפרי המסך קפא לדקות ("לא מגיב"); עכשיו `_bg`. גם בדיקת המספר המזוהה ב-`_save_yemot_settings` (`caller_id_problem`) ברקע — המזוהה החדש נשמר רק אחרי אישור הקו. (ב) **צ'יפ ימות בהגדרות** אומר "מחובר" רק אחרי GetSession אמיתי ברקע (`_probe_yemot_chip`/`_ym_probe_ok`; "בודק חיבור…"/"לא מגיב"), כמו הצ'יפ בצינתוקים מ-3.22. (ג) **כלי "בנה חיבור"** — סקיל `yemot-connect` + פקודה `/בנה-חיבור`: `scripts/connect.py` מאמת מקצה-לקצה (פרטי גישה → GetSession+יתרה → מזוהה מאושר → תבנית קיימת בקו → הקלטה → /76 → שרת המענה → ניתוב בשורש) עם דוח ✓/✗ ותרופה; `--set`/`--fix`/`--caller` להשלמה; **קריאה בלבד עם גדר חיוג בקוד** (`_guard`). אומת חי 15/9: הכל ירוק (יתרה ~26K; רק הניתוב בשורש חסר — ידני). אינווריאנט I50, `test_tzintuk.py` §30.
- **v3.53 (`/שפר-מסך צינתוקים`, 16/9/2026 — עיצוב+זרימה בלבד, כל ה-attributes/signals נשמרו; ההדמיה שאושרה: `dev/הדמיית_צינתוקים_v353.html`):** (א) **כותרת עם 4 צ'יפים חיים** — חיבור/יתרה (`lbl_ok`) · `chip_ready` ("N מוכנים לשליחה" / "עוד לא נטענה רשימה" / "אף נמען לא מסומן", מתעדכן ב-`_update_metrics`) · `chip_msg` ("הודעה מוכנה"/"אין עדיין הודעה", ב-`_refresh_recording_label`) · `chip_sched` ("תזמון ממתין"/"N תזמונים", ב-`_refresh_sched_banner`). (ב) **תגי-השלב הופכים ל-✓ ירוק** כשהשלב הושלם (`_set_step_done(card, num, done)`; `group_update._step_card` שומר `frame.badge`/`frame.title_lbl`, `_BADGE_QSS` משותף) והכרטיס מקבל מסגרת ירוקה (`_CARD_DONE_QSS`). (ג) **מצב ריק של ① = שלושה אריחים** (`_tile`; `load_frame` הוא עכשיו QWidget עם האריחים): "רשימת החלוקה הנוכחית" (ראשי, מציג "N משפחות ברשימה של השבוע" דרך `_refresh_week_tile`) · **"חלוקה קודמת…"** — חלון בחירה חדש `_PastBatchDialog` (`db.get_distribution_batches`, חיפוש, לחיצה כפולה) ⇒ `load_batch` — לא צריך יותר קליק ימני ב"חלוקות קודמות" · "רשימה עצמאית…". אחרי טעינה: כפתור **"החלף רשימה ▾"** (`btn_switch`, QMenu מ-`_source_menu`) ורצועת-מקור: `week_frame`/`lbl_week` לרשימת השבוע (השם דרך `_week_list_name` — קורא `group_tab.name_input`/`hebdate`, לא כותב) או `batch_frame` (חלוקה קודמת/עצמאית — **נשאר מוסתר לרשימת השבוע**, הבדיקות מסתמכות על זה). (ד) **טבלה:** עמודת "מצב" עם "● מוכן" / "⚠ סיבה" (שורת חריג כולה ענבר, tooltip) / "לא נשלח (לא מסומן)" — מתעדכנת חי בסימון (`_set_status_cell`, רק כשאין תוצאות של קמפיין); **צ'יפ "חריגים" הוא כפתור-מסנן** (`m_bad["frame"]` = QPushButton checkable, `_apply_bad_filter`, `_fit_table_height` מדלג על שורות מוסתרות; מתאפס ב-`_reset_results`); **לחיצה כפולה על שורה פותחת את כרטיס המקבל** (`_open_row_card` → `RecipientDialog` → `update_recipient` → `group_tab.refresh()` + `refresh_all`); מספרי טלפון בתא עם LRM (סדר קריאה נכון ב-RTL). (ה) **② "ההודעה הנוכחית"** — פאנל `now_frame` (ירוק; ענבר `state="none"` כשאין הודעה/חיבור) עם `lbl_rec_name` (שם), `lbl_rec` (הועלתה מתי/מאיפה/איך — יחסי + מלא), `btn_test` (עכשיו attribute) ו-**"▶ השמע במחשב"** (`btn_play_local`, רק כשיש עותק במאגר של המחשב הזה — `_local_recording_path` לפי שם); כפתור ראשי אחד **"🎙 הודעה חדשה ▾"** (`btn_new_msg`, QMenu מ-`_new_message_menu`: מיקרופון/מטקסט/קובץ — אותם slots) + "מאגר הקלטות"; "פרסם בשלוחה 1…" בכותרת הכרטיס. (ו) **היסטוריה: 7 עמודות** — מתי (יחסי: "לפני 4 ימים"/"מחר 09:00", המלא ב-tooltip; `_when_label`) · הצינתוק · **מצב** (`_HIST_STATUS`; "⛔ נעצר באמצע" לרשומת done עם רשומות `stopped` — `_was_stopped`, ממוזכר במטמון `_hist_text_cache` כטריפל) · נשלחו/הצליחו/נכשלו (ממורכזים; "—" לתזמון) · תשובות בסקר ("✓ 2 מגיע · ✗ 1 לא · ? 0 לא יודע · 5 לא הגיבו"; `_answers_text` — הבדיקות בודקות "—"/"N לא הגיבו"); גובה עד 420. ⚠ **מספרי העמודות השתנו** (סקר = 6, לא 4) — `_open_history_details` עדיין קורא guid מעמודה 0. (ז) **③ שורת מוכנות** — `lbl_summary` rich-text (`_refresh_summary` מ-`self._metrics`+`_rec_state`/`_rec_name`): "☑ 7 נמענים · ☑ הודעה «שם» · 1 חריגים לא יישלחו" / "☐ טען רשימת נמענים (למעלה, שלב 1)" / "השליחה רצה — הכפתורים נעולים"; הכפתור הראשי "שלח עכשיו ל-N". (ח) **תיקון לוגי אחד (I51):** שומר השליחה-הכפולה מזהה רשומה חדשה לפי **מזהה** (`_date_campaign_guids` לפני הדיאלוג → `_changed_meanwhile(..., seen)`), לא לפי "החדשה ביותר לפי שעה" — מחשב שני עם שעון מפגר הסתיר שליחה מקבילה (התגלה כפליק של `test_tzintuk` ביום רביעי); `test_tzintuk.py` §31. צילום+assert-ים: `dev/_shot_tzintuk.py` (ריק/טעון/שליחה/עמוד מלא/130%; `grab_ok` — ניסיון חוזר כשה-grab שחור).

- **v3.54 (`/שפר-מסך צינתוקים` סבב שני, 16/9/2026 — "בצע הכל", בלי הדמיה; עיצוב+זרימה, כל ה-attributes/signals נשמרו):** (א) **כותרת בשתי שורות** — שורת כותרת, ומתחתיה `chips_row` עם כל החיוויים ב-`utils.ui.FlowLayout` (חדש, RTL-aware, עוטף לשורה נוספת במקום לזרוק צ'יפ ב-130%). (ב) **סרגל ③ בשתי שורות** — שורת המוכנות (`lbl_summary`) לבדה, ומתחתיה `btn_later` "שלח מאוחר יותר ▾" (QMenu מ-`_later_menu`: במועד קבוע / לכל אחד בשעה שלו — אותם slots) + `btn_send`; `btn_sched`/`btn_smart` נשארים attributes **מוסתרים** (`_update_metrics` מנהל גם את `btn_later`). (ג) **חלון האישור `_SendModeDialog(summary, parent, facts=, warnings=)`** — `facts` (מ-`_send_facts`: חלוקה/משפחות/מספרים/חריגים/ההודעה) בכרטיס-גריד + אזהרות ענבר, ושני אריחים `rb_classic`/`rb_voice` (לחיצה על האריח מסמנת; `_paint_tiles`); **קלאסי כברירת מחדל** (יישור לקוד עם ההכרעה מ-1/9 — קודם הקוד סימן "עם הודעה"). בלי `facts` — הטקסט הישן (בדיקות). `dialog_text()` = כל הטקסט שהמפעיל רואה (הבדיקות §25 קוראות דרכו). (ד) **חריג עם תרופה בתא** — `_set_status_cell` כותב "⚠ אין מספר טלפון · תקן…" ו-`cellClicked`→`_on_cell_clicked`→`_open_row_card` (לחיצה אחת; ⚠ ווידג'ט-תא עם layout נחתך לפני הצגה — מלכודת ב-`pitfalls.md`). (ה) **היסטוריה:** "היסטוריה מהשרת" יצא מהכותרת והפך לקישור "רענן עכשיו" (`btn_hist_sync`) ליד `lbl_hist_sync`; בכותרת נשארו פירוט/רענן תשובות/ייצוא. (ו) **טקסטים:** הרמז מתחת לטבלה משפט אחד (השאר ב-tooltip), תיבת "בתפריט הודעה חדשה" הוסרה, "יתרה: N יחידות" (+tooltip עלות), צ'יפ "⚠ N חריגים". צילום+assert-ים: `dev/_shot_tzintuk.py` (6 צילומים `tzintuk_v354_*`, כולל `confirm`; הזריעה של "מחר" לפי שעון ישראל).
### הכרעות ואילוצים קבועים — צינתוקים
- **הכרעות המשתמש:** שליחה **ידנית בלבד** — **אסור** פרסום אוטומטי בשלוחה 1 בשום זרימת שליחה/תזמון (פרסום = כפתור ידני עם דיאלוג אזהרה). **אין חסימת שעות מקומית** (`send_block_reason`→`""`; החסימה היחידה = שבת/חג בשרת, שגיאה 104). **כל המספרים של כל מקבל מצולצלים** (בלי בחירת-מספר; כפילויות בין-שורות מנוקות). **צינתוק קלאסי = ברירת המחדל** (עלות).
- **אל-תעשה:** התוכנה **לא כותבת לשורש הקו** — שינוי בשורש רק ידני, עם גיבוי+diff, כשהתוכנה סגורה בשני המחשבים. **לא לנסות `f-msg` כשאין הקלטה בשלוחה** (קובץ חסר = "אין מענה"). המנגנון הישן של **שלוחה 78 נעול** (`yemot.CALLBACK_ENABLED=False`) — לא להחזיר. `UpdateTemplate` דוחה `yemotContext=REPEAT` — אין להשתמש בו.
- **נותר לאמת בשטח:** נתיב העלאת הקלטה `tpl:`→נפילה ל-`ivr2:` בשגיאות 107/109/110; שקלט הקשות הסקר (77) מתעדכן נכון בדוחות השרת לאורך שבוע חי מלא.

## תיקוני v3.31–v3.38 (תמצית מ-git log; פירוט מלא ב-`git log` ו-`NEXT_TASK.md`)
- **v3.31** — תדירות חדשה **"תלת-שבועי"** (כל 3 שבועות, יום רביעי): בחירה בכרטיס המקבל, וסינון בחלון "הוסף מקבל" (במצב "בלי קבועים" תלת-שבועי וחודשי מוצגים; רק שבועי/דו-שבועי מוסתרים).
- **v3.32** — סמל חדש לתוכנה (סלסלה ירוקה עם לב ושיבולת) בחלון / taskbar / מסך פתיחה / EXE.
- **v3.33–v3.37 — שרת המענה לחזרה-לצינתוק** (Cloudflare Worker + `utils/callback_server.py`). ⚠ **ראה האזהרה במקטע הצינתוקים למעלה:** שלוחת /76 עונה, אך הניתוב בשורש (‎048691834=/76‎) אינו קיים בקו נכון ל-10/9/2026 — הפיצ'ר אינו פעיל אוטומטית עד חיווט ידני של 2 השורות בשורש.
  - **v3.33** — שלב 1 (כבוי כברירת מחדל): בלוק "שרת המענה" בהגדרות (כתובת/סוד/מתג/"בדוק חיבור"); שליחה מיידית מעדכנת את השרת ברשימת השבוע כשהמתג דלוק.
  - **v3.34** — בדיקת שלוחת /76 לפני כל שליחה/תזמון (הקו משותף → השלוחה עלולה להימחק/להשתנות); חלון "תקן ושלח" שכותב מחדש רק את /76 (לא נוגע בשאר הקו).
  - **v3.35** — הכנה בקוד: מי שחוזר עובר ישר לשאלה 1/2/3; לא-זכאי/כבר-ענה → תפריט רגיל בשקט; כל תקלה בשרת → תפריט רגיל (לא "אין מענה"). ⚠ חיבור 2 השורות בשורש נותר ידני.
  - **v3.36** — הרשימה נשלחת לשרת בכל דרך שיגור (תזמון/שיגור-חכם, רשימה נפרדת לכל חלוקה, ביטול תזמון מנקה, שליחה-חוזרת לא נוגעת); "בדוק את שלוחת המענה (76)" בהגדרות, בדיקות ברקע.
  - **v3.37** — בחירה בכל שליחה מה ישמע החוזר (הקלטה+שאלת 1/2/3 / הקלטה בלבד / כלום); ההקלטה מועתקת ל-/76 בכל העלאה.
- **v3.38** — **אישור-הגעה שנרשם כבר לא נעלם בטעות בזמן תקלת-רשת** (נוגע ישירות לנתיב הסקר/אישור-הגעה של הצינתוקים).

## תיקוני v3.29 (דוח 7/9/2026 — 2 פריטים, חלון "＋ הוסף מקבל")
- **ברירת המחדל = כולם (#bsv8c):** `_ManualAddDialog` — אפשרות סינון חדשה "כולם" (כל הרשימה, עם ובלי עדיפות) והיא ברירת המחדל; "כל העדיפויות" (קבוע/ראשונה/שנייה בלבד) נשארה כאפשרות שנייה.
- **מצב 'בלי קבועים' (#lsyyv):** `_add_manual` מעביר `hide_frequencies=_ManualAddDialog.HIDE_FREQ_NO_REGULARS` (שבועי/דו-שבועי) — קבועים בתדירויות אלו לא מוצגים כלל בחלון; חודשיים וכל השאר כן. אימות: `dev/_shot_manualadd.py` (assert-ים + צילום). **v3.30:** שורת הספירה אומרת כמה קבועים הוסתרו (`_hidden_count`) — המשתמש הסיק מ-"503 מתוך 503" שהקבועים עדיין שם.

## תיקוני v2.99 (דוח תקלות 2/9/2026 — 17 פריטים)
- **מצבי ניקוד = מוצרים + רזרבה בלבד (#c9k0m, הכרעת המשתמש):** `selection.limit_to_products(ordered, portions, reserve, keep_ids, reserve_ids)` — ב-`all`/`scored` הרשימה נחתכת ל-N הגבוהים בניקוד (N=מוצרים) + רזרבה לפי מספר הרזרבה; השאר לא מוצגים ולא מודפסים. הוספות ידניות/חד-פעמיים נשארים main, בחירות-רזרבה נשארות רזרבה. מוצרים=0 ⇒ בלי הגבלה. שינוי מוצרים/רזרבה ⇒ `refresh()`. **מנגנון "מובילים" (`_leader_ids`) הוסר** — מיותר כשהרשימה עצמה חתוכה. בדיקות L1–L3 ב-`test_selection.py`.
- **שם אוטומטי בכל מצב (#doluy/#8vztv):** `_effective_dist_name` לא מחזיר יותר None במצב מיוחד — תמיד ממלא "חלוקת פרשת…". `_special_active` נשאר רק לקיפול האזור המתקדם.
- **נפשות (#xjzw9):** בשלב הכנה סופרים את כל שורות ה-main; בשלב רישום — רק המסומנים.
- **עמודת שם (#2mx6a):** ResizeToContents לשם, Stretch לעמודת ההערות (במקום Stretch על השם שנמעך).
- **הוסף מקבל (#dy39u):** קומבו התדירות פעיל רק כשמסנן העדיפות = "קבוע" (ובלי "חד-פעמי"/"ללא תדירות").
- **עיצוב:** מתג הכנה/רישום מבריק (QLinearGradient, #t4m5t) · "חלוקה חדשה" עם גליף אחד וגרדיאנט (#kdld2) · `CommunityAssignDialog` 900×640 עם קומבו 300px גדול (#npk5n).
- **הדפסה:** לוגו 75px וכותרות fs+1 (#dyunn) · צבע כותרות הטבלה inline לבן (`_th`, #nne2s — `.chk`/`.num` דרסו את הלבן) · עמודת "אישר הגעה" ✓ מתוך `yemot.confirmed_phones(date)` דרך `_get_export_rows` → `_confirmed` (#82rfd).
- **שחזור בלחיצה אחת (#69pen):** `settings.BackupListDialog` — כל הגיבויים (תיקיית ברירת מחדל + תיקייה שנבחרה) לפי סוג/תאריך/גודל עם כפתור "שחזר" בכל שורה; `_restore_path(path)` = הזרימה המשותפת (אישור → גיבוי-ביטחון → שחזור).
- **איפוס לא מוחק סיסמת ימות (#rliqc):** הסיבה — `restart_from_peer` הריץ מחדש את ה-seed המקורי של המחשב השני שכלל הגדרות ישנות/ריקות, והגדרות שנכתבו לפני הפעלת הסנכרון לא היו מוטבעות ב-`setting_ts`. עכשיו `restart_from_peer` מטביע את כל ההגדרות המקומיות כ"ידועות עכשיו" ⇒ רק שינוי של המחשב השני *אחרי* האיפוס דורס. בדיקה ב-`test_sync.py`.
- ⚠ מלכודת צילום: תצוגת PNG של QTextDocument עלולה להציג כותרת טבלה בלי רקע — הפיקסלים בקובץ נכונים; לאמת עם ספירת פיקסלים (`th_real.png`). #67rdi (ייצוא היסטוריית צינתוקים) כבר מומש ב-2.93; #0zn42 — סיסמת הקו מספיקה, אין צורך במפתח API.

## תיקוני v2.98 (סקירת באגים מלאה 2/9/2026 — 11 ממצאים תוקנו; שאר הממצאים ב-NEXT_TASK.md)
- **צ'אט:** `messages.refresh` לא מוחק יותר את תווית "אין הודעות" (`_empty`) — מחיקת ההודעה האחרונה קרסה (RuntimeError בתוך slot של הסנכרון).
- **הערות בטבלת החלוקה:** `group_update._row_notes` (dict לפי id) — נקלט ב-`_on_item_changed` (עמודת `_COL_NOTES`), מוזרק ב-`_populate`, ומשמש **בלעדית** ב-`_get_checked_recipients`/`_get_export_rows` (גם לשורות מוסתרות; הערות הכרטיס לא זולגות יותר להיסטוריה). מתאפס בשמירה/ניקוי.
- **חד-פעמיים חוצי-מחשבים:** `weekly_extra_ids`/`weekly_reserve_ids` נשמרים כ-**guid** (לא id מקומי); `_load_extras` פותר guid→id מקומי, מקבל גם מספרים ישנים, ונקרא מחדש בכל `refresh()` כדי לקלוט בחירה מהמחשב השני.

- **הכרעות המשתמש (2/9/2026) — לא באגים, לא לתקן:** (א) `undo_incoming` על "מקבל שנוסף" מוחק בכוח כולל היסטוריה בשני המחשבים — **מכוון**, שליטה מלאה של המנהל. (ב) משקלי הניקוד (`need_w_*`, `set_need_weights`) **לא מסונכרנים** — מכוונים בעת הכנת חלוקה במחשב אחד, זו התנהגות רצויה. (ג) "אפס נתונים" **לא** מסונכרן — משמש לניקוי נתוני בדיקה מקומיים.
- **איפוס = "התחל מחדש מהמחשב השני" (הכרעת המשתמש 2/9/2026):** `settings._reset_data` כשהסנכרון פעיל ← `db.reset_all_data()` (מקומי בלבד) ואז `sync.restart_from_peer()`: מאפס `applied`/`offsets` (היומנים של המחשבים האחרים מוחזרים מההתחלה — נותן מיד את כל מה שהם סידו/כתבו), מוחק את ה-outbox המקומי (שינויים שטרם נשלחו תיארו נתונים שנמחקו), ורושם op `snap_req`. המחשב השני ב-`pull_changes` עונה ל-`snap_req` טרי (עד `SNAP_REQ_MAX_AGE_H`=48 שעות) ב-`snapshot(include_settings=False)` — **נתונים בלבד, ההגדרות המקומיות נשמרות**. ⚠ נתוני בדיקה שנוצרו כשהסנכרון היה פעיל כבר עברו למחשב השני ויחזרו עם הסנאפשוט — לבדוק עם סנכרון כבוי.
- **ייבוא תוצאות מתנדב = רישום החלוקה** (ממצא 10): `_after_round_recorded()` (חולץ מ-`_save`) רץ גם אחרי `_on_inbox_found`/`_import_volunteer_results` — חד-פעמיים מנוקים (לא נרשמים שוב), סימונים/הערות מתאפסים, חזרה לשלב הכנה. `_mark_received_in_table` (סימונים לתצוגה בלבד ששיקרו למונה) הוסר.

- **רשימה עצמאית — כמה מספרים בשורה:** `yemot.find_phones` נכתב מחדש כטוקנייזר (במקום regex חמדן שבלע שני מספרים לאחד): מספר = הרצף **הקצר ביותר** של עד 4 טוקנים מספריים שמנרמל לטלפון תקין; טוקן מספרי שנשאר עם 4+ ספרות = פסול, קצר יותר ("דירה 5") = חלק מהשם; שורה **בלי אף טלפון** מדווחת את המספרים הקצרים שלה כפסולים (כדי שהמפעיל יראה שהשורה דולגה).
- **צינתוק קלאסי — נפילה למסלול הישן רק כשהשרת סירב:** `run_campaign(classic=True)` מפיל ל-RunCampaign קצר-צלצול רק על שגיאת-שרת שאינה ‎-1 (רשת — השרת אולי כבר חייג, צלצול כפול), 103 (אין יחידות), 104 (שבת) — אלו עולות למפעיל. כשנפל: `res["classic_fallback"]=True` והודעת הסיום ב-`tzintukim._send` אומרת שהצלצול ניתן למענה (במקום "אי אפשר לענות").
- **"בחר הכל" לא מסמן רזרבה** (`_check_all` מדלג על `_reserve`/`_reserve_ids`) — כלל 3: רזרבה נרשמת רק אחד-אחד.

- **קריאות לשרת ימות ברקע:** `TzintukimTab._run_blocking(fn, text)` — מריץ קריאה חוסמת ב-`_TaskWorker` מאחורי `QProgressDialog` מודאלי (בלי ביטול) עם `QEventLoop` מקומי, ומחזיר תוצאה / מעלה מחדש את החריגה כאילו רצה inline. כל 7 האתרים (העלאה, בדיקה, שליחה, פרסום בשלוחה 1, שליחה חוזרת, תזמון, ביטול תזמון) עברו אליו — המסך לא מקפיא ("לא מגיב") גם ב-2.5 דקות של ניסיונות חיבור. ⚠ ה-fn רץ ב-thread אחר: אסור לגעת בו בווידג'טים; DB/‏settings בסדר (חיבור sqlite לכל קריאה).
- **ייצוא PDF:** שם קובץ עם תאריך+שעה (כמו אקסל, לא דורס), ואחרי הרינדור בדיקה שהקובץ לא ריק — קובץ 0 בייט נמחק ועולה `RuntimeError` בעברית (במקום לפתוח PDF ריק בלי הודעה).

## תיקוני v2.97 (סקירת באגים של מסך הצינתוקים, 1/9/2026)
- **שליחה מיידית נחסמת כשיש תזמון ממתין** (`_send` → `_pending_sched`): שליחה עם `store_list=True` מנקה את רשימת התבנית, והתזמון היה מחייג לרשימה החדשה. הכלל: קודם "בטל תזמון", אחר כך שליחה.
- **`status_ts` של רשומת צינתוק = עכשיו, לעולם לא `sent_at`** (`add_tzintuk_campaign`, `_apply_tz_add`): לתזמון `sent_at` הוא מועד עתידי, וחותמת עתידית גרמה ל-LWW במחשב השני לדחות ביטול/סטטוס. תאימות: `_apply_tz_update` מתייחס לחותמת מקומית עתידית כמיושנת. הסיד (`seed`) שולח את התוצאות (delivered/failed/report_json) בתוך `tz_add` עצמו — ה-`tz_update` שלו נושא אותה חותמת ונדחה.
- **רשימה עצמאית:** `yemot.find_phones` (regex — מספרים עם רווחים, כמה בשורה, השאר = שם) + `yemot.normalize_phone_loose` (תא אקסל מספרי: בלי 0 מוביל / עם ‎.0). `normalize_phone` הרגיל לא שונה (כרטיסי מקבלים).
- `get_campaign_status` מנרמל את המספר שהשרת מחזיר (‎972…→‎05…) כדי שההתאמה לשורות תעבוד.
- תזמון שרץ: `sent_at` נשאר המועד המתוכנן (לא זמן הגילוי); אין campaignId ⇒ נסגר כ-`done` במקום "בתהליך" לנצח.
- `_last_entries` שומר את תוצאות-הטבלה ומשוחזר ב-`_populate` (רענון/סנכרון לא מוחק "✓ אישר הגעה"); מתאפס בהחלפת רשימה. `QTimer.singleShot(6s)` בבנייה מריץ `_maybe_resume_tracking` גם בלי לבקר בלשונית.

## תכונות v2.96 (בקשת המשתמש 1/9/2026 — סטטוס צינתוק קלאסי + התראות הורדה)
- **מעקב חזרה-לשיחה אחרי צינתוק קלאסי:** לימות **אין API של יומן שיחות עבר** (נבדק חי 1/9: GetCallHistory/GetCalls/HistoryFile לא קיימים); `GetIncomingCalls` מחזיר רק שיחות **חיות**. לכן `yemot.CallbackTracker` (טהור, transport מוזרק) + `_CallbackWorker(QThread)` ב-`tzintukim.py` עוקבים חי אחרי הקו אחרי שליחת קלאסי: מי מהנמענים התקשר חזרה (status `callback` — "📞 חזר לשיחה") ומי הגיע לשלוחת האישור והקיש 7 ("✓ אישר הגעה"). רצועת מעקב עם מונים + כפתורי "⏱ הארך מעקב ב-30 דק'" ו"סיום מעקב"; המעקב חי רק כשהתוכנה פתוחה (`_maybe_resume_tracking` מחדש אחרי הפעלה); התוצאות נשמרות ל-`report_json` (וגם בייצוא האקסל: "חזר לשיחה ושמע"). צילום: `dev/_shot_classic_track.py`.
- **v3.40 — התראות הורדה רק על אנשים אחרים:** כל מחשב שמוריד עדכון רושם setting מסונכרן `self_dl_<device>` = {tag: count} (`updater.record_self_download`, נקרא ב-`settings._on_downloaded` לפני ההחלפה); המחשב המנהל מחסר אותם מהמונים של גיטהאב (`updater.self_download_totals`/`others_downloads`) ומדווח גדילה רק אם היא נשארה **גם בבדיקה הבאה** (10 דק', `dl_others_prev`/`dl_others_known` ב-sync_state) — כי ההורדה של המחשב השני מופיעה בגיטהאב לפני שה-setting שלו מסתנכרן. הבאלון מציג את המונה **לכל גרסה** (לא סכום כל הגרסאות — סכום ירד כש-releases ישנים יצאו מהעמוד; `per_page=100`).
- **התראות הורדה ועדכון-עמית (רק למחשב המנהל):** `updater.fetch_download_stats()` (GitHub releases API, מוני הורדות לכל tag) + `main._setup_download_watch` — באלון כשמונה-ההורדות עולה; `sync.local_get/local_set` (ערכים פר-מחשב ב-sync_state.json) עם דגל `notify_downloads`; צ'קבוקס "קבל התראות במחשב זה…" בפאנל עדכון-תוכנה בהגדרות (הופעל במחשב של המשתמש). בנוסף כל מחשב מפרסם את גרסתו כ-setting מסונכרן ⇒ באלון "המחשב 'X' עודכן לגרסה" (`_check_peer_updates`).
- ⚠ **מלכודת נטפרי שנלמדה בדם (3 צ'אטים נחסמו):** ראו "מלכודות ידועות" — אסור לקרוא את צילומי `dev/shots_classic_track/*.png` לתוך צ'אט.

## תכונות v2.94 (שיחת רון 1/9/2026 — צינתוקים)
- **תיקון חיוג חוזר אחרי בדיקה:** שורש התקלה מהשטח — `run_test` לא שמר את המספר ברשימת התבנית, ולכן מי שחייג חזרה לקו לא שמע כלום והתפריט ניתב אותו ל"ניתוב לטלפון" (תפוס). עכשיו `yemot.add_template_entry` (UploadPhoneList `updateType=UPDATE`, בלי Clear — לא דורס רשימה של תזמון ממתין) מוסיף את מספר-הבדיקה לרשימה.
- **צינתוק קלאסי (בחירה בכל שליחה):** `_SendModeDialog` מחליף את חלון האישור — רדיו "עם הודעה"/"קלאסי". קלאסי = `run_campaign(classic=True)`. **מ-v2.95: קודם `yemot.run_tzintuk` — ה-API הייעודי `RunTzintuk`** (צינתוק אמיתי: השיחה לעולם לא מחוברת, **אי אפשר לענות** — דרישת המשתמש 1/9; ~0.1 יחידה למספר; `phones` מופרד בפסיקים + `TzintukTimeOut=16`; **אסור לשלוח `sayInfoOnAnswer`** — הוא זה שמאפשר מענה; ⚠ טרם אומת בשטח שהקו תומך ושפורמט phones inline מתקבל). אם השרת מסרב (אין שירות צינתוקים בקו) — נפילה אוטומטית למימוש הישן: תבנית שנייה (`ensure_classic_template`, setting `yemot_classic_template_id`; מאמצת את התבנית היתומה 1430693, `originateTimeout=8`+`maxDialAttempts=1`), ההקלטה מועתקת אליה בצד השרת. בשני המסלולים הרשימה נשמרת **בתבנית הראשית** (ההתקשרות-חוזרת מושמעת דרך `campaign_message_to_play` מיקום 17). אין מעקב-מענה בקלאסי — נרשם `done` מיד עם קידומת "צינתוק קלאסי —".
- **מעקב חי לשיחת בדיקה (v2.95):** `_TestStatusDialog` (ב-`tzintukim.py`, רוכב על `_PollWorker`) נפתח אחרי "שלח בדיקה" ומציג בזמן אמת מצלצל/נענתה/אושר-בהקשת-7/לא-נענה/תפוס — כי מספר הבדיקה לא תמיד ביד המפעיל. עוצר מעקב רק ב-`finished` (הקשת 7 נרשמת בסוף השיחה). צילום: `dev/_shot_teststatus.py`.
- **רשימה עצמאית (`_FreeListDialog`):** הדבקת מספרים או טעינת אקסל, מנותק לחלוטין מהחלוקות; ספירה חיה של תקינים/פסולים, dedupe. `self._free`; `dist_date=""` → שומר-הכפילות לא חל. האקסל נשפך לתיבת הטקסט כך שהמפעיל רואה בעיניים מה ישוגר.
- **טבלת הנמענים בגובה מלא** (`_fit_table_height` — הדף גולל, לא חלון פנימי של ~4 שורות) + כפתורי "סמן את כולם"/"נקה סימון מכולם" (`_set_all_checked`).
- באגים שרון דיווח ולא היו תקולים בקוד הנוכחי: רשימה שנטענת אוטומטית (תוקן ב-v2.93), ריבועי +/− (אייקוני qt-material שנעלמו עם מחיקת `_MEI` — תוקן ב-v2.91).
## תכונות v2.93 (דוח 1/9/2026)
- **שם חלוקה עברי אוטומטי (#o2eft):** `utils/hebdate.py` (טהור, pyluach — בspec כ-hiddenimport) — `auto_weekly_name(date)` ⇒ "חלוקת פרשת נצבים — כ׳ אלול תשפ״ו" (שבוע בלי פרשה ⇒ "חלוקה שבועית — <תאריך עברי>"; pyluach חסר ⇒ "" והקורא נופל לפורמט הלועזי). ב-`group_update`: `_is_auto_name` מזהה את כל הפורמטים (`_AUTO_NAME_PREFIXES` = "חלוקה שבועית"/"חלוקת פרשת") — היסטוריית שמות וניקוי-אחרי-שמירה עוברים דרכו.
- **מובילים אוטומטיים במצב ניקוד (#z7xq1):** כפתור "סמן מובילים" הוסר; `_recompute_leaders` מסמן `_leader_ids` = top-N לפי ניקוד (N=מוצרים זמינים, בלי רזרבות) בכל refresh ובשינוי המוצרים (עדכון חי), תג "⭐ מוביל בצורך" ירוק ב-`_row_style`. אין שינוי בכללי מי-מקבל — סימון תצוגה בלבד.
- **צינתוקים — רשימה בטעינה מפורשת (#ifc70):** `_list_loaded` + `load_frame` עם כפתור "הצג את רשימת החלוקה הנוכחית"; הרשימה/מדדים מוסתרים עד טעינה, ההיסטוריה גלויה תמיד. `_clear_batch` חוזר למצב לא-טעון. `load_batch` (חלוקה קודמת) נחשב טעון.
- **ייצוא היסטוריית צינתוקים לאקסל (#67rdi):** `excel_utils.export_tzintuk_history_to_excel` — גיליון "סיכום" + גיליון "פירוט" פר-מספר עם "אישר הגעה"(7)/"קיבל את ההודעה"/"לא נענה"; שם מושלם מ-name_by_phone (המקבלים) כשהדוח בלי שם. כפתור בכותרת ההיסטוריה.
- **תיקון קליק ימני בחלוקות קודמות (#9hgvi):** `selectRow` לא באמת בחר (מלכודת) ⇒ התפריט לא נפתח; הוחלף ב-`setCurrentCell`. אומת ב-`dev/_probe_ctxmenu.py`.
- **כפתור "חלוקה חדשה" צהוב בוהק (#hwnwz)** עם ⟳; חלון אישור השליחה בצינתוקים מציג גם את שם החלוקה (#qmqjx).

## תכונות v2.72–v2.77 (דוח 27/08/2026)
- **פיצול שם (v2.75, #aka27):** עמודות `first_name`/`last_name` (מיגרציה + back-fill). **`full_name` = "משפחה פרטי"** נשאר מזהה סמכותי; `db.split_full_name`/`join_name`/`_apply_name_fields` **כולם משפחה-קודם** — אל תשנה סדר זה (שובר זהות/סנכרון). טופס=2 שדות, טבלת מקבלים=2 עמודות.
- **הודעות צוות (v2.76, #ya4f7):** טבלאות `messages`+`message_reads`; ops סנכרון `msg_add`/`msg_read`. ✓✓ נקרא, זמן יחסי, שעון ישראל, רקע מותאם (`chat_bg_path`, מוחרג מסנכרון).
- **מחיקת הודעה (v2.78):** רק הכותב מוחק הודעה שלו (כפתור "מחק" על בועה משלי) → `db.delete_message(guid)` + op סנכרון `msg_delete` (`_apply_msg_delete`), נמחקת גם אצל שאר הצוות (LWW-פשוט, כמו rec/dist delete). חלון עדכון (`UpdateOfferDialog`): פירוט שינויים מלא ב-`QScrollArea` (בלי קיצוץ 400 תו) + חלון ניתן להגדלה (size grip, גובה מוגבל למסך).
- **גודל טקסט באחוזים (v2.80, #x2yn5):** setting `ui_font_scale` (80–150, פר-מחשב, מוחרג מסנכרון) דרך `db.get_ui_font_percent` (מיגרציה מ-`ui_font_size` הישן). `styles.apply_app_theme(app, percent)` = הדרך היחידה להחיל ערכה: qt-material + EXTRA_QSS עם regex שמכפיל כל `font-size: Npx` + `app.setFont`. קריאה חוזרת בזמן ריצה = עדכון מיידי לכל המסכים (settings: spinbox עם debounce). אל תחזיר `app.setFont` בודד — רוב הטקסט נשלט ע"י QSS.
- **הודעות למפתח בתוך התוכנה (v2.80, #ce6a0):** טבלת `feedback` (guid, status open/done, status_ts) + ops סנכרון `fb_add`/`fb_status` (+seed ב-snapshot). `feedback.save_feedback` כותב גם ל-DB; `feedback.import_legacy_jsonl` מייבא חד-פעמית feedback.jsonl ישן (guid דטרמיניסטי uuid5, דגל `feedback_legacy_imported` פר-מחשב). UI: כפתור "הודעות שנשלחו (N פתוחות)" בהגדרות → `FeedbackInboxDialog` (העתקה, סימון טופל, LWW). ⚠ בטבלת הדיאלוג `selectRow` לא עובד — `setCurrentCell` כן.
- **מגבלות תצוגה (v2.80, #o8l0v):** אין מחיקת נתונים אוטומטית בשום מקום. `get_distributions`/`get_distribution_batches` בלי LIMIT כברירת מחדל (ייצוא היסטוריה מלא!); `get_messages` שומר את ה-400 **החדשות** (תת-שאילתה DESC→ASC — אל תחזיר ASC+LIMIT, זה מציג את הישנות).
- **בקרת מנהל (v2.77, #5rhe9):** `is_manager` פר-מחשב (sync_state) + קוד מנהל מסונכרן (`manager_code_hash` PBKDF2). `pull_changes` מקליט שינויים נכנסים ל-`sync_incoming` (before/after) **רק במחשב מנהל** (`_RECORD_INCOMING`); `db.undo_incoming` מחזיר מצב קודם ומסנכרן חזרה (LWW). UI: מקטע "מחשב מנהל" בהגדרות + `ManagerLogDialog`.
- **סנכרון (v2.73):** `detect_drive_folders` מזהה "האחסון שלי" (עברית) + `ensure_drive_running` מפעיל Drive ברקע; רציף כל 10 שנ' (בלי כפתור "סנכרן עכשיו").
- **תיקיות ייצוא (v2.74, #5e1jc):** `excel_utils.export_dir(kind)` — dist/recipients/volunteers, settings `export_dir_<kind>` (מוחרג מסנכרון), ברירת מחדל הורדות. אזור "אנשים" הוא `QTabWidget` פנימי עם property `subtabs` (עיצוב פיל-בהיר ב-`styles.py`); כל לשונית-תוכן ("עלה") שומרת את ה-attribute וה-objectName `tab_<key>` שלה — הסיור/בדיקות/צילומים מאתרים לפי מפתח, לא לפי מיקום. ניווט בין לשוניות מקוננות: `MainWindow.navigate_to_tab(widget)`; העלה המוצג: `_current_leaf()`; רשימת כל העלים: `_leaf_tabs`. רענון-עצל לפי `_needs_refresh` על העלים (מטופל ב-`_show_leaf`). `tab_order` נשמר רק ברמת 3 האזורים.
- **`group_update.py` — "חלוקה ורישום"** (לשונית פתיחה, מיזוג weekly+group). **אין יותר עורך מוצרים** — במקומו שדות `products_spin` ("מוצרים זמינים") + `reserve_spin` ("רזרבה") שהם מקור-האמת (settings `available_products`/`reserve_count`) + הערה כללית (נשמרת פעם אחת ברמת האצווה, `what_dist`=""). חיווי חי `lbl_leftover`. **פישוט v2.58 (הכרעות המשתמש 08/2026):** חד-פעמיים הם חלק מכל חלוקה → מוצרים/רזרבה/חיווי נשארים גלויים תמיד; שני אזורים מתקפלים `_CollapsibleCard` סגורים כברירת מחדל — "מצבי חלוקה מתקדמים" (`adv_section`: mode_combo+סמן מובילים+הגדר סינון; נפתח אוטומטית בבנייה אם mode≠schedule או סינון פעיל, פתיחה-בלבד) ו"שליחה למתנדב במייל" (`vol_section`; הקליטה האוטומטית רצה ברקע גם כשסגור). הדפסה = הפעולה הראשית. **בחירת חד-פעמיים במסך:** כפתור "בחר חד-פעמיים" (`btn_pick_onetime`) → `OneTimePickerDialog` (תצוגה בלבד על הליבה הקיימת: `get_one_time_list`+`compute_suggested_n`+`assign_roles`; מומלצים מסומנים מראש) → `add_one_time_picks`. **שם אוטומטי:** `_effective_dist_name` — שם ריק בשבועי רגיל ← "חלוקה שבועית DD/MM/YYYY" (נכתב לשדה, לא נדחף להיסטוריה — `_push_name_history`, מנוקה אחרי שמירה); במצב מיוחד (`_special_active`) שם עדיין חובה. **שער `_one_time_gate_ok`:** נשאר בדיקה-טהורה (הבדיקות קוראות לו ישירות — אסור לפתוח בו דיאלוג); העוטף `_ensure_one_time_picks` (בו משתמשים הדפסה/PDF/אקסל) פותח את חלון הבחירה במקום ניווט ללשונית "חד פעמי". חיפוש מהיר מסנן את רשימת השבוע (`db.filter_recipients`). סימון ב-`_checked_ids` (שורד סינון) + `_seen_ids`. **שני שלבים מפורשים (v2.60, `_stage`/`_set_stage`/`_apply_stage`):** שלב "הכנה" בלי עמודת צ'קבוקסים בכלל; כפתור כתום "רישום מי קיבל" עובר לשלב רישום שמתחיל ריק — מסמנים מי הגיע. **בשלב הרישום, מי שלא סומן נרשם מפורשות כ"לא הגיע" (`received=0`)** — שינוי כלל v2.60 (פותר #yjcny), מזין את התראות אי-ההגעה (סף רצוף מתכוונן בהגדרות, ברירת מחדל 3; תג ברשימה + בכרטיס מקבל). מצב "לפי ניקוד" = `db.get_regulars_scored` (קבועים בלבד; חד-פעמיים רק ב-`_extra_ids`). רישום → `bulk_add_distributions(..., dist_name, general_note)`. כפתור **"שמור PDF"** (`_export_pdf`) לצד "הדפסה לחלוקה" — `print_view.export_distribution_pdf` מרנדר את אותו HTML ל-PDF בתיקיית הורדות ופותח אוטומטית (`os.startfile`), בלי מדפסת. combos עם `_style_completer` — מתקין `QListView` מעוצב כ-popup דרך `comp.setPopup()` (הקריאה ל-`popup()` בזמן build מחזירה None). זרימת מתנדבים: `_dispatch_volunteer_email`/`_import_volunteer_results`.
  **עיצוב v3.03 (2/9/2026) — עוצב מחדש בשפת ההגדרות/צינתוקים (עיצוב בלבד, אף לוגיקה/signal/attribute לא השתנה):** `_build_ui` נכתב מחדש כזרימה אחת בתוך `QScrollArea` יחיד (`self._scroll_body`) + סרגל תחתון קבוע מחוצה לו. מבנה: כותרת "חלוקה ורישום" + subtitle + **צ'יפי-סטטוס חיים** (`chip_stage` הכנה/רישום, `chip_mode` מצב לא-רגיל; `_refresh_header_chips` נקרא מ-`_apply_stage`/`_update_mode_controls`) · **כרטיס ① "פרטי החלוקה"** (`_step_card("1",…)`) עם **פאנל הענבר הבולט של "מוצרים זמינים"** (spin 24px/900, מסגרת ענבר — דרישת המשתמש: השדה הכי בולט במסך) + רזרבה + leftover + הערה · שני ה-`_CollapsibleCard` (`adv_section`/`vol_section`) · **כרטיס ② "רשימת המקבלים"** (`_step_card("2",…)`; כפתורי `btn_add_manual`/`btn_export_prep` בכותרת הכרטיס `h2`, שורת כלים חיפוש+בחר/בטל-הכל, מוני-צ'יפ `lbl_checked`/`lbl_total`/`lbl_souls`, `stage_banner`, והטבלה בגובה מלא דרך `_resize_table_height`) · **סרגל תחתון ③** (`_step_badge("3")`+`stage_toggle`+`lbl_summary`+שמור+הדפסה-hero+PDF+חלוקה-חדשה). **עזרי-UI משותפים** `_step_badge`/`_step_card`/`_metric`/`_set_metric` חולצו לרמת-מודול ב-`group_update.py` (+`_CHIP_AMBER`/`_LBL`) ומיובאים ע"י `tzintukim.py` (הוסרה הכפילות). צילום: `dev/_shot_group_v303.py` (מזריע מקבלים, מצלם שני שלבים + עמוד מלא). ⚠ עוד-`_make_card`/`_field` נשארים בקובץ (בשימוש פנימי בדיאלוגים) — לא למחוק.
- **`recipients.py`** — CRUD מקבלים + דיאלוג, סינון עדיפות. **v3.56 (`/שפר-מסך הוסף מקבל`, 17/9/2026):** `RecipientDialog` — שדה "אזור" ירד מהטופס (הכרעת יהודה; `f_area` נשאר מוסתר, הערך הקיים נשמר דרך `_orig_area` — קודם אזור שלא ברשימה נמחק בשקט בעריכה), "פרטים בסיסיים" נכנס בלי גלילה (זוגות בשורה דרך `_pair`: שם/משפחה, טלפון/כתובת, נפשות/סטטוס, חלוקה אחרונה/הבאה), מייל שגוי מסומן באדום בלי לחסום שמירה (`mailer.valid_email`), שגיאת-שמירה קופצת ללשונית הראשונה (`self.tabs`), "שמור" = Enter. **v3.57:** שורת "חלוקה אחרונה / הבאה ✦" ירדה גם בעריכה (הכרעת יהודה — "לא הבנתי מה התועלת"; `f_last_dist`/`f_next_dist` נשארים מוסתרים, הערכים נשמרים ב-`get_data`, ובדיקת-התאריכים ב-`_collect_errors` הוסרה כי אין איך לתקן שדה מוסתר). **v3.58 (סבב עיצוב):** כותרת חיה בראש החלון (`lbl_title` = שם המקבל / "מקבל חדש", `lbl_sub`, צ'יפ `chip_state` = עדיפות/"לא בחלוקה" · סטטוס; `_refresh_header`), לשוניות-פיל (`subtabs`), `btn_add_phone` ("＋ עוד מספר") יושב **בתוך שורת הטלפון** ונשלט ב-`setVisible` (לא `setRowVisible` — אין לו שורה משלו), בלי placeholder-ים שחזרו על התווית, הערות 84–110px, חלון 700 רוחב, "שמור" רחב פי 2 מ"ביטול". צילום+assert-ים: `dev/_shot_recipient_dialog.py`. כפתור "בדיקת כפילויות" → `review.py` כדיאלוג. **שדה "תדירות" מוצג רק כשהעדיפות = "קבוע"** (`_toggle_frequency_row`, #j6czs) עם 3 אפשרויות בלבד (שבועי/דו-שבועי/חודשי, בלי ריק #fw5s2). התדירות ה**נשמרת** נגזרת מהעדיפות (`_effective_frequency`): קבוע→הלוח שנבחר · ראשונה/שנייה→`חד-פעמי` · ללא/בירור→`""`.
- **בחירת חד-פעמיים (הוסר `one_time.py` ב-v2.60 — הלוגיקה ב-`OneTimePickerDialog` בתוך `group_update.py`)** — חלוקת עדיפות לחד-פעמיים: **מציג רק מועמדים** (עדיפות 3/2, `in_distribution`). "מוצרים זמינים"/"רזרבה" נקראים מ-`group_update` (settings `available_products`/`reserve_count`); מומלצים מסומנים מראש דרך `get_one_time_list`+`compute_suggested_n`+`assign_roles`. לחיצה על שם → פירוט ניקוד (`utils.ui.show_score_breakdown`, RTL). `compute_suggested_n` צורך קבועים קודם.
- **`search.py`** — "חיפוש מהיר" מאוחד. רשימה → כרטיס-פרופיל HTML + היסטוריה. `HighlightDelegate`, `BadgeDelegate`, ייצוא, `print_recipient_card`.
- **ייצוא מקבל בודד לאקסל (v2.79):** `excel_utils.export_single_recipient_to_excel(rec, history=None)` — קובץ Excel יחיד למקבל אחד: גיליון "פרטי מקבל" **אופקי** (שורת כותרות "מס'"+`_FULL_FIELDS` כעמודות ושורת נתונים אחת, זהה בפריסה לאקסל המקבלים הרגיל; טלפונים דרך `_write_phone_cell`) + גיליון "היסטוריית חלוקות" (סימון אדום ל"לא קיבל"). שם קובץ=שם המקבל+חותם זמן, ל-`export_dir("recipients")`. שתי כניסות: `search._export_card` (כפתור "ייצוא לאקסל" ליד "הדפס כרטיס", פעיל עם בחירה) ו-`recipients._export_one` (פריט בתפריט ימני-קליק).
- **`distributions.py`** — "חלוקות": שורה לכל אצווה מ-`dist_batches`. לחיצה כפולה → `BatchDetailsDialog`, מחיקה `db.delete_batch`.
- **`settings.py`** — סיסמה, עדכון, משקלי ניקוד, גיבויים, איפוס, פאנל "מייל למתנדבים" (SMTP). **v3.01 — עוצב מחדש בקו של הצינתוקים:** רקע `_BG`, כותרת + צ'יפי-סטטוס חיים (`chip_version`/`chip_sync`/`chip_yemot`/`chip_mail`, `_refresh_header_chips`), כותרות-מקטע (`_section`) ושורות של שני כרטיסים לבנים (`_card`/`_place` — גובה שווה בשורה, תוכן נצמד למעלה): כללי ומראה · נתונים וגיבוי · חיבורים · עבודה משני מחשבים · חישוב החלוקה · התוכנה · אזור מסוכן (כרטיס אדום ברוחב מלא). עזרים מודולריים: `_desc`/`_hint`/`_flabel`/`_btn`/`_btn_row`/`_form`/`_form_row`. כל שמות ה-attributes של הלוגיקה נשמרו. צילום: `dev/_shot_settings_v301.py`.

## זרימת המתנדבים (`utils/email_utils.py` + `utils/excel_utils.py`)
- **שליחה:** `export_volunteer_checklist_to_excel` — אקסל מינימלי, עמודה מוסתרת `id` להתאמה, גיליון `meta` מוסתר, עמודת "הגיע?" עם DataValidation "כן,לא" וברירת מחדל "כן" (המתנדב רק מבטל מי שלא הגיע). `send_email` — multipart + לוגו inline (cid), STARTTLS.
- **ייבוא:** `import_volunteer_checklist` מתאים לפי id המוסתר (fallback לשם), `received = not (ריק/"לא")` — ייבוא ישיר להיסטוריה.
- SMTP דורש **App Password של Gmail** (myaccount.google.com/apppasswords, מצריך 2FA) שמזין המשתמש בהגדרות.
- כל ייצואי האקסל → תיקיית **הורדות** (`_downloads_dir`). לניקוי תא בטסט: `ws.cell(r,c).value = None`.

## עדיפות וניקוד צורך — `selection.py` (מקור-אמת יחיד)
כל שאלת "מי מקבל, באיזה סדר, ולמה" עוברת דרך `selection.py` (טהור, בלי DB/Qt). הלשוניות ו-`database.py` רק צורכים אותו. ארבעת הכללים העסקיים (הכריע המפעיל, 07/2026) — כל אחד ומקומו בקוד:
1. **עדיפות מול ניקוד** — עדיפות (3/2) היא **שער כניסה** בלבד (`is_one_time_candidate`). *איך* ממיינים תלוי במצב:
   - **חלוקת עדיפות חד-פעמי** (`selection.rank_one_time_priority`, דרך `db.get_one_time_list`): עדיפות **גוברת** — כל ראשונה(3) לפני כל שנייה(2), ניקוד ממיין רק **בתוך** דרגה.
   - **מצב "קבועים לפי ניקוד"** (`selection.rank_by_need`, דרך `db.get_scored_all`/`get_regulars_scored`): עדיפות רק שער; הסדר לפי **ניקוד בלבד** — שנייה עם ניקוד גבוה יכולה להקדים ראשונה נמוכה.
2. **קבועים מול חד-פעמיים** — בורר "מצב חלוקה לקבועים" עם 5 מצבים: **`all` (ברירת מחדל מ-v2.66, #0e037)** — כל המקבלים הפעילים (קבועים + מועמדי חד-פעמי 3/2) יחד ברשימה אחת לפי ניקוד (`db.get_scored_all`); אין שלב "בחר חד-פעמיים" נפרד. `all` נחשב **מצב רגיל** (`_special_active` מחזיר False עבורו כמו `schedule`: שם-אוטומטי שבועי, אזור מתקדם מקופל) / `schedule` (קבועים קודם לפי לוח; `compute_suggested_n` מנכה מהמלאי **רק את הקבועים שבתור השבוע** = `len(get_weekly_list())`, לא כל הקבועים — דו-שבועי/חודשי שלא בתור לא גוזל מנה) / `scored` (רק הקבועים לפי ניקוד) / `none` / **`filter`** (סינון מותאם, ראה למטה). `compute_suggested_n` מחזיר `(max(0,מוצרים), 0)` בכל מצב שאינו `schedule` — כל המנות מזינות את הרשימה. **שער בחירת חד-פעמיים (`_one_time_gate_ok`) פעיל רק ב-`schedule`** (#y0rrd: בשאר המצבים הרשימה כבר נבחרת אחרת → אין חלון "בחר חד-פעמיים" בהדפסה/ייצוא).
   - **מצב `filter` (סינון רחב, #vq4fx)** — מתעלם מעדיפות/תדירות לחלוטין: בוחר מ**כל המקבלים הפעילים** את מי שעונה על ספי-מספר מתכווננים (`selection.FILTER_FIELDS`: `children_total`/`income`/`per_soul`, כל אחד עם min/max). `selection.filter_by_criteria`/`matches_criteria` (AND בין שדות; **חסר נתון בשדה מסונן = לא נכלל**, שער-קשיח, לא כמו ניקוד). `selection.to_number` מפרסר שדות-טקסט ("4,500 ₪"→4500). קריטריונים נשמרים ב-settings `dist_filter_criteria` (JSON) דרך `db.get/set_filter_criteria`; הרשימה דרך `db.get_filtered_list` (מדורגת לפי ניקוד). ב-`group_update`: כפתור "הגדר סינון" (מוצג רק במצב זה) → `FilterCriteriaDialog`; כניסה למצב בלי קריטריונים פותחת אותו אוטומטית. **אזור אינו קריטריון** (הכרעת המשתמש 08/2026).
5. **איזון בין קהילות (#lejmr, v2.61)** — במצב `filter`, ברירת מחדל **דלוקה** (`criteria["balance_communities"]`, כיבוי ב-`FilterCriteriaDialog`). "קהילה" = `representative` (שם נציג). `selection.balance_by_community`: מכסה לכל קהילה **יחסית לגודלה הכולל** (`community_quotas`, largest-remainder, תקרה=גודל הקהילה); בתוך קהילה — עומדי-הסינון לפי ניקוד קודם, ואם המכסה גדולה מהעומדים משלימים מ**אנשי אותה קהילה לפי קרבה לסינון** — הכי קרוב לעמוד בסף ראשון (`selection.criteria_gap` = פער יחסי משוקלל, `gap÷סף`; חוסר-נתון=1.0; ניקוד רק שובר-שוויון), מסומנים `_balance_fill` (הכרעת המשתמש 08/2026, שינוי מ"לפי ניקוד"). קבוע שנשאב להשלמה מסומן `_balance_regular` ומודגש ברשימה (ענבר בהיר + תג "קבוע", `is_regular` — כולל חודשי). אחוזים ידניים לקהילה: settings `community_quotas` (JSON) דרך `db.get/set_community_quotas`, סכום>100 מנורמל. חסרי-נציג = קבוצת "ללא קהילה"; `selection.infer_communities`+`db.apply_inferred_representatives` משלימים נציג לפי **רוב בית הכנסת** ורושמים בכרטיס עם `representative_auto=1` (מוצג ב-`RecipientDialog`, נמחק בעריכה ידנית). דיאלוגים: `CommunityAssignDialog` (group_update), `CommunityQuotasDialog` (settings). `db.get_filtered_list` קורא `available_products` — בלי מוצרים אין איזון.
3. **רזרבה = רשימת המתנה** — `selection.assign_roles` מסמן `_role` (main/reserve/out). רזרבה **לא נרשמת** כברירת מחדל (`recorded_by_default`→False): מגיעה ללא-סימון ב-`group_update` (`refresh` מדלג על `_reserve_ids`), אבל **כן מודפסת** כמקטע נפרד (`_get_export_rows` מוסיף רזרבה גם ללא סימון). נרשמת רק אם המפעיל מפעיל אותה במקום מי שלא הגיע.
4. **חוסר נתונים → תחתית התור** — ב-`scoring.annotate_need_scores` גורם **חסר תורם 0 נק'** (לא ניטרלי 0.5), גם ל-"high" וגם ל-"low". נתון חסר רק פוגע, לעולם לא מזכה — משפחה עם נתונים חסרים שוקעת לתחתית.
- קוד מקור (מאקסל): 4=קבוע→שבועי · 3=ראשונה · 2=שנייה (3/2 = `PRIORITY_TIERS`) · 1/0/בירור/**ריק = נתונים בלבד**. נשמר כ-`priority`+`priority_raw`. **המספרים לא מוצגים** — רק תוויות עברית (`PRIORITY_BADGES` ב-`utils/ui.py`).
- `scoring.annotate_need_scores`: 0–100 = שקלול 6 גורמים (`NEED_FACTORS`), משקלים מתכווננים (`need_w_*`, סכום 100). `_score_parts` לפירוט בלחיצה. נורמליזציה **ליניארית יחסית לרשימה** (min/max) — הכרעת המשתמש: חצי-הוצאה=חצי-ניקוד, וחריגים אמיתיים נספרים (המחיר: חריג/טעות-קלדה אחד משטח גורם לכולם — מודע, לא מתקנים בנוסחה).
- **שובר-שוויון בדירוג** (`selection.rank_by_need`/`rank_one_time_priority`): ניקוד → **ותק (הכי הרבה המתנה, `days_since`)** → שם. הכרעת המשתמש: שוויון-ניקוד מוכרע למי שממתין הכי הרבה, לא לפי אלף-בית.
- **ותק (`db.recency_days`)**: ימים מאז חלוקה אחרונה; מי שמעולם לא קיבל — מאז **הרישום** (`start_date`→`created_at`), לא אפוק-2000. תאריך עתידי מתאפס ל-0.
- `db.delete_distribution(id)` — מחיקת רישום חלוקה בודד (מלשונית "חיפוש מהיר"), לרישומים ישנים ללא `batch_id`.
- הדפסת רזרבה: `utils/print_view.py` מפצל לפי `_reserve` ל"חלוקה"/"רזרבה — לפי סדר עדיפות".

## כללים עסקיים קריטיים
- **נפשות בייבוא** = "ילדים בבית" + מבוגרים במשק הבית. מבוגרים נגזר ממצב אישי (`_adults_in_household` ב-`utils/excel_utils.py`): חד-הורי (גרוש/אלמן/רווק/פרוד — עם נרמול אותיות סופיות) = 1, נשוי/ריק/לא ידוע = 2.
- חד-פעמיים לא ברשימה השבועית. שם-חלוקה חובה לפני הדפסה/שליחה (למשל "חלוקת פסח").
- `calculate_next_dist`: שבועי=+7, דו-שבועי=+14, תלת-שבועי=+21, **חודשי=+28 (כל 4 שבועות — הכרעת יהודה 17/9/2026; קודם יצא 5)** — תמיד רביעי. **v3.59 (`/בודק-באגים`):** חלוקה שתאריכה חמישי–שבת נספרת למחזור של הרביעי שלפניה (רישום באיחור דחה דו-שבועי ל-3 שבועות); `bulk_add_distributions` לא מחזיר `last_distribution` אחורה ברישום חלוקה ישנה בדיעבד (תואם ל-`_recompute_recipient_dates` שבמחשב השני; תאריך עתידי שגוי >7 ימים כן נדרס); `get_weekly_list` מתקן-עצמית `next_distribution` שנשמר מאוחר מהכלל. בדיקות: `test_deep.py` A + D2. `get_weekly_list` מבטיח שרביעי הקרוב נכלל בכל יום בשבוע. **קבוע שטרם קיבל** (`last_distribution` ריק) = חייב **מיד** ברביעי הקרוב (כולל היום אם היום רביעי), לא שבוע קדימה — אחרת קבוע שנוסף ביום החלוקה נעלם מהרשימה (bug #pv59q). `get_weekly_list` גם מתקן-עצמית `next_distribution` שנשמר שבוע קדימה.
- **מקור אמת אחד לתאריכי החלוקה בכרטיס (v3.60, `/בודק-באגים` 17/9/2026):** `recipients.last_distribution`/`next_distribution` הם **נגזרים בלבד** — נכתבים רק ב-`db._recompute_recipient_dates`: last = המאוחר מבין (היסטוריה `received=1`, `last_dist_base`), next = `calculate_next_dist(last, frequency)`. `last_dist_base` (עמודה חדשה, מסונכרנת עם הכרטיס) = תאריך שהגיע מחוץ להיסטוריה (אקסל/הוקלד; `_adopt_last_base`, back-fill חד-פעמי). **אסור לכתוב את שני התאריכים ישירות:** `update_recipient` זורק `next_distribution` והופך `last_distribution` ששונה לבסיס; `sync._apply_rec_upsert` לא מחיל אותם מכרטיס נכנס (כרטיס ישן מהמחשב השני החזיר תאריכים אחורה ⇒ קבוע קיבל פעמיים); `RecipientDialog.get_data` לא מחזיר אותם; שינוי תדירות מחשב מחדש את התור; מחיקת חלוקה חוזרת לתאריך הבסיס ולא לריק. בדיקות: `test_deep.py` S1–S4, `test_sync.py` (סוף), `test_fixes.py` #4.
- מחיקת מקבל עם היסטוריה → `ValueError` (שנה סטטוס / מחיקה כפויה).
- הדפסה (`utils/print_view.py`): טבלה RTL (עמודות בסדר הפוך), לוגו + "קופה של צדקה הר יונה", ☐ בעמודת ביצוע, כיווץ גופן 11→6 למינימום דפים, ערכים דרך `html.escape`.

## עדכון אוטומטי (`utils/updater.py`) ושחרור
- repo `JHGJHJCD/distribution-manager`. בודק `releases/latest`, מוריד `Manhal-Haluka.exe`, מאמת שלמות, מחליף EXE רץ ומפעיל מחדש. בדיקה בהפעלה + כפתור בהגדרות.
- **v2.81 (מ-v2.90: כל 2 דקות):** בדיקה חוזרת **כל 2 דקות** בזמן ריצה (`MainWindow._run_update_check`; `UPDATE_CHECK_MS`) — עדכון שמתגלה תוך כדי ריצה מציג **התראת Windows** (QSystemTrayIcon balloon, `_notify_update`, פעם אחת לגרסה) שלחיצה עליה פותחת את חלון העדכון (`_offer_pending_update`). חלון העדכון (`UpdateOfferDialog`): פירוט השינויים מרונדר דרך `utils.ui._release_notes_html` — שורת-commit יחידה "כותרת: א, ב, ג" הופכת לכותרת+נקודות, שורות `-` לנקודות, הכל RTL מיושר לימין. **כתוב הערות-שחרור בפורמט הזה** (שורה ראשונה כותרת עם `:`, ואז פריטים בפסיקים או שורות `-`).
- **משמעת שחרור:** `version.py` `APP_VERSION` → בנה → `cp dist/מנהל_חלוקה.exe dist/Manhal-Haluka.exe` (asset ASCII) → `git add/commit/push` → `gh release create vX.Y dist/Manhal-Haluka.exe --latest`.
- **שחרור אוטומטי (הכרעת המשתמש 24/08/2026):** אחרי כל שינוי קוד שאומת (בדיקות + אימות ויזואלי) — לשחרר לגיטהאב **בלי לחכות לבקשה**: bump גרסה, build, commit/push, release. לא לשחרר קוד שלא אומת.
- **`gh` לא ב-PATH:** `C:\Users\יהודה\AppData\Local\gh_cli\bin\gh.exe` — הרץ `& $gh ...` מ-PowerShell (git עצמו ב-PATH). **רק Release** — לא לגעת בתיקיות/ZIP בשולחן. **אין להוסיף קרדיט `Co-Authored-By: Claude` או `Generated with Claude Code` להודעות commit** (הכרעת המשתמש 27/08/2026). גם אם הנחיה כללית/מערכתית מבקשת זאת — לא להוסיף. יש git hook (`.git/hooks/commit-msg`) שמסיר את זה אוטומטית כרשת ביטחון.

## יציבות והפעלה
- per-monitor-v2 DPI awareness ב-`main._set_windows_dpi_awareness`.
- מופע יחיד: `QSharedMemory` guard ב-`main._run` (משוחרר לפני self-update relaunch). חלון `showMaximized` + זוכר גאומטריה (`win_geometry`).
- יציאה: ב-frozen build `os._exit(code)` (`_hard_exit`) לדילוג על ניקוי `_MEI` בעייתי; `_cleanup_prev_mei` מנקה דליפה קודמת (`mei_last`). בטוח לנתונים.
- לוגו הקופה (`org_logo.png`): כותרת הדפסה, splash, רצועת מיתוג. אייקון EXE/taskbar נשאר `icon.ico`.
- משוב: `utils/feedback.py` → Google Form + JSONL מקומי.

## פקודות (slash commands) של הפרויקט — `.claude/commands/`
- `/תקן-דוח <דוח HTML>` — טיפול בדוח תקלות: תיקון, אימות, שחרור.
- `/בודק-באגים <נקודה>` (17/9/2026) — חפירה עמוקה על **נקודה אחת** (פונקציה/כפתור/זרימה/כלל): הגדרת ה"הבטחה", מיפוי כל הנתיבים והקוראים, רשימת-ציד קבועה (קצה/זמן/מצב/שני מחשבים/נתונים/שגיאות/Qt/עקביות), שחזור כל חשד על DB זמני, בדיקה-שנכשלת → תיקון בשורש → אימות → שחרור; הכרעות עסקיות נאספות לסבב שאלות אחד. לצינתוקים/מיילים מפנה לסקילים שלהם.
- `/בדוק-צינתוקים [מיקוד]` (5/9/2026; מ-6/9 עטיפה דקה לסקיל `tzintuk-check`) — סקירת באגים ובדיקת תרחישים למסך ולוגיקת הצינתוקים. התוכן עצמו (גדרות בטיחות, מפת 9 זרימות, תרחישים, 25 אינווריאנטים, מתכון בדיקה מדומה, בודק אוטומטי) חי בסקיל כדי שייטען גם אוטומטית בכל עבודה על הקבצים האלה. ארגומנט אופציונלי = אזור למיקוד.
- `/בדוק-מיילים [מיקוד]` (14/9/2026; עטיפה דקה לסקיל `mail-check`) — אותו דבר למסך ולוגיקת המיילים (`tabs/mails.py`, `utils/mailer.py`, `utils/google_auth.py`, נתיב השליחה ב-`email_utils.py`, כרטיס Google בהגדרות, ops `mail_*`/`mtpl_upsert`). גדרות: בלי שליחה אמיתית / בלי נגיעה בחשבון Google האמיתי / סודות לא ל-git. מפת 7 זרימות, תרחישים, 32 אינווריאנטים (M29–M32 מ-v3.42), בודק `scripts/check_mail.py`. ⚠ בבנייתו התגלה ש-`test_mail.py` (ו-`test_callback_server.py`) לא היו ברשימת `TESTS` של `release.py` — נוספו (לינט M28 שומר על זה).
- `/בנה-חיבור [הערה]` (15/9/2026; עטיפה לסקיל `yemot-connect`) — מחבר/מאמת את התוכנה מול ימות המשיח מקצה-לקצה בפקודה אחת (`scripts/connect.py`: פרטי גישה, GetSession, מזוהה, תבנית, הקלטה, /76, שרת המענה, שורש) ומשלים לבד מה שבטוח להשלים (`--set` מהסקיל הפרטי, `--fix` לתבנית, `--caller`). קריאה בלבד, בלי חיוג, בלי שורש.
- `/בדוק-הקשר [מיקוד]` (9/9/2026) — בדיקת עקביות של קובצי ההקשר (CLAUDE.md גלובלי+פרויקט, MEMORY.md וקובצי הזיכרון, NEXT_TASK.md): גרסה מול `version.py`, קבצים/מודולים/סקילים שמוזכרים וקיימים (ולהפך), אינדקס-זיכרון תואם, סתירות פנימיות. מתקן את הברור בלבד ומדווח את החשוד. **מ-14/9/2026 גם Lint לפי דפוס LLM Wiki:** דפים יתומים, מושגים בלי דף, טענות מיושנות ("נכון ל…"/"טרם"), `docs/raw/` לא מעובד, מקטעי-גרסה שהתנפחו. הגרסה הגלובלית (זיכרון+raw): `/בדוק-ידע`.
- `/סוואנה [נושא]` — הסבר את הנושא הנוכחי (או שבארגומנט) בשפה פשוטה ליהודה (לא-מתכנת) עם המחשות מעולם הסוואנה.
- `/שפר-מסך <מסך>` (10/9/2026; **מ-17/9/2026 אוטומטי** — הכרעת יהודה: בלי תוכנית, בלי הדמיית HTML, בלי עצירה לאישור) — צילום המצב הקיים (ניתוח דרך Gemini, לא PNG לצ'אט), ביקורת UX, **סבב שאלות אחד** (AskUserQuestion, עד 4) רק על מה שבאמת הכרעה של יהודה (שדות לא רלוונטיים, שתי דרכים שונות מהותית, כלל עסקי) — ואז מימוש (attributes נשמרים; שדה מוסתר שומר את ערכו), אימות ושחרור. ⚠ בשאלות לכתוב **"(מומלץ)" בעברית**, לא "Recommended" (יהודה לא הבין), ולדבר עברית מדוברת.
- `/תשאל-מסך <מסך>` (17/9/2026) — כמו `/שפר-מסך` אבל **יהודה מוביל**: ראיון של 2–3 סבבי שאלות (מה מיותר מכל השדות/כפתורים, איך עובדים בפועל, הכרעות) ואז מימוש+שחרור.
- `/תשאל-תוכנה [מיקוד]` (17/9/2026) — ראיון על כל התוכנה (בלי לסרוק קוד): מה מייגע/חסר/מיותר → רשימה מדורגת ל-`NEXT_TASK.md` → ביצוע בסבבים של 1–2 פריטים עם שחרור לכל סבב.

## בדיקות
סקריפטים עצמאיים (לא pytest): `test_all`, `test_deep`, `test_selection` (4 הכללים העסקיים — עדיפות/ניקוד/רזרבה/חוסר-נתונים), `test_data_safety`, `test_scenarios`, `test_search`, `test_priority_import`, `test_volunteer_flow`, `test_updater`, `test_sync`, `test_tzintuk`, `test_tts`, `test_callback_server`, `test_score_scale`, `test_fixes`. `dev/stress_test.py [rounds]` (offscreen, `PYTHONUTF8=1`). כל הקבצים UTF-8 (טרמינל מציג mojibake אך הנתונים תקינים).
